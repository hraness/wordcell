// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "Wordcell";
export const readmeLead = "A knowledge base for coding agents, built from Markdown, backlinks, semantic search, and Git context. It turns sources, plans, and decisions into inspectable context that agents can recover across sessions without coupling application code to the knowledge system.";
export const readmeHtml = "<h1 id=\"wordcell\">Wordcell</h1>\n<p><a href=\"https://github.com/hraness/wordcell/tree/main/skills/wordcell\">Install the Agent Skill</a>\nA knowledge base for coding agents, built from Markdown, backlinks, semantic\nsearch, and Git context.\nIt turns sources, plans, and decisions into inspectable context that agents can\nrecover across sessions without coupling application code to the knowledge\nsystem.</p>\n<p><a href=\"https://wordcell.io/docs\">Documentation</a> · <a href=\"https://www.npmjs.com/package/@hraness/wordcell\">npm package</a> · <a href=\"https://github.com/hraness/wordcell/blob/main/docs/graph-authority.md\">Graph guide</a> · <a href=\"https://github.com/hraness/wordcell/blob/main/CHANGELOG.md\">Changelog</a></p>\n<h2 id=\"install\">Install</h2>\n<p><a href=\"https://bun.sh/docs/installation\">Bun 1.3.14 or newer</a> and Git are required.\nThe CLI and TypeScript SDK run with Bun. Choose one installation source:</p>\n<pre><code class=\"language-sh\"># Canonical, versioned GitHub archive\nbun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.2/hraness-wordcell-0.21.2.tgz\nwordcell --help\n</code></pre>\n<p>The same release is mirrored to <a href=\"https://www.npmjs.com/package/@hraness/wordcell\">npm</a>:</p>\n<pre><code class=\"language-sh\">npm install --global --ignore-scripts @hraness/wordcell@0.21.2\nwordcell --help\n</code></pre>\n<p>Bun must remain in <code>PATH</code> when npm installs the commands. Exact search and\nMarkdown graph commands need no account, service, or embedding model. Optional\nsemantic search, browser capture, and PDF tools have <a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#review-lifecycle-scripts-before-enabling-optional-adapters\">additional prerequisites</a>.</p>\n<h3 id=\"use-with-a-coding-agent\">Use with a coding agent</h3>\n<p>Install the single public Agent Skill into your choice of compatible agent,\nincluding Claude Code, Codex, Cursor, or GitHub Copilot:</p>\n<pre><code class=\"language-sh\">bunx skills add hraness/wordcell#v0.21.2 --skill wordcell\n</code></pre>\n<p>Then ask:</p>\n<pre><code class=\"language-text\">Use Wordcell to find the notes and plans about packages/parser in ./kb.\nOpen the sources behind the result and explain the recorded decision.\n</code></pre>\n<p>The skill installs instructions, not a running service. It uses an existing\n<code>wordcell</code> command and prepares the pinned runtime only when needed. Installing\nit does not create a vault or modify your notes. <a href=\"https://github.com/hraness/wordcell/blob/main/skills/wordcell/SKILL.md\">Read the skill</a>.</p>\n<h2 id=\"keep-one-decision-available-to-the-next-session\">Keep one decision available to the next session</h2>\n<p>Suppose a parser must stop retrying after three attempts. Record that constraint\nin a note, then link the plan that will implement it:</p>\n<pre><code class=\"language-shell\">wordcell init kb\nwordcell note create notes/parser-contract \\\n  --title &quot;Parser contract&quot; --type concept --tag architecture \\\n  --body &quot;Parser retries stop after three attempts.&quot; --root kb\nwordcell note create plans/parser-v2 \\\n  --title &quot;Parser v2&quot; --type plan \\\n  --body &quot;The plan implements [[notes/parser-contract|the parser contract]].&quot; \\\n  --root kb\n</code></pre>\n<p>The first <code>wordcell note create</code> command stores ordinary Markdown at\n<code>kb/notes/parser-contract.md</code> and assigns its stable <code>document_id</code>. Add the\nexact code boundary to that note's frontmatter so path lookup can recover it:</p>\n<pre><code class=\"language-yaml\">repository_scopes:\n  - packages/parser\n</code></pre>\n<p>Commit the vault with the repository. The Markdown and its Git history are the\ndurable record.</p>\n<h2 id=\"recover-the-stopped-session\">Recover the stopped session</h2>\n<p>In a later session, start from the code path and inspect each independent\nsignal:</p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell search &quot;why parser retries stop&quot; --root kb --mode exact \\\n  --history --repo .\nwordcell backlinks notes/parser-contract --root kb\nwordcell history notes/parser-contract --root kb --repo .\n</code></pre>\n<table>\n<thead>\n<tr><th>Signal</th><th>What it recovers</th></tr>\n</thead>\n<tbody>\n<tr><td>Markdown</td><td>The current parser constraint in the file you can review and edit.</td></tr>\n<tr><td>Backlinks</td><td>The plan that explicitly links to the constraint.</td></tr>\n<tr><td>Exact search</td><td>The current note matched from its words, without a network request or embedding model.</td></tr>\n<tr><td>Repository context</td><td>Inherited <code>AGENTS.md</code> guides and records scoped to <code>packages/parser</code>.</td></tr>\n<tr><td>Git history</td><td>The commits and bounded co-change evidence associated with the note.</td></tr>\n</tbody>\n</table>\n<p>Together, those views recover the persisted decision, related plan, applicable\nrules, and provenance needed to resume the work. They do not reconstruct\nprivate chat or prove that the note is still correct. Open the returned\nMarkdown and guides before acting on them.</p>\n<p>The boundaries stay visible: Markdown and Git are authoritative, backlinks and\nindexes are replaceable views, and Git work is opt-in. Application code imports\nneither the vault nor a hosted knowledge service.</p>\n<h2 id=\"what-you-can-do\">What you can do</h2>\n<table>\n<thead>\n<tr><th>Task</th><th>Command</th><th>Evidence and effects</th></tr>\n</thead>\n<tbody>\n<tr><td>Find a saved decision</td><td><code>wordcell search &quot;parser retries&quot; --root kb --mode exact</code></td><td>Reads current Markdown; no model or network request.</td></tr>\n<tr><td>Recover context for code</td><td><code>wordcell context packages/parser/src/index.ts --root kb --repo .</code></td><td>Returns scoped notes, plans, and inherited <code>AGENTS.md</code> rules.</td></tr>\n<tr><td>Inspect explicit connections</td><td><code>wordcell backlinks notes/parser-contract --root kb</code></td><td>Returns notes that link to the decision.</td></tr>\n<tr><td>Search by meaning</td><td><code>wordcell search &quot;retry policy&quot; --root kb --mode hybrid</code></td><td>Adds optional local QMD keyword and vector retrieval; model setup is required.</td></tr>\n<tr><td>Query with graph proofs</td><td><code>wordcell graph query --program scope-route --scope packages/parser --root kb</code></td><td>Returns bounded results tied to the source revision. Queries do not write a cache.</td></tr>\n<tr><td>Capture a source</td><td><code>wordcell clip https://example.com/article --output kb/articles</code></td><td>Reads the selected URL and writes a Markdown bundle with a capture receipt.</td></tr>\n<tr><td>Capture a PDF</td><td><code>wordcell pdf /absolute/path/to/document.pdf --output kb/articles</code></td><td>Preserves the original PDF and extracted evidence; Poppler is required.</td></tr>\n<tr><td>Check the vault</td><td><code>wordcell check --root kb</code></td><td>Reports structural and attachment problems without editing files.</td></tr>\n</tbody>\n</table>\n<p>Use <code>--json</code> for structured output and <code>wordcell --help</code> for the complete command\nsurface. <a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#command-surface\">Full command reference</a>.</p>\n<h2 id=\"how-the-files-fit-together\">How the files fit together</h2>\n<pre><code class=\"language-text\">repository/\n├── AGENTS.md                # rules that govern edits\n├── packages/parser/\n│   └── AGENTS.md            # rules scoped to this code\n└── kb/\n    ├── index.md             # authored or managed front door\n    ├── articles/            # captured sources and assets\n    ├── notes/               # maintained explanations\n    └── plans/               # decisions and outcomes\n</code></pre>\n<p>Markdown, YAML frontmatter, explicit wikilinks, and Git hold the record. Open\nthe same files in Obsidian, a text editor, or ordinary file-search tools.\nApplication code does not need to import Wordcell or its vault.</p>\n<p>QMD supplies optional local search. <a href=\"https://oh.computer\">Oh</a>, the Hraness\nrecord and memory kernel (<a href=\"https://github.com/hraness/oh\">source</a>), is embedded\nas a derived graph authority behind an engine-neutral port: only an explicit\n<code>wordcell graph rebuild --root kb</code> writes <code>.wordcell/oh.sqlite</code>, the file stays\nignored and rebuildable, and nothing flows from the projection back into notes.\nBacklinks and typed relationships come from authored links. Percolation\nsuggests connections for review and does not add inferred edges to notes.</p>\n<p>Graph proofs explain a supported derivation from a specific source revision.\nThey do not prove that a note is true or that a missing relationship cannot\nexist. <a href=\"https://github.com/hraness/wordcell/blob/main/docs/graph-authority.md\">Graph queries and proof limits</a>.</p>\n<h2 id=\"build-with-the-typescript-sdk\">Build with the TypeScript SDK</h2>\n<p>Add the same immutable release to a Bun project:</p>\n<pre><code class=\"language-sh\">bun add --exact --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.21.2/hraness-wordcell-0.21.2.tgz\n</code></pre>\n<p>The SDK provides read-only vault sessions, metadata queries, search, graph\nproofs, Git context, and composable workflows. A session owns one snapshot;\nreopen it after Markdown changes. <a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#graph-reference\">SDK and workflow examples</a>\nshow the public imports and lifecycle.</p>\n<h2 id=\"privacy-and-boundaries\">Privacy and boundaries</h2>\n<ul>\n<li>Structural queries and exact search read local files. Optional semantic\nsearch downloads its model on first use and runs locally.</li>\n<li>URL capture contacts the requested source. Signed-in capture uses only\nexplicitly selected browser state. Review the <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">security policy</a>\nbefore using it with private sources.</li>\n<li>An agent that reads the vault follows its own provider and data-handling\nsettings. Keep private records out of public repositories and outputs.</li>\n<li>Git history is opt-in. Saved notes preserve recorded context; Wordcell does\nnot reconstruct unsaved conversations or silently record every agent action.</li>\n</ul>\n<h2 id=\"documentation\">Documentation</h2>\n<table>\n<thead>\n<tr><th>Read next</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/agent-workflow.md\">Agent workflow</a></td><td>Set up, query, maintain, and revise repository memory.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md\">Installation and command reference</a></td><td>Exact interfaces, SDK imports, optional adapters, and troubleshooting prerequisites.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/capture.md\">Web capture</a> and <a href=\"https://github.com/hraness/wordcell/blob/main/docs/pdf.md\">PDF capture</a></td><td>Save sources with provenance, assets, and explicit completeness limits.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/graph-authority.md\">Graph guide</a></td><td>Named queries, proofs, revisions, resource limits, and cache recovery.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/portfolio.md\">Portfolio federation</a></td><td>Search only selected, authorized vaults.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/design.md\">Design</a> and <a href=\"https://github.com/hraness/wordcell/blob/main/docs/agent-memory.md\">memory rationale</a></td><td>File contracts, design choices, and evaluation context.</td></tr>\n<tr><td><a href=\"https://github.com/hraness/wordcell/blob/main/docs/publishing.md#verify-a-published-release\">Release verification</a></td><td>Verify archive identity, signatures, and provenance.</td></tr>\n</tbody>\n</table>\n<h3 id=\"installation-reference\">Installation reference</h3>\n<p><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#installation-reference\">Alternative installs and optional capabilities</a>.</p>\n<h3 id=\"the-kb-vault-format\">The kb vault format</h3>\n<p>Existing <code>kb/</code>, <code>index.md</code>, <code>kb://</code> identifiers, and Markdown formats remain\ncompatible. <a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#the-kb-vault-format\">Vault format reference</a>.</p>\n<h3 id=\"command-surface\">Command surface</h3>\n<p><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#command-surface\">Complete CLI reference</a>.</p>\n<h3 id=\"capture-reference\">Capture reference</h3>\n<p><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#capture-reference\">Capture commands and dependencies</a>.</p>\n<h3 id=\"graph-reference\">Graph reference</h3>\n<p><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#graph-reference\">Graph and SDK interfaces</a>.</p>\n<h3 id=\"agent-skills\">Agent skills</h3>\n<p><a href=\"https://github.com/hraness/wordcell/blob/main/docs/reference.md#agent-skills\">Installation and runtime contract</a>.\nThe same skill is included at <code>node_modules/@hraness/wordcell/skills/wordcell/</code>.</p>\n<h2 id=\"release-notes\">Release notes</h2>\n<p>Version 0.21 adds Oh graph queries and proofs. The deprecated <code>kb</code> command was\nremoved at this boundary; use <code>wordcell</code>. Existing vaults need no migration.\n<a href=\"https://github.com/hraness/wordcell/blob/main/CHANGELOG.md\">All release notes and upgrade instructions</a>.</p>\n<h2 id=\"contributing\">Contributing</h2>\n<p>Run <code>bun run check</code> for source, architecture, package installation, types, and\nnative-helper validation. The site has its own <code>bun run check</code> under <code>site/</code>.\nSee <a href=\"https://github.com/hraness/wordcell/blob/main/CONTRIBUTING.md\">Contributing</a>, <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">Security</a>, and the <a href=\"https://github.com/hraness/wordcell/blob/main/LICENSE\">MIT License</a>.</p>\n<p><a href=\"https://github.com/hraness/wordcell/releases\">Install Wordcell from GitHub Releases</a> ·\n<a href=\"https://github.com/hraness/wordcell\">Wordcell source on GitHub</a> ·\n<a href=\"https://wordcell.io\">Wordcell overview</a></p>\n";
