// Generated from content/blog/*.md by scripts/sync-blog.ts. Do not edit.
export const blogHtml: Readonly<Record<string, string>> = {
  "introducing-wordcell": "<p>Wordcell is a local knowledge base built on the Markdown files you already keep. It indexes those files for search, builds a graph from the links you wrote, and gives every result a path back to the note it came from. The files stay where they are, in a format Obsidian, Git, and any text editor can read, and every index can be deleted and rebuilt from them.</p>\n<p>A rule like &quot;parser retries stop after three attempts&quot; tends to get decided once and then lost. It lives in a chat that closes, a commit message nobody searches, or a note on someone's laptop. The next coding agent to touch the parser starts from the code and never sees it. With Wordcell, the rule is a note the agent can find and cite.</p>\n<h2 id=\"your-markdown-stays-the-record\">Your Markdown stays the record</h2>\n<p>Wordcell reads a folder of Markdown and never moves your notes into a database of its own, as some agent memory tools do. Everything it builds on top is a view it can rebuild: exact search, optional search by meaning with a local model, backlinks, graph queries, and published sites. None of these views writes back into your notes, so losing one costs you a way to look things up and leaves the notes untouched.</p>\n<p>You can check any Wordcell answer against the file. An exact search result names the note and the line that matched. A graph result names the note that wrote a link, the note it points to, and the line where the link appears, plus a proof that ties the row to the exact version of the file it was read from. When an agent cites a Wordcell result, you open the file and read the sentence yourself.</p>\n<p>Wordcell was called KB until version 0.20.0. Releases up to 0.19.6 are still published under the package name <code>@hraness/kb</code>, so older install notes and lockfiles may show that name. Newer releases use <code>@hraness/wordcell</code>, and the vault format keeps its <code>kb</code> names, so an existing vault needs no migration.</p>\n<h2 id=\"who-it-suits\">Who it suits</h2>\n<p>Wordcell is for people who keep decisions, sources, and plans in Markdown or an Obsidian vault and work with coding agents such as Claude Code, Codex, Cursor, or GitHub Copilot. It helps most when the notes explain code: why a module rejects a tempting shortcut, which plan introduced a constraint, which source backed a decision.</p>\n<p>Some people need less. A small set of notes may be fine with plain Markdown and a text search. If you only want local document retrieval, QMD is a good fit on its own; Wordcell uses it for its optional search by meaning. And if you want a service that silently records everything an agent does, Wordcell is the wrong tool: only what you save becomes part of the record.</p>\n<h2 id=\"from-one-saved-rule-to-a-cited-answer\">From one saved rule to a cited answer</h2>\n<p>Wordcell runs as a command-line tool and TypeScript SDK on Bun, with Git. After installing it from the release on GitHub or its npm mirror, one saved rule and one search look like this:</p>\n<pre><code class=\"language-sh\">wordcell init kb\nwordcell note create notes/parser-contract \\\n  --title &quot;Parser contract&quot; --type concept \\\n  --body &quot;Parser retries stop after three attempts.&quot; --root kb\nwordcell search &quot;parser retries&quot; --root kb --mode exact\n</code></pre>\n<p>The result points to <code>notes/parser-contract</code>, which is an ordinary Markdown file you can open and edit. Exact search needs no model, account, or network request. You can also point the same search at a vault you already have, without initializing or converting it.</p>\n<p>Links you write become the graph. A plan that mentions <code>[[notes/parser-contract]]</code> shows up when you ask what depends on the rule:</p>\n<pre><code class=\"language-sh\">wordcell graph query --program backlinks --note notes/parser-contract --root kb --json\n</code></pre>\n<p>Each row names the source note, the target, and the line of the link. Wordcell computes these queries with Oh, an embedded engine that needs no separate account or service, and by default the whole graph lives in memory for one query and then closes. <a href=\"/blog/how-wordcell-uses-oh\">How Wordcell uses Oh</a> covers what the proofs contain and what they leave out.</p>\n<p>To tie a note to code, add the paths it explains to its frontmatter:</p>\n<pre><code class=\"language-yaml\">repository_scopes:\n  - packages/parser\n</code></pre>\n<p>Then an agent about to edit a file in that package can ask for the notes and repository rules that apply to it:</p>\n<pre><code class=\"language-sh\">wordcell context packages/parser/src/index.ts --root kb --repo .\n</code></pre>\n<p>The result groups current notes and plans apart from finished or superseded ones, and includes the <code>AGENTS.md</code> files that govern the path. A public Agent Skill teaches compatible agents these commands. Installing it adds instructions only; it does not create a vault, change your notes, or give an agent access to other accounts. xcb can bind one vault you choose and give its workers read-only exact search over it with citations, and saving a note back is always a separate step, as <a href=\"https://xcb.sh/blog/how-xcb-uses-wordcell\">How xcb uses Wordcell</a> explains.</p>\n<h3 id=\"clipped-pages-and-pdfs-land-in-the-same-folder\">Clipped pages and PDFs land in the same folder</h3>\n<p>Sources you read become files in the vault too. <code>wordcell clip</code> saves a web page as a Markdown bundle with its images and a record of how the page was fetched, and <code>wordcell pdf</code> keeps the original PDF beside the extracted text. Search and the graph read them alongside your notes, and the vault's own rules keep captured text as source material rather than your conclusions.</p>\n<p>Some of what people read sits behind their own logins: a newsletter they subscribe to, a member article, a page already open in their browser. Wordcell can save those pages for the person who is signed in. It can read the tab you have open without navigating it, open a page with a browser profile you select (a profile given by its folder path runs from a temporary copy, so that profile is unchanged), or use your browser's cookies for that site when the page needs nothing else. Capture only reads. It does not post, like, follow, send, delete, or submit anything. When a site answers with a login wall, paywall, or CAPTCHA, Wordcell stops rather than looking for an archived copy elsewhere. For a public page that no direct route can read, it may make one read-only lookup of that exact URL on Archive.today, which tells that service the URL. Screenshots can include private notifications, so review a bundle before sharing it.</p>\n<h2 id=\"what-stays-fixed\">What stays fixed</h2>\n<p>The project's guidelines keep Markdown and Git as the only record. Search indexes, backlinks, and the graph are views rebuilt from the files, and none of them writes back. Material from other tools follows the same rule. Wordcell's SDK can turn a verified set of Oh records into a Markdown review candidate, and that step never opens the vault, writes a note, or marks anything as reviewed. A person reads the candidate and writes the note. The agent workflow asks agents to start from <code>wordcell context</code> for the path they are changing, and the notes a person wrote about it, before running a broad search.</p>\n<h2 id=\"status-and-limits\">Status and limits</h2>\n<p>Latest release: v0.22.4. Install the versioned archive from GitHub Releases or the npm mirror with Bun 1.3.14 or newer and Git; the <a href=\"/docs\">documentation</a> has the commands.</p>\n<p>Wordcell does not write answers. It returns notes, snippets, and graph rows, and your agent writes the answer from them. A graph proof shows that a file said something at a given version, not that the note is correct. Graph queries accept vaults of up to 4,000 notes and mark a result as truncated rather than presenting a partial answer as complete. Search by meaning downloads a local model the first time you use it. An optional reranking step sends query and note snippets to a paid hosted provider and is off by default.</p>\n",
  "how-wordcell-uses-oh": "<p>Every Wordcell graph answer comes with a proof you can check against your notes. Wordcell can list the notes that link to a decision, the plans that reach it through a chain of links, and the notes that share its tags. When Wordcell says a retry plan depends on your parser rule, you want to see the line in the plan that makes the link, and you want to know that line still reads the way it did when the answer was computed.</p>\n<p>Wordcell reads your Markdown and hands the graph work to Oh, which works out the answer and keeps a proof for every row, naming the file that supports it, a fingerprint of that file's contents, and the rule that joined the pieces. The Markdown files stay the record. The graph is a copy Wordcell can throw away and build again from them.</p>\n<h2 id=\"what-oh-does-for-someone-who-has-not-used-it\">What Oh does, for someone who has not used it</h2>\n<p>Oh is a memory framework that applications embed as a library. It stores typed records, derives new facts from rules, and returns each derived answer with the chain of facts and rules that produced it. Wordcell uses the part that stores records and answers graph questions. There is no Oh account to create and no service to run. Wordcell pins one released version of Oh and upgrades only by changing that pin.</p>\n<p>Oh writes every record in one exact text form, which it calls canonical JSON, and names the record by the SHA-256 fingerprint of that text. Two programs holding the same record produce the same bytes and the same fingerprint, whatever order they assembled its fields in. So a fingerprint in a proof names exactly one record.</p>\n<h2 id=\"from-a-folder-of-notes-to-an-answer-you-can-check\">From a folder of notes to an answer you can check</h2>\n<p>When you run a graph query, Wordcell reads the whole vault as it is at that moment:</p>\n<ol>\n<li>It fingerprints the text of each Markdown file.</li>\n<li>It collects what you wrote: links, typed relationships, tags, and the code paths a note declares under <code>repository_scopes</code>. Each becomes a fact tied to the note it came from, and links keep their line.</li>\n<li>It stores each note's facts as an Oh record in canonical JSON, and fingerprints the whole snapshot as one revision.</li>\n<li>It turns the named query you asked for into a small set of Oh rules, and Oh evaluates them over that snapshot.</li>\n</ol>\n<p>By default all of this lives in memory for one query and then closes. Nothing is written into the vault and no cache file appears. Here is the smallest case, a note that links to a rule and a query for what points at the rule:</p>\n<pre><code class=\"language-sh\">wordcell note create notes/retry-review --title &quot;Retry review&quot; --type concept \\\n  --body &quot;Use [[notes/parser-contract]] when changing retry behavior.&quot; --root kb\nwordcell graph query --program backlinks --note notes/parser-contract --root kb --json\n</code></pre>\n<p>The row names <code>notes/retry-review</code> as the source, <code>notes/parser-contract</code> as the target, and the line where the link was written. Its proof names the source note, the fingerprint of that note's contents, and the fingerprint of the Oh record built from it. A longer answer, such as everything reachable within three links, adds the rule applied at each step and the facts it used. Six named queries are available: backlinks, reachability, relation-closure, scope-route, shared-tags, and shared-concepts.</p>\n<p>To keep a graph on disk, <code>wordcell graph rebuild</code> writes one to a <code>.wordcell/oh.sqlite</code> file that Git ignores, checks it by replaying it, and only then replaces the previous file. Deleting that file loses nothing, because Wordcell can rebuild it from the notes.</p>\n<h2 id=\"three-rules-that-give-a-proof-its-meaning\">Three rules that give a proof its meaning</h2>\n<p><strong>Same files, same answer.</strong> Equal snapshots of your notes produce equal rows and equal source proofs. An in-memory graph uses a fixed logical start time so the result can be reproduced in a later session; that time never claims anything about when a note was written.</p>\n<p><strong>An edit retires the old proof.</strong> A proof carries the fingerprint of each file it relies on, taken over the file's exact text. Change the file, even its spacing, and the fingerprint changes, so the old proof no longer matches. A Wordcell session holds one snapshot for its whole life, so reopen it after editing. From code, you can re-check any result against its session:</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase } from &quot;@hraness/wordcell&quot;;\n\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot; });\ntry {\n  const result = await kb.graphQuery({ program: &quot;backlinks&quot;, note: &quot;notes/parser-contract&quot; });\n  console.log(await kb.graphVerifyResult(result)); // false for modified, foreign, or stale evidence\n} finally {\n  await kb.close();\n}\n</code></pre>\n<p><strong>Answers never write back.</strong> A query never adds a link or an inferred relationship to a note. With <code>wordcell percolate --proofs</code>, Wordcell can show shared tags or shared concepts as evidence beside a suggested connection, but whether two notes should link stays your decision, made by editing the Markdown.</p>\n<h2 id=\"two-engines-held-to-the-same-bytes\">Two engines held to the same bytes</h2>\n<p>Oh ships its canonical encoder and its query engine twice: a TypeScript reference and a Rust version compiled to WebAssembly. Wordcell's graph queries prefer the Rust engine when it loads and fall back to TypeScript when it does not, with the same source revision and the same limits either way. Wordcell also uses Oh's Rust canonical encoder to fingerprint the review drafts described in the last section, again with a TypeScript fallback.</p>\n<p>Two encoders are only safe if they agree on every input, because one differing character changes a fingerprint and breaks every proof that cites it. The rule they share is short. Object keys are sorted, array order is kept, there is no extra whitespace, and numbers are written the way JavaScript's JSON writer writes them:</p>\n<pre><code class=\"language-ts\">import { canonicalJson } from &quot;@hraness/oh&quot;;\n\ncanonicalJson({ b: 1, a: [2, 1] }); // '{&quot;a&quot;:[2,1],&quot;b&quot;:1}'\n</code></pre>\n<p>Wordcell's own tests hold the Rust encoder it loads from Oh to that rule. They confirm that the WebAssembly bytes Wordcell loads match the SHA-256 recorded in the Oh package, then generate random JSON values, including nested arrays and objects and very large and very small numbers, and require the Rust and TypeScript encoders to return the same text and the same fingerprint for each. Fixed cases such as <code>1e21</code>, <code>5e-324</code>, an empty key, and an emoji key are checked on every run. Oh runs its own version of this test; How Oh keeps its TypeScript and Rust encoders byte for byte identical covers that side.</p>\n<h2 id=\"what-a-wordcell-user-can-check\">What a Wordcell user can check</h2>\n<p>Every row of a graph answer names a file you can open and, for links, the line to read. When an agent cites a Wordcell graph result, you can open the cited note and read the line yourself instead of taking the agent's word for it. A result you saved can be verified against the session it came from, and a stale one fails that check. Your knowledge stays in the notes. If you delete the graph or the Rust engine is unavailable, the notes are unchanged and give the same answers.</p>\n<h2 id=\"where-the-proofs-stop\">Where the proofs stop</h2>\n<p>A proof shows that a file said something at a given version. It does not show that the note is right. Wordcell runs only its six reviewed queries; there is no free-form query language. Absences, orphan notes, and counts are computed by Wordcell from the complete snapshot, not proved by Oh. A vault can hold up to 4,000 notes, 100,000 facts, and 64 MiB of text for graph queries, and a query that runs out of work fails instead of returning a partial answer as complete. A truncated result says so in its JSON and exits with code 4.</p>\n<p>Wordcell's search does not use Oh. Exact search and optional local search by meaning are Wordcell's own, so Oh's memory benchmarks say nothing about Wordcell's search or answers. Wordcell also does not keep an agent's memory in Oh. Wordcell's SDK can turn selected records from an Oh memory store into a Markdown review draft that lists the source records and their fingerprints and fingerprints the draft itself; preparing that draft never opens a vault or writes a note, and whether any of it becomes a note is a separate, reviewed decision. A fresh rebuild of the on-disk graph returns the same rows and source proofs, though the stored graph's own history identifiers can differ.</p>\n<p>Latest release: v0.22.4. For the full query reference, see <a href=\"/docs/graph-authority\">Query the derived graph</a> in the Wordcell documentation. <a href=\"https://oh.computer\">Oh</a> is the library behind the graph.</p>\n",
};

export const blogContents: Readonly<Record<string, readonly { href: `#${string}`; label: string }[]>> = {
  "introducing-wordcell": [{"href":"#your-markdown-stays-the-record","label":"Your Markdown stays the record"},{"href":"#who-it-suits","label":"Who it suits"},{"href":"#from-one-saved-rule-to-a-cited-answer","label":"From one saved rule to a cited answer"},{"href":"#what-stays-fixed","label":"What stays fixed"},{"href":"#status-and-limits","label":"Status and limits"}],
  "how-wordcell-uses-oh": [{"href":"#what-oh-does-for-someone-who-has-not-used-it","label":"What Oh does, for someone who has not used it"},{"href":"#from-a-folder-of-notes-to-an-answer-you-can-check","label":"From a folder of notes to an answer you can check"},{"href":"#three-rules-that-give-a-proof-its-meaning","label":"Three rules that give a proof its meaning"},{"href":"#two-engines-held-to-the-same-bytes","label":"Two engines held to the same bytes"},{"href":"#what-a-wordcell-user-can-check","label":"What a Wordcell user can check"},{"href":"#where-the-proofs-stop","label":"Where the proofs stop"}],
};
