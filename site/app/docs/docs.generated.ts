// Generated from ../docs/*.md and ../README.md by scripts/sync-docs.ts. Do not edit.
export const docHtml: Readonly<Record<string, string>> = {
  "getting-started": "<h1 id=\"get-started-with-wordcell\">Get started with Wordcell</h1>\n<p><a href=\"https://wordcell.io/docs\">Documentation index</a> · <a href=\"/docs/reference\">Installation reference</a></p>\n<p>This tutorial walks through one complete loop on a fresh vault: create it, save\na decision, find it, connect a second note, and preview a published page. Every\nstep runs locally; no account, model, or network request is involved until the\noptional last step.</p>\n<h2 id=\"install-the-cli\">Install the CLI</h2>\n<p>Install <a href=\"https://bun.sh/docs/installation\">Bun 1.3.14 or newer</a> and Git first,\nthen install the pinned release archive:</p>\n<pre><code class=\"language-sh\">bun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.0/hraness-wordcell-0.22.0.tgz\nwordcell --help\n</code></pre>\n<p>If your shell cannot find <code>wordcell</code>, add your package manager's global\nexecutable directory to <code>PATH</code> and reopen the terminal. The npm mirror\n<code>npm install --global --ignore-scripts @hraness/wordcell@0.22.0</code> carries the\nsame bytes.</p>\n<h2 id=\"create-the-vault\">Create the vault</h2>\n<p>Choose a directory where you want the vault to live, then create <code>kb/</code> inside\nit:</p>\n<pre><code class=\"language-sh\">wordcell init kb\n</code></pre>\n<p>The vault is a folder of Markdown. <code>kb/index.md</code> is its front door; you can\nopen every file it makes in Obsidian or any text editor.</p>\n<h2 id=\"save-a-decision\">Save a decision</h2>\n<pre><code class=\"language-sh\">wordcell note create notes/parser-contract \\\n  --title &quot;Parser contract&quot; --type concept --tag architecture \\\n  --body &quot;Parser retries stop after three attempts.&quot; --root kb\n</code></pre>\n<p>Open <code>kb/notes/parser-contract.md</code> and look at the file. It is ordinary\nMarkdown with YAML frontmatter; Wordcell added a stable <code>document_id</code>. You can\nedit the prose normally.</p>\n<h2 id=\"find-the-note\">Find the note</h2>\n<pre><code class=\"language-sh\">wordcell search &quot;parser retries&quot; --root kb --mode exact\n</code></pre>\n<p>The result names <code>notes/parser-contract</code> and the saved retry constraint. Exact\nmode reads the current Markdown on your machine; it downloads nothing.</p>\n<h2 id=\"connect-a-second-note\">Connect a second note</h2>\n<p>Create a plan that links to the decision:</p>\n<pre><code class=\"language-sh\">wordcell note create plans/parser-v2 \\\n  --title &quot;Parser v2&quot; --type plan \\\n  --body &quot;The plan implements [[notes/parser-contract|the parser contract]].&quot; \\\n  --root kb\n</code></pre>\n<p>The wikilink is a graph edge you authored. Ask for the decision's incoming\nconnections:</p>\n<pre><code class=\"language-sh\">wordcell backlinks notes/parser-contract --root kb\n</code></pre>\n<p>The result includes <code>plans/parser-v2</code>: the work that depends on the constraint\nis now recoverable from the note itself.</p>\n<h2 id=\"preview-the-vault-as-a-site\">Preview the vault as a site</h2>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include notes/parser-contract --include plans/parser-v2 --dry-run --json\n</code></pre>\n<p>The dry run reports what would be published without writing files. Remove\n<code>--dry-run</code> to build, then preview it:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include notes/parser-contract --include plans/parser-v2\nwordcell serve --root site --port 8080\n</code></pre>\n<p>Open <code>http://127.0.0.1:8080</code> to read the two pages and search them in the\nbrowser. Publishing writes a local folder; nothing uploads until you copy the\nfiles somewhere yourself.</p>\n<h2 id=\"where-to-go-next\">Where to go next</h2>\n<ul>\n<li>Search an existing folder without initializing:\n<code>wordcell search &quot;a phrase from your notes&quot; --root /path/to/vault --mode exact</code>.</li>\n<li><a href=\"/docs/agent-workflow\">Set up repository memory for a coding agent</a>: repository\nscopes, <code>AGENTS.md</code> rules, Git history, and the Agent Skill.</li>\n<li><a href=\"/docs/capture\">Capture a web source</a> or <a href=\"/docs/pdf\">a PDF</a> beside your notes.</li>\n<li><a href=\"/docs/reference\">Look up any command</a> in the reference, or read\n<a href=\"/docs/design\">how the pieces fit together</a>.</li>\n</ul>\n",
  "agent-workflow": "<h1 id=\"working-in-a-hranesswordcell-vault\">Working in a hraness/wordcell vault</h1>\n<p>This guide gives coding agents a conservative workflow for reading and\nmaintaining a vault. Markdown is the durable record. Tool output, catalogs,\nbacklinks, traversed paths, semantic indexes, and percolation candidates are\nviews over that record.</p>\n<h2 id=\"customize-before-preparing-a-runtime\">Customize before preparing a runtime</h2>\n<p>When the request is to set up or evolve a Wordcell, design the boundary before\ndiscovering or installing the CLI. Inspect the explicitly proposed repository\nand vault location without mutation. A new location does not need an existing\n<code>index.md</code>. Interview the user about the recurring questions the Wordcell should\nanswer, then present the exact read and write targets in a proposal.</p>\n<p>The approved proposal may choose the standard Markdown layout, no change, or\nzero to three companion skills for recurring rituals. A companion skill must\nname its inputs, surfaces, authority, approval boundary, durable outputs,\nidempotence, failure behavior, and verification. Skill discovery and ambient\napplication or account sessions grant no authority. A changed target,\nrepository, skill, account, or integration requires renewed approval.</p>\n<p>After approval, scaffold only the allowlisted paths. Matching existing bytes\nare a no-op. Stop on divergent content, path escape, a symbolic link, partial\nfailure, or an unapproved external surface. Prepare the runtime only when the\napproved operation needs it. Installation never authorizes initialization,\nindexing, QMD state, account access, or vault mutation.</p>\n<h2 id=\"orient-before-editing\">Orient before editing</h2>\n<ol>\n<li>For a repository-path question, run <code>wordcell context</code>, read the returned\n<code>AGENTS.md</code> files from root to nearest, and inspect its bounded current-memory\ngroups before opening optional hubs or running a broad search.</li>\n<li>Read <code>index.md</code>, then search filenames, frontmatter titles, aliases, and note\ntext before creating a new identity.</li>\n<li>Read the notes that already own the concept or source in question.</li>\n<li>Use the narrowest view that answers the question:\n<ul>\n<li><code>wordcell list</code> for exact frontmatter or tag filters.</li>\n<li><code>wordcell links &lt;note&gt;</code>, <code>wordcell backlinks &lt;note&gt;</code>, or <code>wordcell relation list &lt;note&gt;</code> for authored relationships.</li>\n<li><code>wordcell history</code> for direct note or repository-path provenance.</li>\n<li><code>wordcell search</code> for a fused exact, full-text, and semantic view with visible evidence.</li>\n<li><code>wordcell graph</code> for whole-vault diagnostics.</li>\n</ul>\n</li>\n</ol>\n<p>Update an existing note when the identity is unambiguous. Create a new note when the subject has a distinct durable identity, not merely because a search phrase differs.</p>\n<h2 id=\"load-repository-context-by-path\">Load repository context by path</h2>\n<p>When the question concerns a repository file or directory, start from the\nrepository root:</p>\n<pre><code class=\"language-sh\">wordcell context src/index.ts --root kb --repo .\n</code></pre>\n<p>The command lists inherited guides from the repository root toward the nearest\nscope, verified Wordcell hubs from the nearest scope back toward the root, and\nauthored records whose exact <code>repository_scopes</code> declaration contains the\ntarget. It keeps maintained knowledge, active plans, dated research, reports,\nand terminal plans in separate bounded groups. Every record reports why it\nmatched and whether its declared path exists. It prints summaries, not bodies.\nRead every returned guide; they are the normative, always-loaded source for\nownership, required commands, prohibitions, invariants, and edit gates. Open\nonly the records whose summaries apply, then expand through a bounded command:</p>\n<pre><code class=\"language-sh\">wordcell links scopes/src--25a6634263c1 --root kb --depth 1 --limit 25\nwordcell backlinks scopes/src--25a6634263c1 --root kb\nwordcell list --root kb --scope src --where area=source --json\nwordcell search &quot;why source errors retain source ranges&quot; --root kb --scope src --json\n</code></pre>\n<p>Use the exact hub ID returned by <code>wordcell context</code>. Use <code>--kind file</code> or\n<code>--kind directory</code> when a missing path cannot be classified reliably by\n<code>--kind auto</code>.</p>\n<p>A mapped scope hub carries pull-based rationale, history, examples, evidence,\nand links. It cannot override its guide or become the only home of a\nload-bearing edit rule. A guide does not need a hub.</p>\n<p>Add <code>repository_scopes</code> to a plan, maintained note, dated research record, or\nreport when that record explains work under specific code paths:</p>\n<pre><code class=\"language-yaml\">repository_scopes:\n  - src/parser\n  - tests/parser.test.ts\n</code></pre>\n<p>Use exact canonical repository-relative paths without globs. Directory scopes\nmatch descendants, while file scopes match only that file. Future paths are\nvalid and appear as absent until created. Update active or maintained records\nwhen code moves; preserve retired paths on terminal plans when they remain\nuseful history.</p>\n<p>Path-context research is a dated snapshot, not a generic note with a date. Put\nit under <code>projects/&lt;domain&gt;/market/</code> with <code>type: market-research</code>, <code>status: snapshot</code>, and a valid <code>as_of</code> date. A path-context report declares <code>type: report</code> and a valid <code>generated</code> date. Both need <code>repository_scopes</code>; other\nrecords remain searchable without appearing in those current-memory groups.</p>\n<h2 id=\"query-before-reading-broadly\">Query before reading broadly</h2>\n<p>Use typed metadata for exact selection and sorting:</p>\n<pre><code class=\"language-sh\">wordcell list --where type=plan --where status=in-progress --sort metadata.updated --order desc\nwordcell list --tag retrieval --scope packages/kb --sort inbound --order desc --json\n</code></pre>\n<p>Filters can address nested fields with dotted paths. Repeat <code>--where</code>, <code>--has</code>, or <code>--tag</code> to require every condition. Unquoted <code>true</code>, <code>false</code>, <code>null</code>, and numeric values are typed; retain inner quotes to select a string with the same spelling, as in <code>--where 'external_id=&quot;9007199254740993&quot;'</code>. JSON output includes the live metadata, tags, backlinks, and inbound and outbound contextual counts for each result.</p>\n<p>Use bounded traversal to understand explicit context around a note:</p>\n<pre><code class=\"language-sh\">wordcell links plans/improve-ingestion --direction both --depth 2 --limit 25\n</code></pre>\n<p>Traversal defaults to at most 50 notes and reports when either the node or\ncombined-connection cap truncates a high-degree neighborhood. Lower the limit\nfor agent context discipline; raise it deliberately when the structural\nquestion requires a wider view.</p>\n<p>Use the whole-vault graph only when the question spans several note\nneighborhoods:</p>\n<pre><code class=\"language-sh\">wordcell graph --root . --json\n</code></pre>\n<p>The report is rebuilt from current Markdown and returns canonical note IDs,\nresolved wikilinks, typed relationships, and diagnostics. Prefer <code>wordcell links</code>,\n<code>wordcell backlinks</code>, or <code>wordcell relation list</code> when a known note provides a narrower\nstarting point. Open returned notes and inspect edge provenance before treating\na relationship as supported. If a recurring structural question is awkward to\nanswer from the JSON report, add a focused command with a bounded contract\nrather than a second graph store.</p>\n<p>Use hybrid search for broad recall while preserving exact evidence:</p>\n<pre><code class=\"language-sh\">wordcell search &quot;capturing a signed-in virtualized page&quot;\nwordcell search &quot;capture&quot; --tag ingestion --where status=accepted\nwordcell search &quot;notes/write-path&quot; --mode exact\nwordcell search &quot;browser profile&quot; --mode keyword\n</code></pre>\n<p>The default combines a live exact scan with QMD's local full-text and compact\nembedding rankings, then reports each lane's evidence. It skips query expansion\nand reranking models. The first hybrid or semantic query downloads the embedding\nmodel and builds a local cache; subsequent queries incrementally index changed\nMarkdown. Wordcell validates a bounded immutable Markdown projection before QMD\nindexes it. Shared-database mutations are serialized across local agent\nprocesses, same-generation readers can overlap, and a projection change waits\nfor older readers to close. <code>wordcell index</code> can prewarm that cache. <code>--mode exact</code>\nrequires no model.</p>\n<p>Reranking is opt-in through <code>--rerank typesafe</code>. It uses <code>jev-1.13.0</code> and sends\nthe query plus each candidate's identifier, title, vault-relative path, and at\nmost 512 UTF-8 bytes of snippet text to TypeSafe. Enable it only for vaults\napproved for external processing and provider input-token charges. Use\n<code>--rerank-limit 25</code> to bound the window (2–25 candidates); four requests run\nconcurrently under one eight-second deadline with no retries. Exact identities\nremain first. Results retain their original scores and retrieval evidence.</p>\n<p>The CLI reads <code>TYPESAFE_API_KEY</code>, an explicit <code>TYPESAFE_API_KEY_FILE</code>, or the\nowner-only file <code>~/.config/wordcell/typesafe-api-key</code> (honoring\n<code>XDG_CONFIG_HOME</code>). Keep credentials outside the repository. A missing key or\nprovider failure retains baseline ordering and marks the rerank lane\nunavailable or degraded. Inspect its structured <code>rerank</code> receipt for attempted\nrequests, known usage, elapsed time, and whether usage is complete; an unknown\ncharge is never reported as zero. A successful exit alone does not establish\nthat reranking occurred. Omit <code>--rerank</code> for local-only retrieval.</p>\n<p>For a repository's ordinary KB searches, prefer its approved, pinned\n<code>kb:search</code> script when present. That script declares the vault and processing\nchoice. Read returned notes before acting; model probabilities are ranking\nsignals, not proof of truth. Explicit priority rules still run last.</p>\n<p>Graph neighbors and Git provenance are returned separately from the primary\nrank. Use <code>--related &lt;note&gt;</code> to seed a bounded explicit neighborhood,\n<code>--no-graph</code> when it is unnecessary, and <code>--history</code> when recent note\nprovenance helps. Omitted history performs no Git work. Use <code>--require-history</code>\nwhen a result without Git provenance is not usable; the command then fails\ninstead of returning an unavailable or incomplete Git lane. Automatic history\nkeeps normal commits usable when one\ncommit exceeds its changed-path detail limit. The affected note retains that\ncommit's identity and vault-local association, while diagnostics and packed\ncontext identify the incomplete co-change evidence. These views explain a\nresult; they do not create links or establish that a claim is correct.</p>\n<p>Ask Git directly when text retrieval is unnecessary:</p>\n<pre><code class=\"language-sh\">wordcell history notes/write-path --root kb --repo . --json\nwordcell history search src/parser.ts --root kb --repo . --json\n</code></pre>\n<p>The second command searches bounded commit subjects, note paths, and co-change\npaths. Co-change is evidence of historical association, not causation, and it\nnever writes repository scopes or graph edges.</p>\n<p>For several related operations, open one read-only SDK session and reuse its\nsingle vault scan:</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase } from &quot;@hraness/wordcell/sdk&quot;;\n\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot;, repository: &quot;.&quot; });\ntry {\n  const plans = kb.list({\n    filters: [{ kind: &quot;equals&quot;, path: &quot;type&quot;, value: &quot;plan&quot; }],\n  });\n  const context = await kb.search({\n    query: &quot;current ingestion plan&quot;,\n    history: &quot;auto&quot;,\n  });\n  const source = kb.read(context.results[0]?.id ?? plans[0]?.id ?? &quot;index&quot;);\n  console.log(source.content);\n} finally {\n  await kb.close();\n}\n</code></pre>\n<p>The session does not watch the filesystem. Close and reopen it after any note\nwrite, authoring command, capture, refresh, or Git update that should appear in\nlater results. Use the bounded <code>defineWorkflow</code> and <code>runWorkflow</code> API when\nindependent retrieval branches can run concurrently; QMD work remains\nserialized by default. Import <code>decisionContextWorkflow</code>,\n<code>explainChangeWorkflow</code>, or <code>planRadarWorkflow</code> from <code>@hraness/wordcell/workflows</code>\nwhen one of those common DAGs matches the task.</p>\n<p><code>decisionContextWorkflow</code> returns a bounded untrusted context envelope.\n<code>explainChangeWorkflow</code> and <code>planRadarWorkflow</code> return raw Wordcell and Git result\nobjects for application-side inspection; their source-derived strings remain\nuntrusted data. Never execute those fields or insert them into an instruction\nchannel. Select and project the needed fields through\n<code>packUntrustedSearchContext</code> or <code>@hraness/wordcell/untrusted-content</code> before an agent\nhandoff.</p>\n<p>Use <code>history: &quot;required&quot;</code> when provenance is mandatory, or\n<code>history: { policy: &quot;required&quot;, noteLimit: 5 }</code> when the same requirement needs\ncustom bounds. Custom workflows use a staged builder so every node sees a typed\nWordcell session and only its declared dependency results:</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase } from &quot;@hraness/wordcell/sdk&quot;;\nimport { defineWorkflow, runWorkflow } from &quot;@hraness/wordcell/workflow&quot;;\n\ntype Input = { readonly query: string };\n\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot;, repository: &quot;.&quot; });\nconst research = defineWorkflow&lt;Input&gt;(&quot;research&quot;)\n  .node({\n    id: &quot;search&quot;,\n    resource: &quot;qmd&quot;,\n    run: ({ input, kb }) =&gt; kb.search({ query: input.query }),\n  })\n  .node({\n    id: &quot;pack&quot;,\n    needs: [&quot;search&quot;],\n    run: ({ result }) =&gt; result(&quot;search&quot;).results.map(({ path }) =&gt; path),\n  })\n  .output(&quot;pack&quot;);\n\nconst execution = await runWorkflow(research, {\n  kb,\n  input: { query: &quot;current ingestion plan&quot; },\n});\nconsole.log(execution.output);\nawait kb.close();\n</code></pre>\n<h2 id=\"evaluate-retrieval-changes-on-a-frozen-corpus\">Evaluate retrieval changes on a frozen corpus</h2>\n<p>Use a versioned evaluation manifest when a ranking, filter, graph, context, or\nhistory change needs empirical evidence. Author query prose, structured lane\ninputs, and 0–3 relevance judgments before inspecting the candidate run. Keep\ndevelopment and test queries distinct.</p>\n<pre><code class=\"language-sh\">wordcell evaluate kb/evaluations/repository-memory-v1.json \\\n  --root kb --repo . --split test \\\n  --model-file /path/to/the/recommended-model.gguf \\\n  --cache-state warm --json &gt; kb/reports/repository-memory-v1.json\n</code></pre>\n<p>The command verifies the manifest's exact Git commit and vault tree and rejects\ndirty vault content before opening retrieval. It runs the selected built-in\nexact, keyword, semantic, hybrid, metadata, graph, path-context, and Git\nadapters through one bounded snapshot. Use repeated <code>--retriever</code> values for an\nablation. Semantic and hybrid runs verify the file against the pinned model\nSHA-256, give those exact bytes to QMD, and verify them again after retrieval.\nThe report retains the stable model URI, revision, and digest without storing\nthe local path.</p>\n<p>Read per-query rankings, unavailable or degraded lanes, timings, raw resource\ncounters, per-class metrics, and paired intervals together. A small frozen\ncorpus is a regression and harness proof. It is not evidence about another\nvault, machine, scale, cache state, or end-to-end agent outcome.</p>\n<h2 id=\"preserve-authority-boundaries\">Preserve authority boundaries</h2>\n<ul>\n<li>Captured articles preserve what the source said and how it was acquired. Put later interpretation in a maintained note.</li>\n<li>Riffs preserve the speaker's first-person claims and uncertainty. Clean transcription noise without converting the riff into an essay by someone else.</li>\n<li>Maintained notes own synthesis, comparison, and current understanding.</li>\n<li>Plans own proposed work, decisions, execution state, and verification evidence.</li>\n</ul>\n<p>Do not silently rewrite a capture to match a later conclusion. Link the source to the maintained interpretation instead.</p>\n<h2 id=\"grow-durable-plans\">Grow durable plans</h2>\n<p>Before creating a plan, use <code>wordcell list --where type=plan</code> and search the vault for an existing artifact that owns the outcome. Prefer extending that file to creating a parallel progress log.</p>\n<p>A durable plan records an observable outcome, context, scope and non-goals,\nconstraints, decisions, dependency-ordered work, verification, and recovery.\nKeep its frontmatter easy to query, including <code>type: plan</code>, a description, an\narea, applicable <code>repository_scopes</code>, and one status from <code>proposed</code>,\n<code>accepted</code>, <code>in-progress</code>, <code>blocked</code>, <code>completed</code>, <code>superseded</code>, or <code>cancelled</code>.\nAdd dated findings, decisions, review evidence, and the final result to the same\nfile as the work develops. Before setting a terminal status, fill <code>## Result</code>\nand <code>## Durable memory</code>: link each reusable conclusion to the maintained note,\nguide, documentation, or checked code contract that now owns it, or state that\nno durable promotion was needed.</p>\n<p>The packaged <code>wordcell</code> Agent Skill routes plan requests to the complete authoring workflow. It treats a plan as a growing implementation record, not a disposable checklist or a directory of satellite status documents.</p>\n<h2 id=\"link-for-meaning\">Link for meaning</h2>\n<p>Use vault-root wikilinks without <code>.md</code>, for example:</p>\n<pre><code class=\"language-md\">The capture strategy follows [[notes/bounded-acquisition|bounded acquisition]] so incomplete threads remain visible as incomplete.\n</code></pre>\n<p>Use ordinary Markdown links for external URLs. Add an internal link where the relationship helps a reader understand the sentence. Do not add bare reciprocal links, manufactured <code>Related</code> lists, or links whose only purpose is to improve graph counts.</p>\n<p>Backlinks are derived from explicit wikilinks. Never paste generated backlink sections into notes. Catalog links in <code>index.md</code> are navigation and do not establish contextual relationships. Mention candidates are prompts for review, not instructions to edit.</p>\n<p>Promote a reusable idea into an ordinary concept note:</p>\n<pre><code class=\"language-sh\">wordcell note create notes/local-first --title &quot;Local-first&quot; --type concept --tag architecture\n</code></pre>\n<p>Author a typed relationship from the note that owns the assertion:</p>\n<pre><code class=\"language-sh\">wordcell relation add notes/write-path supports notes/durable-agent-memory\n</code></pre>\n<p>Predicates use lower-kebab-case and targets use exact vault-root IDs without\n<code>.md</code>. Recommended predicates for common Wordcell claims are <code>synthesizes</code>,\n<code>evidenced-by</code>, <code>informed-by</code>, <code>supersedes</code>, and <code>contradicts</code>. The list is\nadvisory; a vault may use another canonical predicate whose meaning its prose\nestablishes. Explain the assertion in prose or evidence. Do not author inverse,\nreciprocal, transitive, or similarity-derived edges. External or unclassified\nmaterial does not gain a relationship automatically.</p>\n<h2 id=\"capture-a-source\">Capture a source</h2>\n<p>Check the local environment and the installed adapters before relying on optional capabilities:</p>\n<pre><code class=\"language-sh\">wordcell doctor\nwordcell adapters\n</code></pre>\n<p>Inspect an unfamiliar source before writing it:</p>\n<pre><code class=\"language-sh\">wordcell inspect https://example.com/article\nwordcell inspect https://example.com/article --json\n</code></pre>\n<p>Capture a URL or the page already open in the signed-in browser:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/article --output articles\nwordcell clip current --browser-live --output articles\n</code></pre>\n<p>Review the Markdown and <code>capture.json</code> together. Preserve the recorded status, warnings, counts, acquisition attempts, and artifact outcomes. <code>partial</code> is a useful result, not a defect to hide. Do not infer thread completeness from visible prose alone.</p>\n<p>The capture command reads content and writes a local bundle. It does not post, like, follow, send, delete, or submit on the source service.</p>\n<p>Capture a local or public remote PDF through its separate ingestion path:</p>\n<pre><code class=\"language-sh\">wordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\nwordcell pdf &quot;https://example.com/document.pdf&quot; --output articles\n</code></pre>\n<p>Review native headings, OCR-derived text, and retained source images together.\nThe bundle keeps <code>source.pdf</code> byte-for-byte and never records its original\nabsolute path. A text-bearing screenshot still needs its source-image\nreference; a useful native-text result does not hide an unprocessed image or\npage.</p>\n<p>Use the source inbox to find recent captures that have not yet been linked from\nmaintained knowledge:</p>\n<pre><code class=\"language-sh\">wordcell inbox --root kb --limit 25 --json\n</code></pre>\n<p>The inbox is advisory. A source can remain an intentional leaf; review it and\nrecord a disposition only when it changes maintained understanding.</p>\n<h2 id=\"finish-every-change\">Finish every change</h2>\n<p>After adding, renaming, moving, or materially revising notes:</p>\n<pre><code class=\"language-sh\">wordcell percolate &quot;&lt;changed-note-id&gt;&quot; --root . --limit 25 --json\nwordcell refresh --root .\nwordcell graph --root .\nwordcell check --root .\n</code></pre>\n<p>Any code-mode session opened before those writes is now stale. Close it before\nthe write and open a new session after the final check.</p>\n<p>Open the evidence cited by each percolation candidate. Promote only concepts\nlikely to be reused and relationships established by the source material.\nIn Percolation Result V2, a missing relationship is an unordered endpoint pair\nwith a required predicate. It does not choose a source, direction, or\n<code>related-to</code> fallback. Read both endpoints and author the directed assertion\nonly when their prose and evidence establish it. Parse historical unversioned\nV1 results explicitly; do not silently transform their suggested predicate\ninto a V2 claim.\nReview broken and ambiguous links, typed relationships, local attachments, and\nrepository-scope advisories first. Then inspect orphans and high-confidence\ntitle or alias mentions in context. Add a suggested link only when it improves\nthe prose. Finish with a clean <code>wordcell check</code>. In a managed vault, inspect the\ncatalog diff; in an authored vault, refresh leaves the front door unchanged and\n<code>wordcell catalog --root .</code> provides an exhaustive disposable inventory.</p>\n<p>When multiple agents are editing different notes, each lane runs:</p>\n<pre><code class=\"language-sh\">wordcell check --root . --no-catalog\n</code></pre>\n<p>The integrating agent runs one final refresh and normal check after the lanes\njoin. Managed vaults serialize their one catalog write. Authored vaults declare\n<code>kb_catalog: authored</code> in <code>index.md</code> and avoid that write entirely. No shared\ngraph database or generated fact log exists to contend on.</p>\n<p>If the change adds, removes, renames, or moves a scope hub, changes its\n<code>type</code> or <code>scope</code>, or edits an <code>kb:context</code> marker, also run:</p>\n<pre><code class=\"language-sh\">wordcell agents identity packages/parser --json\nwordcell agents check --root kb --repo .\n</code></pre>\n<p>Use <code>wordcell agents identity</code> when creating or moving a mapping; it derives the\ncanonical path and marker without writing either file. The gate checks\ncanonical content-derived IDs, exact repository-relative\nscopes, collisions, real confined scope directories and guide files, guide\nshape, and reciprocal markers. A scope move changes identity, so rename the hub\nand marker together. Unmapped <code>AGENTS.md</code> files are valid.</p>\n<p>Use the audit when reviewing instruction size or inheritance:</p>\n<pre><code class=\"language-sh\">wordcell agents audit --root kb --repo .\nwordcell agents audit --root kb --repo . --json\n</code></pre>\n<p>The audit adds deterministic per-guide and per-section measurements,\ninherited-chain totals, long-bullet advisories, and exact duplicate-rule\nadvisories. Inspect them as refactoring leads. Length is not correctness, and a\nrule that must be known before editing stays in <code>AGENTS.md</code> even when it is\nlong. Guide discovery skips common generated and vendor directories and never\nfollows symbolic-link directories.</p>\n",
  "capture": "<h1 id=\"capture-web-content\">Capture web content</h1>\n<p><code>wordcell clip</code> saves public and signed-in web content as an auditable Markdown bundle. It combines bounded structured adapters, HTTP extraction, browser rendering, a read-only Archive.today fallback, localized assets, and explicit completeness metadata.</p>\n<h2 id=\"check-local-capabilities\">Check local capabilities</h2>\n<p>Run the diagnostics before using local search, browser state, PDF ingestion, or video capture:</p>\n<pre><code class=\"language-sh\">wordcell doctor\nwordcell adapters\n</code></pre>\n<p><code>wordcell doctor --json</code> reports the installed runtime, QMD and static semantic-search\nprerequisites, extraction dependencies, browser support, profile display names,\nyt-dlp, ffmpeg, Poppler (<code>pdfinfo</code> and <code>pdftohtml</code>), and Tesseract. The semantic\nreport distinguishes model-free keyword readiness from SQLite and sqlite-vec\nvector prerequisites; it does not inspect or download the embedding model. The\nPoppler pair is required by <code>wordcell pdf</code>; Tesseract adds local OCR for scans and\nscreenshots.\n<code>wordcell adapters --json</code> returns the current platform capability matrix.</p>\n<h2 id=\"capture-or-inspect-a-page\">Capture or inspect a page</h2>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/article\nwordcell inspect https://example.com/article\nwordcell inspect https://example.com/article --json\n</code></pre>\n<p>The default route tries stable public structured data when available, bounded HTTP extraction, and a rendered browser when the platform or result requires one. If those routes produce no usable representation, Wordcell may make one read-only lookup for the exact URL through Archive.today's fixed <code>archive.ph/newest/</code> route. An authentication, paywall, CAPTCHA, access-control, or rate-limit response disables that fallback instead of using an archive to bypass the current source's controls. Wordcell never submits the URL for archiving, retries through aliases, or lets archived HTML displace a complete or partial Hacker News or Bluesky structured capture. It validates the source through the public-network boundary before disclosure, shares one deadline across validation and provider requests, and binds every snapshot and redirect to the exact source. An archived result is rescored and always reported as <code>partial</code>; it keeps the original canonical URL and records the timestamped snapshot as its acquisition URL. Inspection returns the selected Markdown and capture report without writing artifacts.</p>\n<p>By default, a capture writes <code>kb/articles/&lt;slug&gt;/</code>:</p>\n<pre><code class=\"language-text\">&lt;slug&gt;/\n  &lt;slug&gt;.md\n  capture.json\n  url-metadata.json # after an optional metadata backfill\n  assets/\n  evidence/       # only when requested\n</code></pre>\n<p>The Markdown records source and capture metadata. <code>capture.json</code> records the acquisition attempts, selected extractor, scope, status, item counts, warnings, asset hashes, and requested artifact outcomes. The optional <code>url-metadata.json</code> is a separate tool-owned enrichment record. It does not rewrite acquisition provenance in <code>capture.json</code>. Writes stage beside the target and install with an atomic rename. <code>--force</code> replaces only a compatible clip-owned bundle and restores the previous bundle if installation fails.</p>\n<p>Set <code>KB_CLIP_OUTPUT</code> to change the default output root, or pass <code>--output &lt;directory&gt;</code> for one command. Set <code>KB_CLIP_USER_AGENT</code> or pass <code>--user-agent &lt;value&gt;</code> to override the default request user agent.</p>\n<h2 id=\"inspect-and-verify-a-saved-bundle\">Inspect and verify a saved bundle</h2>\n<pre><code class=\"language-sh\">wordcell capture show kb/articles/example\nwordcell capture show kb/articles/example --verify-assets\nwordcell capture verify kb/articles/example\nwordcell capture verify kb/articles/example --verify-assets --json\n</code></pre>\n<p><code>show</code> reads the document, compares its v4 byte count and digest when present, and prints its Markdown as untrusted data. <code>verify</code> reports integrity without printing the document in its text output. Add <code>--verify-assets</code> to either command to read and hash every listed asset. A mismatch or an unavailable authoritative document digest returns exit status 3.</p>\n<p>The JSON forms preserve the same disclosure boundary: <code>show --json</code> includes the stored Markdown as explicitly untrusted inspection data, while <code>verify --json</code> returns only integrity metadata and issues. Verification JSON omits both the Markdown and retained source HTML so an integrity check does not accidentally disclose captured content.</p>\n<p>Schema v4 records <code>document.path</code>, <code>document.bytes</code>, and <code>document.sha256</code> in <code>capture.json</code>. The lowercase SHA-256 digest covers the exact credential-redacted, newline-terminated Markdown bytes that Wordcell writes. It does not cover <code>capture.json</code>, source HTML, or assets. Each asset has a separate byte count and SHA-256 digest. Schema versions one through three report document integrity as <code>unavailable</code> because they do not contain the v4 document record; <code>verify</code> does not present that state as success.</p>\n<p>When asset verification is requested, a listed asset that is absent is an integrity mismatch rather than an operational reader crash. It produces an <code>asset-integrity</code> issue and exit status 3, like a digest or byte-count mismatch. Structural alias, link, confinement, and resource-budget failures remain operational errors because the reader cannot safely characterize the requested bytes.</p>\n<p>Bundle inspection keeps the canonical root open, rejects linked or aliased path ancestors and linked leaves, and rechecks root, ancestor, and file identities around every read. Asset verification is also bounded by file count, per-file bytes, aggregate bytes, and elapsed time. The SDK defaults are 1,000 files, 100 MB per file, 500 MB total, and 30 seconds; callers may raise them only to the capture contract's hard ceilings. These checks protect against hostile stored bundle structure and ordinary concurrent replacement. A malicious process with the same filesystem authority that can swap and restore path components during a system call is outside the pure-JavaScript reader's boundary; do not inspect bundles in a directory concurrently writable by an untrusted local process.</p>\n<p>Source HTML remains opt-in when you read a bundle:</p>\n<pre><code class=\"language-sh\">wordcell capture show kb/articles/example --include-source-html\n</code></pre>\n<p>This flag has an effect only when the capture used <code>--evidence source</code> or <code>--evidence all</code>. Wordcell removes active subtrees and form state, redacts credential-shaped values, and stores the result as inert HTML. Treat the retained text as hostile data, not instructions. It can still contain private content or text intended to influence an agent.</p>\n<h2 id=\"use-git-for-capture-history\">Use Git for capture history</h2>\n<p>Commit retained capture bundles to Git, then compare the current Markdown document with an earlier revision:</p>\n<pre><code class=\"language-sh\">wordcell capture diff kb/articles/example\nwordcell capture diff /path/to/repository/kb/articles/example --repo /path/to/repository --ref main\nwordcell capture diff kb/articles/example --repo . --ref main --json\n</code></pre>\n<p>The bundle must be inside the selected repository. The default reference is <code>HEAD</code>; <code>--ref</code> accepts a bounded branch, tag, or commit name, not a revision expression such as <code>HEAD~1</code>. Wordcell validates the work tree and ref separately, so a bad repository or ref is not mislabeled as a missing file. The command reports <code>changed</code>, <code>unchanged</code>, or <code>missing-at-ref</code> for the exact Markdown path and emits a bounded Git diff when it changed. It rereads and matches the capture digest after diff generation, rejecting a concurrent change instead of pairing one digest with another snapshot's diff. It does not compare the manifest, source HTML, or assets. Run <code>wordcell capture verify</code> separately when integrity matters. Git is the capture version history; Wordcell does not maintain a second content-history database.</p>\n<h2 id=\"track-capture-jobs-programmatically\">Track capture jobs programmatically</h2>\n<p>Use the optional <code>@hraness/wordcell/clip/jobs</code> ledger when a service needs durable capture progress:</p>\n<pre><code class=\"language-ts\">import {\n  completeCaptureJob,\n  createCaptureJob,\n  openCaptureJobStore,\n  updateCaptureJob,\n} from &quot;@hraness/wordcell/clip/jobs&quot;;\n\nconst store = await openCaptureJobStore(&quot;/absolute/path/to/capture-jobs&quot;);\nlet job = await createCaptureJob(store, { target: &quot;https://example.com/article&quot; });\njob = await updateCaptureJob(store, job.id, {\n  expectedRevision: job.revision,\n  phase: &quot;acquiring&quot;,\n});\njob = await completeCaptureJob(store, job.id, {\n  expectedRevision: job.revision,\n  status: &quot;complete&quot;,\n});\n</code></pre>\n<p>Create the dedicated canonical mode-0700 store directory before opening it, and pass the returned store to every operation. The opener rejects filesystem aliases, links, and group/world access. Each job is one private, bounded UUID JSON record. Creation starts revision 1 in <code>running</code> and <code>queued</code>; updates require the expected revision, append attempts and warnings, and move phases forward. Completion records a capture status. It can also record <code>bundle: { path, sha256 }</code>; use <code>capture.json</code>'s v4 <code>document.sha256</code> as <code>sha256</code> because the ledger does not calculate a separate bundle digest. <code>failCaptureJob</code> records an operational error separately from capture status. <code>readCaptureJob</code> and <code>listCaptureJobs</code> inspect retained records. A terminal <code>completed</code> or <code>failed</code> record cannot transition again, and a process crash leaves the last <code>running</code> record visible.</p>\n<p>The ledger redacts credential-shaped values and strips terminal controls from targets, attempts, warnings, bundle paths, and errors. A store-local, heartbeat-backed filesystem lease serializes cooperating processes across revision validation, atomic whole-record replacement, record verification, and directory synchronization; exactly one same-revision transition can succeed. There is no shared append log. Records remain until the caller applies an explicit external retention policy. The module provides no delete or automatic pruning API, and it does not replace Git as capture history. The lease is a same-host coordination mechanism, not a distributed lock for multi-host network filesystems, and a malicious same-user writer with direct store access remains outside its guarantee.</p>\n<h2 id=\"select-acquisition-and-scope\">Select acquisition and scope</h2>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/article --mode http\nwordcell clip https://example.com/application --mode browser\nwordcell clip https://example.com/post --scope page\nwordcell clip https://example.com/post --scope thread\nwordcell clip https://example.com/discussion --scope comments\n</code></pre>\n<p><code>auto</code> is the normal acquisition mode. <code>http</code> disables browser fallback but retains the final read-only archive lookup. <code>browser</code> requires rendered state and does not query Archive.today. Saved HTML can be imported without browser automation:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/article --html &quot;$KB_SAVED_HTML&quot;\nwordcell clip https://example.com/article --html - &lt; page.html\n</code></pre>\n<p>Default resource bounds are 30 seconds per request, process, or extraction operation; 500 scoped items; depth 16; 25 MB of HTML; 100 MB per asset; and 500 MB across assets. Browser observation also has fixed DOM and scroll ceilings. Reaching a bound is recorded and can downgrade a result to <code>partial</code>.</p>\n<h2 id=\"capture-images-media-and-evidence\">Capture images, media, and evidence</h2>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/article --media none\nwordcell clip https://example.com/article --media images\nwordcell clip https://example.com/video --media all\nwordcell clip https://example.com/article --evidence source\nwordcell clip https://example.com/article --evidence screenshot\nwordcell clip https://example.com/article --evidence all\n</code></pre>\n<p>Image downloads are signature-checked, content-addressed, byte-bounded, and\nrewritten to relative bundle paths. Failed images remain inert links. Video\nposters and thumbnails exposed by the page are localized without downloading\nthe full video.</p>\n<p>For YouTube, the normal capture route uses yt-dlp to retain the title,\ndescription, duration, channel, thumbnail, and one exact-language transcript\nwhen those fields are available. Full audio/video download remains opt-in:\n<code>--media all</code> invokes yt-dlp for accessible media, and ffmpeg may be required\nfor merging or remuxing.</p>\n<p>Source evidence is sanitized into inert HTML with credential-shaped values redacted and a deny-all content security policy. Screenshots are viewport-only pixels and are not structurally sanitized. They may contain private content or notifications, so review them before retaining or sharing a bundle.</p>\n<h2 id=\"capture-a-signed-in-page\">Capture a signed-in page</h2>\n<p>If the page is already open, read the current tab without navigating it:</p>\n<pre><code class=\"language-sh\">wordcell clip current --browser-live\nwordcell clip current --cdp 9222\n</code></pre>\n<p>For <code>--browser-live</code>, first enable Chrome's local debugging connection at <code>chrome://inspect/#remote-debugging</code> (Chrome 144+). If Chrome was launched with an explicit loopback debugging port, pass that numeric port to <code>--cdp</code> instead.</p>\n<p>To open a URL with existing browser state, select a profile name or path. Path-backed profiles run from a temporary copy, so the source profile is unchanged:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/member/article --browser-profile &quot;$KB_CAPTURE_PROFILE&quot;\n</code></pre>\n<p>Cookie-backed HTTP capture is useful when the page does not require local storage, IndexedDB, or other browser-only state:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/member/article --cookie-source chrome --cookie-profile &quot;Default&quot;\nwordcell clip https://example.com/member/article --cookies-file &quot;$KB_COOKIES_FILE&quot;\n</code></pre>\n<p>Choose at most one browser session and one cookie input. A browser session may use a separate cookie input for later asset or media downloads because attached browser state is not exported.</p>\n<p>Current-tab capture issues no navigation, click, form, typing, upload, or submit command. URL-based browser capture may navigate and scroll within fixed work limits, taking bounded observations as content is rendered. Both routes are ingestion-only: they do not post, like, follow, send, delete, or submit.</p>\n<h2 id=\"interpret-status-and-counts\">Interpret status and counts</h2>\n<p>Capture status is one of:</p>\n<ul>\n<li><code>complete</code>: the selected bounded representation was acquired without a known missing boundary.</li>\n<li><code>partial</code>: useful content was retained, but a count, cursor, configured bound, hidden branch, or generic rendered representation prevents a completeness claim.</li>\n<li><code>auth-required</code>: the selected routes reached an authentication gate.</li>\n<li><code>blocked</code>: the source returned a block or verification shell.</li>\n<li><code>unsupported</code>: no route produced a usable representation.</li>\n</ul>\n<p>For page scope, counts cover primary entries. For thread and comment scopes, counts cover replies or comments and exclude roots, quotes, ancestors, and pagination markers. Generic rendered conversations often report <code>capturedItems: 0</code> because visible prose does not prove a trustworthy per-item tree.</p>\n<p>A <code>complete</code> or <code>partial</code> capture exits with status 0. Authentication, blocked, and unsupported outcomes use status 3. Argument errors use status 2, environment diagnostic failures use status 4, and operational errors use status 1. Automation should inspect the structured status and warnings rather than relying only on the process exit code.</p>\n<h2 id=\"platform-routes\">Platform routes</h2>\n<ul>\n<li>Hacker News uses the official Firebase item API for bounded recursive discussions.</li>\n<li>Bluesky uses public AT Protocol resolution and thread APIs.</li>\n<li>Reddit first tries its unofficial public listing JSON and falls back when that surface is denied or changes.</li>\n<li>X uses article extraction plus rendered capture; unloaded or virtualized replies remain partial.</li>\n<li>Substack uses article extraction and a signed-in browser for subscriber text when selected.</li>\n<li>GitHub issues, pull requests, and discussions use the Defuddle GitHub extractor, with a signed-in browser fallback for private repositories.</li>\n<li>Discourse topics use the Defuddle Discourse extractor and rendered fallback.</li>\n<li>YouTube adds bounded yt-dlp video context—title, description, duration,\nchannel, thumbnail, and an available exact-language transcript—to HTTP or\nrendered page capture. Full audio/video remains opt-in with <code>--media all</code>.</li>\n<li>Instagram, Facebook, LinkedIn, TikTok, Threads, WhatsApp Web, and arbitrary\napplications use rendered or saved-HTML capture. They do not gain a\ntrustworthy item tree without a dedicated adapter.</li>\n</ul>\n<p>Platform markup and endpoints change. Run <code>wordcell adapters</code> for the installed version's current claims.</p>\n<h2 id=\"backfill-saved-url-metadata\">Backfill saved URL metadata</h2>\n<p>The URL metadata command enriches every external URL record under <code>articles/</code>, including legacy web clips and remote PDF sources. It skips local-only PDFs. It writes one sibling <code>url-metadata.json</code> without changing the source Markdown or synthesizing an old capture manifest.</p>\n<p>Build the isolated Rust helper, then run the resumable backfill:</p>\n<pre><code class=\"language-sh\">wordcell url-metadata tool build\nwordcell url-metadata backfill --root .\n</code></pre>\n<p>The helper pins <a href=\"https://github.com/MikeLuu99/searxng-rust\"><code>MikeLuu99/searxng-rust</code></a> at revision <code>f40a00ea67a857ee996e1caba1ebab3ee7a14a47</code>. Its crate is named <code>metadata-search-engine-rs</code>; it queries DuckDuckGo, Brave, Startpage, and Yahoo and combines their results. Wordcell resolves those four fixed hosts through its public-network boundary, passes only the validated addresses to the helper, disables redirects, and runs the upstream engines serially. A bounded global allocator caps Rust-owned response buffers and parsed data at 128 MiB. Linux also applies a 256 MiB process data ceiling. macOS rejects a lowered <code>RLIMIT_DATA</code>, so it retains the allocator ceiling plus the parent's subprocess deadline and output bounds. Wordcell passes requests over stdin to a private subprocess with ambient proxy and credential variables removed, bounds the subprocess deadline and output, parses the closed response itself, and does not use the upstream URL normalizer for saved identity.</p>\n<p>This operation discloses each eligible source URL as an exact search query to those search engines. Archive discovery also discloses it to Archive.today. Before either request, Wordcell resolves the source host through its public-network boundary and rejects credential-bearing URLs, private targets, and credential-shaped query parameters. Safe identity parameters such as Hacker News <code>item?id=</code> and YouTube <code>watch?v=</code> remain part of the exact query.</p>\n<p>Only a result with the same conservative URL identity can supply the selected title or description. A timestamped Archive.today result must embed that same source identity before it can be recorded. Provider failures, partial engine coverage, throttling, and absence remain explicit in the sidecar. A zero-result record is <code>not-found</code> only after complete attempted coverage; any failed engine or archive lookup keeps the top-level result <code>partial</code>.</p>\n<p>The command skips compatible sidecars by default. Use <code>--refresh</code> to query them again, <code>--no-archive</code> to omit Archive.today discovery, or <code>--delay-ms</code> to change the default one-second interval between outbound requests. It reads Markdown and sidecars through validated no-follow descriptors, carries the source Markdown and article-directory identities through provider work, and revalidates them under an inode-bound kernel advisory lock immediately before atomic installation. A live writer's lock cannot be stolen. The kernel releases a crashed writer's lock, and the next run safely reuses its verified lock file while UUID-scoped temporary names prevent an orphan from blocking progress. Malformed, linked, mismatched, replaced, or concurrently changed sources, locks, and sidecars fail closed.</p>\n",
  "pdf": "<h1 id=\"capture-pdf-documents\">Capture PDF documents</h1>\n<p><code>wordcell pdf</code> converts a local PDF or a public HTTP(S) PDF into an auditable Markdown\nbundle. Remote downloads use the same DNS-pinned, private-network-denying\nacquisition boundary as web capture, and sensitive URL parameters are removed\nbefore provenance is persisted. It preserves\nthe original bytes, reconstructs native document structure, extracts embedded\nimages, and uses local OCR for text that exists only inside scans or\nscreenshots.</p>\n<h2 id=\"capture-a-pdf\">Capture a PDF</h2>\n<pre><code class=\"language-sh\">wordcell doctor\nwordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\nwordcell pdf &quot;https://example.com/document.pdf&quot; --output articles\nwordcell pdf &quot;/absolute/path/to/document.pdf&quot; --slug stable-slug --output articles\n</code></pre>\n<p>The output mirrors a web capture without pretending the local file is a URL:</p>\n<pre><code class=\"language-text\">articles/&lt;slug&gt;/\n  &lt;slug&gt;.md\n  capture.json\n  source.pdf\n  annotations.json  # present after a reviewed annotation pass\n  assets/\n</code></pre>\n<p><code>source.pdf</code> is byte-identical to the input. <code>capture.json</code> records the\noriginal filename, SHA-256, bytes, page count, bounded PDF metadata, processed\npage count, tool versions, text blocks, inferred headings, image geometry, OCR\nmethod and confidence, embedded platforms, and warnings. It never records the\ninput's original absolute path.</p>\n<p>When <code>--annotations</code> is used, the bundle also retains the normalized annotation\narray as <code>annotations.json</code>. Its path, count, byte count, and SHA-256 in\n<code>capture.json</code> bind the reviewed interpretation to the saved evidence and make\nthe second pass reproducible.</p>\n<p>Pass <code>--force</code> only to replace a compatible PDF bundle previously created at\nthe same slug. Installation stages beside the target and commits with an\natomic rename.</p>\n<h2 id=\"text-headings-and-page-order\">Text, headings, and page order</h2>\n<p>Poppler supplies native text, font, emphasis, link, geometry, and page data.\nThe converter infers a body style from the document and promotes only\nwell-supported heading candidates. Uncertain structure remains a paragraph\ninstead of becoming a confident but invented hierarchy.</p>\n<p>Every emitted page is marked in the Markdown, and the manifest records\nprocessed-page and block counts. Repeated page headers and footers can be\nomitted from the readable Markdown while remaining accounted for by the source\nPDF. Font and spacing can identify a likely heading without proving its\nsemantic depth, so review peer-versus-child levels against the source.</p>\n<h2 id=\"images-and-ocr\">Images and OCR</h2>\n<p>Every extracted image becomes a content-addressed local asset and remains\nembedded in the Markdown. A text-bearing image also receives a Markdown\ntranscription. A mixed screenshot keeps both\nrepresentations so a message, chart, photograph, code sample, or UI state is\nnot discarded merely because OCR found words.</p>\n<p>Tesseract is the local OCR fallback. Its output is bounded and records method,\nword count, and confidence. Missing OCR or an unclassified image makes the\ncapture <code>partial</code> but never removes the image. Screenshot-heavy captures\nshould be visually reviewed; recognizable Slack or other conversation images\ncan preserve visible platform, author, channel, timestamp, and reply metadata\nwithout inventing fields the image does not show.</p>\n<p>For a reviewed second pass, copy each image's <code>id</code> and <code>asset.sha256</code> from the\nfirst <code>capture.json</code> into an annotation array. A text or mixed annotation\naccepts <code>markdown</code>, optional <code>method</code> (<code>agent</code> or <code>manual</code>), and optional\n<code>metadata</code> fields for <code>platform</code>, <code>contentType</code>, <code>channel</code>, <code>author</code>,\n<code>timestamp</code>, and <code>participants</code>. A visual annotation accepts an optional\n<code>alt</code>. Re-run against the same bundle with:</p>\n<pre><code class=\"language-sh\">wordcell pdf &quot;/absolute/path/to/document.pdf&quot; \\\n  --output articles \\\n  --annotations /tmp/pdf-image-annotations.json \\\n  --force\n</code></pre>\n<p>IDs and hashes bind the interpretation to the exact extracted image; stale or\nmisaddressed annotations are rejected. Annotation <code>markdown</code> contains only the\ntranscription; the renderer supplies the image embed, metadata line, and\n“Text visible in…” heading. The normalized input is copied into the installed\nbundle, so the path passed to <code>--annotations</code> does not need to remain available.</p>\n<h2 id=\"local-tools\">Local tools</h2>\n<p>PDF ingestion uses the open-source Poppler command-line tools <code>pdfinfo</code> and\n<code>pdftohtml</code>. Tesseract enables image text extraction. <code>wordcell doctor</code> reports\nwhether each route is available.</p>\n<p>Tool processes run with fixed argument shapes, bounded output, wall-time\nlimits, page and image caps, per-asset limits, and an aggregate asset budget.\nReaching any boundary is recorded and makes the result <code>partial</code>.</p>\n<h2 id=\"review\">Review</h2>\n<ol>\n<li>Compare source and processed page counts.</li>\n<li>Sample native-text, screenshot, scanned, and visual-only pages.</li>\n<li>Confirm heading levels match the source.</li>\n<li>Confirm every asset is referenced by the Markdown or manifest.</li>\n<li>Inspect low-confidence OCR against its source image.</li>\n<li>Keep later summaries and interpretations in maintained notes linked to the\ncapture.</li>\n</ol>\n",
  "publish": "<h1 id=\"publish-a-static-site\">Publish a static site</h1>\n<p><code>wordcell publish</code> turns selected Markdown notes into a static website with\nsearch, backlinks, and a graph. Build it locally, inspect the result, then copy\nthe files to your preferred host. Publishing does not upload your vault or\nrequire an account, database, model, or API key.</p>\n<p>The HTML pages work without JavaScript. Serve the directory over HTTP to use\nthe bundled browser-local search and graph; opening HTML files directly is\nsuitable for reading pages, but browser restrictions can block the reader.</p>\n<h2 id=\"preview-and-publish-a-slice\">Preview and publish a slice</h2>\n<p>Choose notes by exact path, directory, glob, metadata, or a graph neighborhood.\nThis example combines a directory tree with one note from elsewhere in the\nvault, then removes drafts:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include-glob 'guides/**/*.md' --include notes/getting-started \\\n  --exclude drafts --dry-run --json\n</code></pre>\n<p>The dry run builds the same artifact in memory and leaves the output directory\nuntouched. Its JSON report includes counts, a source digest, and up to 20\nselected note ids. The digest covers selected Markdown; it does not cover\nattachment bytes. Review those ids, counts, and referenced assets before writing. Repeat the same\nselectors without <code>--dry-run</code> to build the site:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include-glob 'guides/**/*.md' --include notes/getting-started \\\n  --exclude drafts\nwordcell serve --root site --port 8080\n</code></pre>\n<p>Open <code>http://127.0.0.1:8080</code> to inspect the pages. Nothing is publicly hosted\nuntil you upload the generated directory. When rebuilding an existing site,\nadd <code>--force</code> to replace that output directory. Keep it separate from your\nsource vault: neither directory may contain the other. An empty selection\nreturns a zero-count dry-run report, but writing it fails so a mistyped selector\ncannot replace an existing site. Wordcell writes the complete replacement\ninto a sibling staging directory first. A write failure leaves the previous\nsite in place. Promotion moves the previous site to a temporary backup, then\nmoves the staged site into place. These are two renames, so a running server can\nbriefly see no output directory. If promotion fails, Wordcell restores the\nbackup; if restoration also fails, the error names the retained backup path.\nThe backup is removed only after successful promotion.</p>\n<p>For agent workflows, <code>--json</code> returns the report without note bodies, HTML,\nsearch postings, or file contents. <code>--list-limit 0</code> returns counts and the source\ndigest only; a value from zero through 1,000 changes the maximum listed ids.\nThe ids also have a combined 16 KiB byte budget, and <code>selection.truncated</code>\nreports when the list is incomplete. The bound changes only the report, never\nwhich notes publish. Read the generated <code>catalog.json</code> when you need the full\npublished list. An SDK caller can reuse a scanned vault with <code>projectVault</code>.</p>\n<h2 id=\"choose-what-publishes\">Choose what publishes</h2>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site/docs --include docs\nwordcell publish --root kb --out site/public --tag public --exclude drafts\nwordcell publish --root kb --out site/guides \\\n  --include-glob 'guides/**/*.md' --exclude-glob '**/draft-*'\nwordcell publish --root kb --out site/topic \\\n  --from notes/topic-seed --depth 2 --direction both\nwordcell publish --root kb --out site/concepts --where type=concept --has relations\n</code></pre>\n<ul>\n<li><code>--include &lt;path&gt;</code> selects a note id, <code>.md</code> path, or directory prefix. Repeat\nit to combine notes from anywhere in the vault.</li>\n<li><code>--include-glob &lt;pattern&gt;</code> matches note ids or <code>.md</code> paths, case sensitively.\n<code>*</code> matches within one path segment, <code>?</code> matches one character, and a whole\n<code>**</code> segment matches zero or more directories. Quote patterns so your shell\ndoes not expand them. Bracket classes and brace expansion are unsupported.\nGlob matching has a shared work budget; if a large or complex selection\nexceeds it, use fewer patterns or exact path prefixes.</li>\n<li><code>--where &lt;path=value&gt;</code>, <code>--has &lt;path&gt;</code>, <code>--tag &lt;tag&gt;</code>, and <code>--scope &lt;repository-path&gt;</code> form one metadata query. All filters in that query must\nmatch, as with <code>wordcell list</code>.</li>\n<li><code>--from &lt;note&gt;</code> selects a link-and-relation neighborhood with <code>--depth</code>\n(1–10) and <code>--direction in|out|both</code>. Neighborhoods are bounded at 1,000 notes\nand 10,000 connections; exceeding either bound fails instead of silently\npublishing an incomplete neighborhood. Narrow the depth or use paths/globs.</li>\n<li>Include paths, include globs, the metadata query, and the graph neighborhood\nform a union. For example, <code>--include docs --tag public</code> selects every note\nunder <code>docs</code> plus every public-tagged note elsewhere.</li>\n<li><code>--exclude &lt;path&gt;</code> and <code>--exclude-glob &lt;pattern&gt;</code> remove notes after that\nunion. <code>publish: false</code> frontmatter always removes a note, even when another\nselector matches it. With no positive selector, all remaining notes publish.</li>\n</ul>\n<p>Review selected prose and attachments before hosting. Selection controls which\nnotes enter the site; it does not redact text inside them. A selected note can\nstill mention a private name, contain sensitive metadata in its search fields,\nor reference an attachment you intended to keep private. Exclusion selectors\napply to notes, not attachment files. Only validated local attachments directly\nreferenced by selected notes are copied.</p>\n<p>Links and typed relationships to excluded notes are omitted from structured\nnavigation. An authored link label can remain in the selected note's rendered\nprose. Public manifests record selector counts without storing raw include,\nexclude, filter, or seed values.</p>\n<h2 id=\"set-the-site-identity\">Set the site identity</h2>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site --title &quot;Team Handbook&quot; \\\n  --description &quot;Public engineering notes&quot; --base-path /handbook/ \\\n  --base-url https://docs.example.com\n</code></pre>\n<p><code>--base-path</code> sets the URL prefix (default <code>/</code>). <code>--base-url</code> adds a\n<code>sitemap.xml</code>; omit it when the site has no canonical origin. <code>--noindex</code>\nemits a robots disallow and a noindex marker on every page. These are indexing\nrequests, not access controls. <code>--deterministic</code> omits the generation timestamp\nso identical inputs and options produce byte-identical artifacts.</p>\n<h2 id=\"the-emitted-artifact\">The emitted artifact</h2>\n<pre><code class=\"language-text\">site/\n  index.html              # front door: the vault index note, or a catalog page\n  index.json              # the index note payload, when one published\n  manifest.json           # hraness.wordcell.site.v1 manifest\n  catalog.json            # every note: slug, title, type, tags\n  graph.json              # selection-scoped link and relation edges\n  graph/index.html        # interactive map of the published graph\n  n/&lt;slug&gt;/index.html     # prerendered note pages with backlink panels\n  n/&lt;slug&gt;.json           # hydrated note payloads for richer readers\n  index/docs.json         # document table with normalized eager fields\n  index/terms.json        # sorted term dictionary for prefix lookup\n  index/c/&lt;hh&gt;.json       # content postings shards, only for large selections\n  assets/&lt;hash&gt;.&lt;ext&gt;     # referenced attachments, content-addressed\n  reader/reader.js|.css   # the bundled reader: search overlay and graph map\n  reader/theme.js         # synchronous appearance bootstrap (see below)\n  404.html, robots.txt, sitemap.xml\n</code></pre>\n<h2 id=\"reading-the-site\">Reading the site</h2>\n<p>Every page ships the same chrome: a header with the site title, a <code>Graph</code> link,\nand a <code>Search</code> button; a sidebar tree of the published notes; and a footer.\nThe tree is derived from slug paths. Directories sort before notes at each\nlevel, groups open along the path to the current page, and oversized branches\nfold into bounded <code>+N more</code> links back to the catalog. Everything is\nprerendered HTML: navigation works with JavaScript disabled.</p>\n<p>Note pages add breadcrumbs above the article and an <em>On this page</em> table of\ncontents beside it, generated from the same heading anchors the Markdown\nrenderer emits. The backlink, relation, and referenced-by panels still sit in\nthe article aside.</p>\n<p>The graph page maps <code>graph.json</code> onto a canvas: nodes sized by degree, links\nas solid edges and typed relations as dashed ones. Drag to pan, scroll or\npinch to zoom, hover a node to light its neighborhood, and click a node to\nopen the note. <code>graph/#n=&lt;slug&gt;</code> deep-links to a focused node. The layout is a\nseeded deterministic force layout, so every visitor sees the same map. Sites\nover the contract's node limit publish the same page with the static note\nindex instead of the live canvas, and the prerendered index below the map\nkeeps the page useful without JavaScript at any size.</p>\n<h3 id=\"appearance\">Appearance</h3>\n<p>The reader supports the shared Hraness palette contract: the <code>wordcell</code>\nneutral palette plus Catppuccin, Gruvbox, Rosé Pine, and Tokyo Night, each\nin light and dark. A small classic script, <code>reader/theme.js</code>, loads\nsynchronously in every page head, ahead of the stylesheet, and applies\nthe stored preference to <code>data-palette</code> and <code>data-theme</code> on the document\nelement before first paint, so a saved palette never flashes the default.\nThe preference lives under <code>hraness-design-palette-v1</code> in the origin's\nlocal storage, the same key design-kit applications use, so a reader\nhosted beside one honors the visitor's existing choice; <code>system</code> mode\nfollows the operating system and <code>storage</code> events synchronize other tabs.\nThe deferred reader adds a single appearance menu as the rightmost header\naction — radio groups for mode and palette — and the graph map repaints\non every applied change. Without JavaScript, or with no stored preference,\npages fall back to the neutral palette under <code>prefers-color-scheme</code>.</p>\n<p>Every JSON file carries an explicit format identifier and parses under a\nbounded <code>unknown</code>-value contract; parsers reject unexpected keys, oversize\nfields, and wrong formats. <code>manifest.json</code> records the generator identity and\nversion, selector counts, the source digest, artifact paths, search\nmode, counts, and any truncated aspects. In <code>--deterministic</code> mode the manifest\ncarries no timestamp and the whole directory is reproducible.</p>\n<h3 id=\"browser-local-search\">Browser-local search</h3>\n<p>The document table eagerly stores normalized title, alias, path, tag, and\nmetadata fields so in-page matching reproduces the exact lane's substring\nsemantics. Note content is indexed inline while the selection fits the\ncontract's aggregate budget; larger selections store sharded postings that the\nreader fetches lazily by FNV-1a term hash. <code>--no-index-content</code> disables content\nindexing entirely, leaving field-only search. Query, prefix-expansion, result,\nand hydration bounds are fixed by the contract so a hostile or oversized\nartifact cannot exhaust the browser.</p>\n<p>The overlay opens from the <code>Search</code> button or the <code>/</code> key. Alongside free\ntext, field filters narrow the candidate set:</p>\n<ul>\n<li><code>tag:&lt;tag&gt;</code> requires every listed tag.</li>\n<li><code>type:&lt;type&gt;</code> matches the catalog <code>type</code> value; notes without one count as\n<code>note</code>.</li>\n<li><code>path:&lt;prefix&gt;</code> limits results to notes whose slug, id, or vault path sits\nunder the prefix on a segment boundary (<code>path:docs</code> matches <code>docs/alpha</code>,\nnever <code>docs2/x</code>).</li>\n</ul>\n<p>Filters combine with free text. <code>tag:public path:docs migration</code> searches\n&quot;migration&quot; inside public notes under <code>docs/</code> — and a filter-only query lists\neverything that matches. Unknown or malformed <code>name:</code> tokens stay in the\nfree-text query. Matched terms render with <code>&lt;mark&gt;</code> highlighting built from\nDOM text nodes, never injected HTML.</p>\n<h3 id=\"safety\">Safety</h3>\n<p>Raw HTML never passes through the renderer: every text span and attribute is\nentity-escaped, only the controlled Markdown grammar emits markup, and external\nURLs are restricted to <code>http</code>, <code>https</code>, and <code>mailto</code>. Pages ship a strict\n<code>default-src 'self'</code> Content-Security-Policy, so no remote image, script, or\nframe can load. The output directory and vault must be separate: neither may contain the\nother, including through symlinked parent directories. The output directory\nitself cannot be a symlink, and every written path is confined to <code>--out</code>.\nGenerated JSON must pass the reader contracts before any output is replaced.\nEvery generated filename component must fit 255 UTF-8 bytes, and a generated\nfile cannot also serve as a directory. A conflicting or oversized filename\nproduces a rename instruction before an existing site is touched.\nAn oversized title or too many aliases or tags produces a named validation\nerror; shorten that field or narrow the selection and publish again.</p>\n<h2 id=\"host-the-result\">Host the result</h2>\n<p>The site is static files. Sync the output directory to any object store or\nstatic host — no adapter is required or bundled:</p>\n<pre><code class=\"language-sh\">aws s3 sync site/ s3://bucket/handbook/ --delete\nrclone sync site/ remote:handbook\n</code></pre>\n<p>Serve under a subpath by passing <code>--base-path</code> at publish time. For a local\npreview, <code>wordcell serve</code> binds a static file server to the output directory:</p>\n<pre><code class=\"language-sh\">wordcell serve --root site --port 8080\n</code></pre>\n<p>The server defaults to the loopback interface (<code>--host</code> overrides), maps\ndirectories to <code>index.html</code>, returns the published <code>404.html</code> for missing\npaths, confines requests and symlinks to the root, and answers <code>GET</code> and\n<code>HEAD</code> only. Production hosting stays with the object store or CDN. Any other static file\nserver works for previews too:</p>\n<pre><code class=\"language-sh\">python3 -m http.server --directory site 8080\n</code></pre>\n<h2 id=\"host-on-wordcellio\">Host on wordcell.io</h2>\n<p><code>wordcell.io</code> also publishes a vault for you: the API runs the same projection\nserver-side and serves the emitted artifact at <code>https://wordcell.io/p/&lt;key8&gt;/&lt;slug&gt;/</code>.\nThis path exists for agents and machines that cannot hold files or deploy a\nstatic host; the CLI remains the primary publication path, and the artifact\ncontract is identical either way.</p>\n<pre><code class=\"language-sh\">curl -s -X POST https://wordcell.io/api/v1/tokens -d '{&quot;label&quot;:&quot;my agent&quot;}'\n# → { &quot;ok&quot;: true, &quot;token&quot;: &quot;wc_pub_…&quot;, &quot;key8&quot;: &quot;…&quot;, &quot;urlBase&quot;: &quot;…/p/&lt;key8&gt;/&quot; }\n\ncurl -s -X PUT https://wordcell.io/api/v1/sites/handbook \\\n  -H &quot;Authorization: Bearer wc_pub_…&quot; -H &quot;content-type: application/json&quot; -d '{\n    &quot;operation&quot;: {\n      &quot;contract&quot;: &quot;hraness.wordcell.hosted-operation.v1&quot;,\n      &quot;id&quot;: &quot;32d708ac-782d-4ca0-8c34-3e89ad0ed02a&quot;,\n      &quot;expectedRevision&quot;: 0\n    },\n    &quot;title&quot;: &quot;Team Handbook&quot;,\n    &quot;files&quot;: {\n      &quot;index.md&quot;: &quot;# Handbook\\n\\nStart with [[onboarding]].\\n&quot;,\n      &quot;onboarding.md&quot;: &quot;# Onboarding\\n\\nWelcome.\\n&quot;,\n      &quot;assets/logo.png&quot;: {&quot;upload&quot;: &quot;&lt;id from POST /api/v1/uploads&gt;&quot;}\n    }\n  }'\n# → { &quot;ok&quot;: true, &quot;site&quot;: { &quot;url&quot;: &quot;https://wordcell.io/p/&lt;key8&gt;/handbook/&quot;, … } }\n</code></pre>\n<ul>\n<li><strong>Tokens are capability tokens.</strong> <code>POST /api/v1/tokens</code> is self-serve and\nfree, bounded per client address. The server stores only the token's SHA-256\ndigest; the digest's first 8 hex chars form the site namespace, so a token\ncan only create or replace its own sites. There is no account and no device\nflow at this stage.</li>\n<li><strong>Files</strong> map vault-relative paths to a UTF-8 string, <code>{&quot;base64&quot;: &quot;…&quot;}</code>, or\n<code>{&quot;upload&quot;: &quot;&lt;id&gt;&quot;}</code>. Uploads come from <code>POST /api/v1/uploads</code>, which mints a\nshort-lived, write-once presigned PUT (≤32 MiB); uploads expire after a day.\nInline bytes are capped at 4 MiB per request, 256 files per request.</li>\n<li><strong>Options</strong> (<code>title</code>, <code>description</code>, <code>index</code>, <code>noindex</code>, <code>indexContent</code>,\n<code>selection</code>) mirror the CLI flags; <code>selection</code> accepts the same\nincludes/excludes/globs/tags/repositoryScopes/filters/from shape. <code>index</code>\nselects the vault's index note path when it is not <code>index.md</code>.</li>\n<li><strong>Conditional writes and recovery.</strong> Read the current revision with <code>GET</code>\nbefore a write. PUT and DELETE require a unique operation ID and the revision\nthey expect to replace. Exact retries return the original receipt. Resolve\nuncertain outcomes with <code>GET ?operation=&lt;id&gt;</code>. One conditional site-head\nwrite changes the record and public visibility after all artifact bytes are\ndurable. DELETE retains a tombstone and receipts; shared artifact bytes stay\nstored. See the <a href=\"/docs/hosted-publication\">hosted operation contract</a> for request\nshapes, conflict handling, and recovery. Public reads can remain cached for\nup to 60 seconds.</li>\n<li><strong>Bounds.</strong> Hosted publication accepts ≤256 files per request, applies the\ncontract's per-note and per-asset byte caps, and rejects projected output\nover 3,500 files or 256 MiB. Tokens get 60 publishes per day and 50 distinct\nreserved slugs, including deletions and interrupted operations; addresses get 8 token mints and\n120 publishes per day.\nPublic reads carry no quota beyond ordinary CDN caching.</li>\n<li><strong>Data.</strong> The request vault is materialized to a temporary directory for the\nprojection and deleted when the request ends; only the emitted artifact\npersists. Quota counters expire within two days; token digests, site records,\nnamespace reservations, operation receipts, and tombstones persist. Published sites are\npublic by contract — never publish private content.</li>\n</ul>\n<p><code>GET /api/v1/openapi.json</code> returns the OpenAPI 3.1 description;\n<code>GET /api/v1/health</code> reports service and storage health. MCP clients can use\nthe streamable-HTTP endpoint <code>POST /api/v1/mcp</code> instead of REST: it exposes\n<code>create_token</code>, <code>get_site</code>, <code>publish_site</code>, <code>list_sites</code>, and <code>delete_site</code> tools that\ndispatch to the identical route logic — send the <code>wc_pub_</code> Bearer token as on\nREST, and <code>create_token</code> stays unauthenticated so an MCP-only client can\nonboard itself. The storage layer is\na private Cloudflare R2 bucket behind the <code>wordcell-sites</code> worker — object\nreads and writes are HMAC-signed, and the public <code>/p/</code> path resolves the site\nhead to immutable artifact bytes with directory-index and <code>404.html</code>\nsemantics identical to <code>wordcell serve</code>.</p>\n<h2 id=\"programmatic-use\">Programmatic use</h2>\n<p><code>publishVault</code> drives the filesystem pipeline; <code>projectVault</code> projects an\nalready-scanned snapshot through an injectable <code>PublishIo</code> seam, so tests and\nalternative hosts can resolve and read assets without disk access:</p>\n<pre><code class=\"language-ts\">import { publishVault } from &quot;@hraness/wordcell/publish&quot;;\n\nconst preview = await publishVault({\n  root: &quot;kb&quot;,\n  out: &quot;site&quot;,\n  selection: { includeGlobs: [&quot;guides/**/*.md&quot;], includes: [&quot;notes/getting-started&quot;] },\n  dryRun: true,\n  listLimit: 10,\n});\nconsole.log(preview.report); // counts, selected ids, and digest; no note bodies\n</code></pre>\n<p><code>parseSiteManifestV1</code> and <code>WORDCELL_SITE_LIMITS_V1</code> are available from\n<code>@hraness/wordcell/publish-model</code>. The constants fix every artifact bound: note,\nasset, term, shard, preview, inline-text, payload-text, and hydration caps.</p>\n",
  "portfolio": "<h1 id=\"portfolio-federation\">Portfolio federation</h1>\n<p>Portfolio federation searches and audits selected Markdown vaults without combining their files or indexes. A strict registry gives each vault a stable logical identity and maps that identity to a checkout under one workspace. Each vault keeps its own Markdown, local graph, optional QMD index, and Git history.</p>\n<p>The registry discovers vaults. An explicit selection determines which vaults a command or SDK session may open.</p>\n<h2 id=\"define-a-v1-registry\">Define a v1 registry</h2>\n<p>A v1 registry is a JSON file with the contract <code>hraness.kb-portfolio/v1</code> and <code>schemaVersion</code> <code>1</code>:</p>\n<pre><code class=\"language-json\">{\n  &quot;contract&quot;: &quot;hraness.kb-portfolio/v1&quot;,\n  &quot;schemaVersion&quot;: 1,\n  &quot;vaults&quot;: [\n    {\n      &quot;owner&quot;: &quot;example&quot;,\n      &quot;id&quot;: &quot;product-history&quot;,\n      &quot;repository&quot;: &quot;example/product-history&quot;,\n      &quot;checkout&quot;: &quot;history-vault&quot;,\n      &quot;root&quot;: &quot;kb&quot;,\n      &quot;role&quot;: &quot;repository&quot;,\n      &quot;visibility&quot;: &quot;organization&quot;,\n      &quot;defaultRef&quot;: &quot;main&quot;,\n      &quot;parserVersion&quot;: 1\n    },\n    {\n      &quot;owner&quot;: &quot;hraness&quot;,\n      &quot;id&quot;: &quot;legacy-notes&quot;,\n      &quot;repository&quot;: &quot;hraness/legacy-notes&quot;,\n      &quot;checkout&quot;: &quot;legacy-notes&quot;,\n      &quot;root&quot;: &quot;kb&quot;,\n      &quot;role&quot;: &quot;archive&quot;,\n      &quot;visibility&quot;: &quot;private&quot;,\n      &quot;parserVersion&quot;: 1\n    },\n    {\n      &quot;owner&quot;: &quot;personal&quot;,\n      &quot;id&quot;: &quot;tiff&quot;,\n      &quot;repository&quot;: &quot;personal/tiff&quot;,\n      &quot;checkout&quot;: &quot;tiff&quot;,\n      &quot;root&quot;: &quot;kb&quot;,\n      &quot;role&quot;: &quot;portfolio&quot;,\n      &quot;visibility&quot;: &quot;personal&quot;,\n      &quot;parserVersion&quot;: 1\n    }\n  ],\n  &quot;authorityGroups&quot;: [\n    {\n      &quot;id&quot;: &quot;product-history&quot;,\n      &quot;members&quot;: [\n        &quot;example/product-history&quot;,\n        &quot;hraness/legacy-notes&quot;\n      ],\n      &quot;state&quot;: &quot;unresolved&quot;,\n      &quot;protected&quot;: true,\n      &quot;reason&quot;: &quot;An owner must choose the maintained authority.&quot;\n    }\n  ]\n}\n</code></pre>\n<p>The vault key is the explicit <code>owner/id</code> pair. It remains <code>example/product-history</code> even when the local checkout is named <code>history-vault</code>. Do not derive a vault key from <code>checkout</code>, <code>root</code>, or a Git remote.</p>\n<p>The other fields have separate purposes:</p>\n<ul>\n<li><code>repository</code> is the logical repository identity.</li>\n<li><code>checkout</code> is a relative path under the <code>--workspace</code> directory.</li>\n<li><code>root</code> is a relative path under the checkout. Use <code>.</code> when the repository root is the vault root.</li>\n<li><code>role</code> is <code>portfolio</code>, <code>repository</code>, <code>archive</code>, <code>template</code>, or <code>sample</code>. It changes audit severity for missing stable IDs. It does not grant access.</li>\n<li><code>visibility</code> is <code>public</code>, <code>organization</code>, <code>private</code>, or <code>personal</code>. It controls the CLI's <code>--shared</code> selection. It is not a filesystem permission.</li>\n<li><code>parserVersion</code> is exactly <code>1</code> in this contract.</li>\n</ul>\n<p>The parser rejects unknown properties and unsafe paths. A registry is limited to 1 MiB, 128 vaults, and 64 authority groups. One operation may select at most 32 vaults. <code>--shared</code> and <code>--all</code> reject a registry-derived selection above that bound; they never truncate it silently.</p>\n<h2 id=\"select-vaults-explicitly\">Select vaults explicitly</h2>\n<p>The SDK requires a nonempty <code>authorizedVaults</code> list. The CLI offers three selection forms:</p>\n<ul>\n<li>Repeated <code>--vault owner/id</code> selects the named vaults.</li>\n<li><code>--shared</code> selects entries whose visibility is <code>public</code> or <code>organization</code>.</li>\n<li><code>--all</code> selects every registry entry and is available only for audit.</li>\n</ul>\n<p>Use <code>--vault</code> for <code>private</code> and <code>personal</code> entries. Use <code>--all</code> only when the process is permitted to read every configured vault.</p>\n<p>After selection, the portfolio core resolves, opens, scans, and reports only selected vaults. Results do not expose names, counts, paths, or failures from unselected vaults. The CLI must still read and validate the registry to compute <code>--shared</code> or <code>--all</code>, so access to registry metadata is a separate prerequisite. It passes that exact immutable registry snapshot into the operation; authorization and checkout resolution cannot observe two different registry versions.</p>\n<p>Visibility is a registry classification. Operating-system permissions and workspace confinement remain the access boundary. A registry entry cannot give the process access to a checkout it cannot already read.</p>\n<h2 id=\"search-selected-vaults\">Search selected vaults</h2>\n<p>Search the shared vaults under one workspace:</p>\n<pre><code class=\"language-sh\">wordcell portfolio search &quot;durable agent memory&quot; \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --shared \\\n  --mode hybrid \\\n  --limit 10\n</code></pre>\n<p>Select private or personal vaults by exact key:</p>\n<pre><code class=\"language-sh\">wordcell portfolio search &quot;release rationale&quot; \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --vault example/product-history \\\n  --vault hraness/legacy-notes \\\n  --mode exact\n</code></pre>\n<p>The supported modes are <code>hybrid</code>, <code>exact</code>, <code>keyword</code>, and <code>semantic</code>. Add <code>--json</code> to inspect per-vault diagnostics and local retrieval evidence.</p>\n<p>A query that is a canonical qualified URI routes directly to its named selected vault and uses exact retrieval:</p>\n<pre><code class=\"language-sh\">wordcell portfolio search &quot;kb://example/product-history/018f4b20-7c95-7af2-a11f-89011baf1137&quot; \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --vault example/product-history\n</code></pre>\n<p>If that vault or document is outside the selection, the search does not reveal whether it exists elsewhere.</p>\n<h3 id=\"failure-policy\">Failure policy</h3>\n<p>Portfolio search uses the <code>partial</code> failure policy by default. A selected vault that cannot resolve, open, or complete its search produces a selected-vault diagnostic. Results from available selected vaults remain usable, and the result has <code>partial: true</code>.</p>\n<p>Add <code>--require-all</code> when every selected vault must resolve, open, and execute its search:</p>\n<pre><code class=\"language-sh\">wordcell portfolio search &quot;release rationale&quot; \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --shared \\\n  --require-all\n</code></pre>\n<p><code>--require-all</code> does not turn an optional QMD or Git provenance degradation into a fatal error. Inspect <code>partial</code> and the lane diagnostics when completeness matters.</p>\n<h3 id=\"deterministic-federation-order\">Deterministic federation order</h3>\n<p>Each vault performs its own exact or QMD-backed retrieval. The federation layer keeps QMD scores inside that vault's evidence because scores from separate QMD databases are not comparable.</p>\n<p>The global merge uses this order:</p>\n<ol>\n<li>Exact identity matches precede other matches.</li>\n<li>A reciprocal score derived from the result's local rank orders the remaining candidates.</li>\n<li>The stable vault key breaks a tie.</li>\n<li>The qualified identity or current path breaks the final tie.</li>\n</ol>\n<p>The returned portfolio <code>score</code> describes this reciprocal local-rank merge. It is not a probability or a QMD similarity score. Raw local evidence remains available under the hit's <code>local</code> and <code>evidence</code> fields.</p>\n<p>Graph traversal remains vault-local. Portfolio search does not infer a cross-vault neighborhood or use graph context as a relevance boost.</p>\n<h2 id=\"give-notes-stable-identities\">Give notes stable identities</h2>\n<p>Use <code>document_id</code> for an identity that survives a path change:</p>\n<pre><code class=\"language-md\">---\ndocument_id: 018f4b20-7c95-7af2-a11f-89011baf1137\ntype: concept\ntitle: Durable agent memory\n---\n\n# Durable agent memory\n</code></pre>\n<p><code>document_id</code> is a lowercase ASCII ID of at most 128 bytes. It may contain letters, digits, dots, underscores, and hyphens. Ordinary notes created through <code>wordcell note create</code> or <code>createNote</code> receive a UUID v4 when the caller does not provide an ID.</p>\n<p>An idempotent create preserves an existing valid ID. It leaves a legacy note without an ID unchanged, and it rejects an explicit ID that conflicts with the existing note. Add IDs to legacy notes through a reviewed Markdown edit rather than treating an idempotent create as a migration.</p>\n<p>A stable portfolio URI combines the logical vault identity and the authored document ID:</p>\n<pre><code class=\"language-text\">kb://owner/vault/document_id\n</code></pre>\n<p>For example:</p>\n<pre><code class=\"language-text\">kb://example/product-history/018f4b20-7c95-7af2-a11f-89011baf1137\n</code></pre>\n<p>The URI is byte-canonical. Uppercase aliases, percent-encoded components, path traversal, and extra path segments are rejected. A <code>document_id</code> must be unique within its vault. The same ID in two different vaults produces two different qualified URIs.</p>\n<p>Notes without a valid <code>document_id</code> remain searchable through a tagged <code>legacy-path</code> identity. A legacy path is not reported as stable and cannot be used for a qualified read.</p>\n<h2 id=\"author-cross-vault-relations\">Author cross-vault relations</h2>\n<p>A typed relation may target a canonical qualified URI:</p>\n<pre><code class=\"language-yaml\">relations:\n  supports:\n    - kb://example/product-history/018f4b20-7c95-7af2-a11f-89011baf1137\n</code></pre>\n<p>The source note still owns the assertion. The local graph records it as an external authored relation instead of resolving it to a local note ID.</p>\n<p>Portfolio audit verifies the target against valid stable IDs in the explicitly selected, successfully audited vaults. It reports <code>external-relation-unavailable</code> when the target is not available in that selection. This diagnostic does not claim that the target is absent from an unselected vault.</p>\n<p>Cross-vault relations do not create inferred backlinks, reciprocal assertions, or cross-vault graph traversal. Author each assertion in Markdown where its source belongs.</p>\n<h2 id=\"audit-a-portfolio\">Audit a portfolio</h2>\n<p>Run a report-only audit across the shared selection:</p>\n<pre><code class=\"language-sh\">wordcell portfolio audit \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --shared\n</code></pre>\n<p>Audit exact vaults when the review includes private material:</p>\n<pre><code class=\"language-sh\">wordcell portfolio audit \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --vault example/product-history \\\n  --vault hraness/legacy-notes \\\n  --strict\n</code></pre>\n<p>The default command exits successfully after producing the report, even when the report contains issues. <code>--strict</code> exits with status <code>3</code> when the report contains at least one error or is truncated. Warnings and advisories do not trigger that exit status. Use <code>--json</code> for the complete bounded report; its <code>ok</code> field is also false for an error or truncation.</p>\n<p>Audit checks include:</p>\n<ul>\n<li>missing, invalid, and duplicate <code>document_id</code> values;</li>\n<li>unavailable cross-vault relation targets within the selected audit scope;</li>\n<li>broken or ambiguous links and malformed or unresolved local relations;</li>\n<li>local attachment failures;</li>\n<li>stale managed catalogs and unavailable Git provenance;</li>\n<li>invalid, unavailable, or overlapping selected roots;</li>\n<li>exact duplicate content across selected notes; and</li>\n<li>unresolved authority groups whose complete membership is selected.</li>\n</ul>\n<p>Missing IDs are warnings in <code>portfolio</code> and <code>repository</code> vaults. They are advisories in <code>archive</code>, <code>template</code>, and <code>sample</code> vaults. Invalid or duplicate IDs are errors. Exact duplicate content is a warning when an active vault is involved and an advisory otherwise.</p>\n<p>An audit is <code>partial</code> when a selected vault is unavailable or the report is truncated. Ordinary findings do not by themselves make the report partial. The default issue limit is 500, and the maximum is 5,000. Severity counts include every discovered issue even when only a bounded severity-preserving sample is materialized. <code>--strict</code> fails closed when any error is counted or when the report is truncated.</p>\n<p>Audit is read-only. It does not assign IDs, repair links, update a catalog, deduplicate notes, move files, or choose an authority.</p>\n<h2 id=\"record-authority-decisions\">Record authority decisions</h2>\n<p>An authority group records a reviewed decision state among registry members. Use <code>state: &quot;unresolved&quot;</code> while an owner must still choose. An unresolved group cannot declare <code>canonical</code>.</p>\n<p>After review, change the state and name one member:</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;product-history&quot;,\n  &quot;members&quot;: [\n    &quot;example/product-history&quot;,\n    &quot;hraness/legacy-notes&quot;\n  ],\n  &quot;state&quot;: &quot;resolved&quot;,\n  &quot;canonical&quot;: &quot;example/product-history&quot;,\n  &quot;reason&quot;: &quot;The maintained repository owns current product history.&quot;\n}\n</code></pre>\n<p><code>protected: true</code> marks a group whose duplicate material must remain visible during review. It does not suppress duplicate findings, grant access, or change search rank.</p>\n<p>Use this migration sequence:</p>\n<ol>\n<li>Register each real vault with its current stable logical <code>owner/id</code> identity.</li>\n<li>Mark unresolved authority groups before moving or deleting content.</li>\n<li>Run a selected audit and review root, identity, relation, and duplicate findings.</li>\n<li>Add <code>document_id</code> to maintained legacy notes through reviewed Markdown edits. Preserve an ID when a note moves.</li>\n<li>Replace cross-vault path assumptions with reviewed <code>kb://</code> relation targets.</li>\n<li>Resolve an authority group only after its owner selects a canonical member and records the reason.</li>\n<li>Enable <code>--strict</code> in automation after expected errors have been resolved.</li>\n</ol>\n<p>Keep protected duplicate sources until their owner approves a consolidation. The registry and audit surface an authority conflict; they do not settle it.</p>\n<h2 id=\"keep-search-rules-opt-in\">Keep search rules opt-in</h2>\n<p>Search rules are a strict configuration with <code>schemaVersion: 1</code>, named leading aliases, and priority rules. Configure them through <code>OpenKnowledgeBaseOptions.searchRules</code>, or through <code>OpenKnowledgePortfolioOptions.knowledgeBase.searchRules</code> for each local vault session. The CLI reads the same bounded JSON contract from <code>--rules &lt;file&gt;</code>.</p>\n<p>For example, a consumer-owned <code>search-rules.json</code> can define one constrained alias and one opt-in maintained-note tier:</p>\n<pre><code class=\"language-json\">{\n  &quot;schemaVersion&quot;: 1,\n  &quot;aliases&quot;: {\n    &quot;active-plans&quot;: {\n      &quot;mode&quot;: &quot;hybrid&quot;,\n      &quot;filters&quot;: [\n        {\n          &quot;kind&quot;: &quot;one-of&quot;,\n          &quot;path&quot;: &quot;status&quot;,\n          &quot;values&quot;: [&quot;proposed&quot;, &quot;accepted&quot;, &quot;in-progress&quot;]\n        }\n      ]\n    }\n  },\n  &quot;priorityRules&quot;: [\n    {\n      &quot;id&quot;: &quot;maintained-notes&quot;,\n      &quot;tier&quot;: 1,\n      &quot;pathPrefix&quot;: &quot;notes/&quot;\n    }\n  ]\n}\n</code></pre>\n<p>The full alias fields are <code>query</code>, <code>mode</code>, <code>filters</code>, <code>tags</code>, and <code>repositoryScopes</code>. A priority rule needs a canonical <code>id</code>, a tier from 1 through 32, and at least one of <code>pathPrefix</code>, <code>tagsAll</code>, <code>repositoryScope</code>, <code>metadata</code>, or <code>vaultId</code>. Unknown fields and malformed predicates are rejected.</p>\n<p>A leading alias expands only when it is the first query token, such as <code>@active context</code>. Alias filters, tags, and repository scopes add constraints. Caller-provided constraints are preserved.</p>\n<p>Priority rules may match <code>pathPrefix</code>, <code>tagsAll</code>, <code>repositoryScope</code>, <code>metadata</code>, or <code>vaultId</code>. Conditions on one rule are combined with AND. Smaller positive <code>tier</code> values are preferred. Exact identity matches remain ahead of every tier, and ties retain the original relevance order.</p>\n<p>Priority ordering applies only when a search request sets:</p>\n<pre><code class=\"language-ts\">ordering: &quot;priority-then-relevance&quot;\n</code></pre>\n<p>The default is <code>ordering: &quot;relevance&quot;</code>, which leaves configured priority rules inactive. A single-vault <code>KnowledgeBaseSearchResult</code> includes a trace when at least one selected hit matches a priority rule, whether or not that match changes the final ordering. Search rules do not create authority, permissions, graph edges, or Markdown edits.</p>\n<p>On the CLI, <code>--rules</code> enables leading aliases. Add <code>--priority</code> to request priority ordering:</p>\n<pre><code class=\"language-sh\">wordcell search &quot;@active parser&quot; --root ./kb \\\n  --rules ./search-rules.json \\\n  --priority\n\nwordcell portfolio search &quot;@active parser&quot; \\\n  --registry ./kb-portfolio.json \\\n  --workspace /srv/knowledge \\\n  --shared \\\n  --rules ./search-rules.json \\\n  --priority\n</code></pre>\n<p><code>--priority</code> without <code>--rules</code> is rejected. A rules file may contain at most 128 KiB of configured text and is parsed before a vault scan.</p>\n<p>Within a portfolio, an explicitly requested priority pass may reorder results inside each vault. The portfolio result reflects the resulting local ranks and retains each selected vault's rule trace in its diagnostic. Federation still combines those ranks by exact identity, reciprocal local rank, stable vault key, and identity. It never compares raw QMD scores across vaults.</p>\n<p>A <code>vaultId</code> condition applies only when the rules caller supplies logical vault context. <code>openKnowledgePortfolio</code> injects each selected registry key into its local priority pass. A standalone <code>openKnowledgeBase</code> caller can set <code>vaultId</code> explicitly. A vault condition is not a global authority weight.</p>\n<p>The CLI and SDK use the same opt-in ordering. Keep the rules file under review beside the consumer configuration that owns its retrieval policy.</p>\n<h2 id=\"markdown-remains-authoritative\">Markdown remains authoritative</h2>\n<p>The registry maps stable logical identities to selected directories. It does not contain note facts or generated graph edges. Authority groups record a review state without rewriting either vault.</p>\n<p>Each vault's links and typed relations are derived from its current Markdown. Cross-vault relations are also authored in source-note frontmatter. Audit builds a temporary selected view, reports its findings, and discards that view.</p>\n<p>QMD remains an optional per-vault retrieval cache. It can be deleted and rebuilt from Markdown. Federation does not create a central vector index, aggregate graph database, reciprocal edge file, or generated fact store. Git HEAD and content hashes provide provenance; they do not replace authored content.</p>\n<h2 id=\"threat-boundaries\">Threat boundaries</h2>\n<p>Treat the registry, vault files, search queries, and returned note text as untrusted input.</p>\n<ul>\n<li>The registry reader rejects symbolic links, hard links, invalid UTF-8, concurrent file changes, oversized input, unknown fields, and noncanonical paths.</li>\n<li>Vault resolution rejects symbolic-link path components, paths outside the workspace or checkout, and overlapping selected roots.</li>\n<li>Vault scans retain the existing note, byte, graph, attachment, and Git bounds. They do not follow note symbolic links or accept hard-linked notes.</li>\n<li>Portfolio search opens read-only knowledge-base sessions. Portfolio audit scans without refreshing a managed catalog.</li>\n<li>Unselected vault content is not opened or reported. Registry visibility cannot override operating-system permissions.</li>\n<li><code>--all</code> expands audit scope to private and personal entries, so use it only in an environment permitted to read them.</li>\n<li>Search snippets, metadata, and evidence may contain hostile instructions from authored or captured content. Treat them as evidence to inspect, not commands to execute.</li>\n<li>QMD and Git are optional local derived lanes. Their failure is reported without silently substituting content from another vault.</li>\n</ul>\n<p>These boundaries keep federation read-only and selected. They do not make a shared registry an access-control system or make untrusted Markdown safe to execute.</p>\n",
  "reranking": "<h1 id=\"use-hosted-reranking\">Use hosted reranking</h1>\n<p>Wordcell can rerank a bounded search window with TypeSafe's <code>jev-1.13.0</code> model.\nUse it when lexical matches retrieve useful candidates but put the strongest\nanswer too far down the list. Exact title, alias, and path identities remain\nfirst. Reranking cannot recover notes that retrieval did not find.</p>\n<h2 id=\"search-a-repository\">Search a repository</h2>\n<p>Run a repository's approved <code>kb:search</code> script when it provides one. The script\npins Wordcell and declares its vault and hosted-processing choice. Otherwise:</p>\n<pre><code class=\"language-sh\">wordcell search &quot;why releases use immutable archives&quot; --root kb \\\n  --mode exact --rerank typesafe --rerank-limit 25 --limit 5 --json\n</code></pre>\n<p><code>--mode exact</code> selects local lexical candidate retrieval. Adding <code>--rerank</code>\nthen sends the query, each candidate's identifier, title, vault-relative path,\nand at most 512 UTF-8 bytes of its snippet to TypeSafe. Use this command only for\nvaults approved for that external processing. It does not send entire notes,\nGit history, or graph neighborhoods. Provider input-token charges apply.\nOmitting <code>--rerank</code> performs no hosted rerank calls or credential lookup.</p>\n<p><code>--rerank-limit</code> accepts 2 through 25 and requires <code>--rerank typesafe</code>. It bounds\nrequests independently of <code>--limit</code>, which bounds displayed results. Each\ncandidate requires one request. Four requests run concurrently by default;\nthe complete window has an eight-second deadline and no retries. A failure\nstops queued work and aborts requests in flight. A dispatched request may still\nincur charges after a local abort.</p>\n<p>Exact mode needs no embedding download. Existing indexed vaults can explicitly\nuse <code>--mode hybrid</code>, <code>keyword</code>, or <code>semantic</code>; those candidate pools have not\nbeen qualified by the scientific-retrieval study below.</p>\n<h2 id=\"configure-a-local-credential\">Configure a local credential</h2>\n<p>The CLI resolves a credential only for <code>--rerank typesafe</code>, in this order:</p>\n<ol>\n<li><code>TYPESAFE_API_KEY</code>, when present. An invalid explicit value does not fall back.</li>\n<li>The absolute path in <code>TYPESAFE_API_KEY_FILE</code>, when set.</li>\n<li><code>$XDG_CONFIG_HOME/wordcell/typesafe-api-key</code>, or\n<code>~/.config/wordcell/typesafe-api-key</code> when <code>XDG_CONFIG_HOME</code> is unset.</li>\n</ol>\n<p>The file contains one API key with an optional final newline. On Unix it must\nbelong to the current user and grant no group or other permissions, normally\nmode <code>0600</code>. Symlinks, hard links, nonregular files, oversized files, and\nmultiline values are rejected. Keep the directory private, normally <code>0700</code>.\nStore the file outside repositories. Do not paste credentials into commands,\npackage scripts, notes, or source control. CI can inject <code>TYPESAFE_API_KEY</code>\nthrough its existing secret mechanism; ordinary checks require no key.</p>\n<p>A missing or invalid credential returns the baseline results and an\n<code>unavailable</code> rerank diagnostic. The CLI never silently reads a different key\nwhen an explicit credential fails.</p>\n<h2 id=\"inspect-the-result\">Inspect the result</h2>\n<p>Read <code>diagnostics.lanes</code> and select <code>lane: &quot;rerank&quot;</code>. <code>ready</code> means the complete\nwindow was accepted. <code>unavailable</code> or <code>degraded</code> means Wordcell retained the\nbaseline ordering, with <code>partial: true</code>. The command remains usable, so a\nsuccessful exit code alone does not prove reranking occurred.</p>\n<p>The lane's structured <code>rerank</code> receipt records the validated model and known\ninput/output token totals. Its <code>accounting</code> records candidates, attempted\nrequests, settled requests, elapsed milliseconds, and <code>usageComplete</code>. False\n<code>usageComplete</code> means some dispatched requests have unknown usage; known token\ntotals are not a complete charge estimate. Provider error bodies and secrets\nare excluded from diagnostics.</p>\n<p>Each accepted hit adds a <code>rerank</code> evidence entry containing its baseline rank,\nfinal rank, and model-assigned relevance probability. The original score and\nretrieval evidence remain available. The probability is a ranking signal; it\nis not calibrated confidence that the note is correct. Explicit priority rules\nrun after reranking and retain final ordering authority. Read the underlying\nnotes and cited sources before acting on them.</p>\n<h2 id=\"use-the-sdk\">Use the SDK</h2>\n<p>SDK callers supply credentials through <code>TYPESAFE_API_KEY</code> and explicitly install\nthe engine. Global credential-file discovery belongs to the CLI.</p>\n<pre><code class=\"language-ts\">import { createTypeSafeReranker, openKnowledgeBase } from &quot;@hraness/wordcell&quot;;\n\nconst kb = await openKnowledgeBase(\n  { root: &quot;kb&quot; },\n  { rerankers: [createTypeSafeReranker()] },\n);\ntry {\n  const result = await kb.search({\n    query: &quot;why releases use immutable archives&quot;,\n    mode: &quot;exact&quot;,\n    limit: 5,\n    rerank: { engine: &quot;typesafe&quot;, limit: 25 },\n  });\n  console.log(result);\n} finally {\n  await kb.close();\n}\n</code></pre>\n<h2 id=\"evidence-and-limits\">Evidence and limits</h2>\n<p>A frozen evaluation on all 300 public BEIR SciFact test queries searched 5,183\nscientific abstracts through Wordcell's actual exact-mode SDK, then reranked\nup to 25 candidates with the production prompt and <code>jev-1.13.0</code>. The first\n40 queries were followed by an unchanged 260-query confirmation; no query was\nexcluded and no prompt or threshold was tuned after seeing results.</p>\n<table>\n<thead>\n<tr><th>Metric</th><th align=\"right\">Exact baseline</th><th align=\"right\">Reranked</th></tr>\n</thead>\n<tbody>\n<tr><td>nDCG at 5</td><td align=\"right\">0.4024</td><td align=\"right\">0.5781</td></tr>\n<tr><td>Relevant result at rank 1</td><td align=\"right\">101/300</td><td align=\"right\">161/300</td></tr>\n<tr><td>Recall at 5</td><td align=\"right\">0.4628</td><td align=\"right\">0.6125</td></tr>\n<tr><td>Candidate recall at 25</td><td align=\"right\">0.6158</td><td align=\"right\">0.6158</td></tr>\n</tbody>\n</table>\n<p>nDCG improved for 87 queries, regressed for eight, and tied for 205. The paired\n95% query-bootstrap interval for the mean gain was 0.1389 to 0.2151.\nThe confirmation set alone improved by 0.1672 (interval 0.1265 to 0.2089).\nThere were 110 queries with no judged relevant candidate; reranking could not\nrepair them. All eight regressions remain in the report.</p>\n<p>The 5,886 requests used 3,307,096 reported input tokens and 129,492 output\ntokens, with no provider failures. Added reranking latency in this local run\nwas median 1.782 seconds and p95 1.897 seconds at concurrency four. These are\nwarm local measurements, not an end-to-end service guarantee. The run host\nwas an Apple M4 Max with 128 GiB RAM, macOS 26.5.2, and Bun 1.3.14;\nthat environment was checked after the run rather than captured in a frozen\nrun receipt.</p>\n<p>See the <a href=\"https://github.com/hraness/wordcell/blob/main/docs/evaluations/wordcell-scifact-20260919.json\">frozen result report</a>.\nThe public dataset can overlap model training data; it does not prove private\nrepository-KB quality, probability calibration, or hybrid-mode gains. Freeze\nrepresentative repository questions and expected source notes before comparing\nbaseline and reranked results in a new domain. Preserve misses, regressions,\nlatency, fallback, and token usage in that comparison.</p>\n",
  "publishing": "<h1 id=\"publish-wordcell\">Publish Wordcell</h1>\n<p>The canonical artifact contract starts at <code>0.19.4</code>; that first attempt stopped\nwith a retained partial draft. Each successful release contains\none checked package archive, its packing receipt, a source/run manifest,\nchecksums, and signed GitHub provenance. The same tag run then publishes those\nexact archive bytes to npm as <code>@hraness/wordcell</code> through OIDC trusted\npublishing. An npm outage does not undo a GitHub release; the npm job is\nretried on its own.</p>\n<p>Wordcell 0.20.0 renamed the product from KB. The package is <code>@hraness/wordcell</code>,\nthe commands are <code>wordcell</code> and <code>wordcell-evaluation-builder</code> (<code>kb</code> remains a\ndeprecated alias through 0.20.x), the public Agent Skill is <code>wordcell</code>, and the\nhomepage is <a href=\"https://wordcell.io\">wordcell.io</a>. The vault format keeps its\n<code>kb</code> names; see the README. Versions through 0.19.6 stay published under\n<code>@hraness/kb</code> with their tags and Releases. The historical npm-first\nprocedure is preserved in\n<a href=\"https://github.com/hraness/wordcell/blob/v0.19.2/docs/publishing.md\">the 0.19.2 source</a>.</p>\n<h2 id=\"prepare-a-canonical-release\">Prepare a canonical release</h2>\n<ol>\n<li>\n<p>Merge the intended source and a strictly increasing stable version through\na current-head pull request. Resolve review threads and require <code>Required</code>\nCI. Stable components are canonical decimal integers bounded by\n<code>Number.MAX_SAFE_INTEGER</code>.</p>\n</li>\n<li>\n<p>Run the required local source gate with Bun <code>1.3.14</code>, Node <code>24</code>, and npm\n<code>11.19.0</code>:</p>\n<pre><code class=\"language-sh\">bun install --frozen-lockfile --ignore-scripts\nbun run check\ngit status --porcelain --untracked-files=all -- dist bun.lock\n</code></pre>\n<p>The final command must produce no output. Preserve separate native,\ninstalled-package, and live acceptance whenever the change affects them.</p>\n</li>\n<li>\n<p>Verify exact current <code>main</code>, the package version, and the release-tag\nrulesets. <strong>Immutable version tags</strong> must prevent updates and deletion\nwithout bypass. <strong>Release tag creation</strong> must allow only owner User\n<code>894119</code> to create <code>refs/tags/v*</code>; it must not allow generic Actions,\nadministrators, roles, teams, or other integrations to bypass creation.</p>\n</li>\n<li>\n<p>Create the matching annotated <code>v&lt;version&gt;</code> tag on that reviewed <code>main</code>\ncommit using the owner's existing Git credential, then push that exact\ntag. Never move a tag or create a probe tag.</p>\n</li>\n<li>\n<p>Wait for the tag-triggered <strong>Release</strong> workflow and inspect its immutable\nrelease readback. Do not begin another stable release until this one has\ncompleted or its exact partial state has been reconciled.</p>\n</li>\n</ol>\n<p>The workflow checks owner actor and event-sender identity and public\nrepository ID <code>1308971873</code> before checkout. Its read-only verification job\nchecks out exact current <code>main</code>, requires the protected annotated tag and\ncurrent workflow closure, materializes exact tagged source, runs the complete\nsource gate, and checks committed outputs. It packs once with\n<code>npm pack --ignore-scripts</code> and smokes those exact bytes with both Bun and npm.\nPacking is local tooling, not registry publication.</p>\n<p>The handoff contains exactly:</p>\n<ul>\n<li><code>hraness-wordcell-&lt;version&gt;.tgz</code></li>\n<li><code>npm-pack.json</code></li>\n<li><code>release-manifest.json</code></li>\n<li><code>SHA256SUMS</code></li>\n<li><code>provenance.jsonl</code></li>\n</ul>\n<p>The first four files are bound to verified job outputs and attested together\nby a separate source-free job. That job reauthorizes both original and\ntriggering actors and the exact active workflow, repository, tag, source,\nrun, and attempt before requesting signing credentials. It executes no\ncheckout, package installation, or repository code.</p>\n<p>The publication job independently rebinds the artifact bytes and validates\nGitHub's cryptographic verification output. The signed certificate must bind\npublic repository and owner IDs, the exact tagged source and workflow,\nGitHub-hosted execution, the tag push, and the originating run/attempt.\nUnsigned manifest fields or matching checksums alone never grant authority.</p>\n<p>Publication discovers retained drafts in the authenticated release inventory\nand keeps the exact release ID for draft reads, uploads, and publication.\nNew drafts retain the verified creation response's ID because the release list\nresponse may omit a successful creation.\nGitHub may return 404 for a draft looked up by tag; that response alone never\nauthorizes creating another release. Publication uploads only missing matching assets, verifies\nprovider asset names, sizes, SHA-256 digests, and Actions-bot creator\n<code>41898282</code>, then downloads each exact asset ID and checks its bytes before\npublishing an immutable Latest release. It repeats the exact asset-byte readback\nafter publication. Draft descriptors may use the exact versioned URL or a\nsame-repository <code>untagged-</code> URL with exactly twenty lowercase hexadecimal digits\nand the exact asset name. Published descriptors require the versioned URL.\nThese browser URLs never carry authenticated downloads; those use the exact\nGitHub API asset ID. Current main,\nannotated tag, source ancestry, and the complete helper/workflow closure are\nrevalidated before each mutation. Existing matching state is reconciled;\nassets are never overwritten and releases are never deleted/recreated.</p>\n<p>A draft or release from another attempt is not relabeled as the current\nattempt. If its exact original evidence cannot be proved, stop with its\nrelease/run identity and reconcile that state. Retry the original authorized\noperation only after inspecting uncertain provider results.</p>\n<h2 id=\"verify-a-published-release\">Verify a published release</h2>\n<p>Resolve a published immutable version before installation. A <code>latest</code> URL is\nonly discovery; do not use it as an unchecked dependency pin. Download all five\nassets for that version into a new directory and inspect their exact names.\nWith the checked repository verifier, set <code>VERIFIED_SOURCE_SHA</code> to the\nindependently resolved annotated tag commit and run:</p>\n<pre><code class=\"language-sh\">VERIFIED_SOURCE_SHA=&lt;exact-tag-commit&gt; \\\n  node scripts/github-release.ts download &lt;new-directory&gt; &lt;version&gt;\n</code></pre>\n<p>The verifier binds release metadata, the annotated tag, packing receipt,\narchive SHA-256/SHA-512, exact checksum file, complete asset inventory, and\nsigned provenance. It invokes <code>gh attestation verify</code> with exact repository,\nsigner workflow, signer digest, source digest, source ref, hosted-runner\nrestriction, and the downloaded bundle. Both the original signed attempt and\nthe latest effective job inventory must contain the exact six release jobs,\nwith authorization, source verification, attestation, and canonical publication\ncompleted successfully. An overall failed run is accepted only when its failure\nis explained by the two known npm jobs. A failed canonical rerun, incomplete or\nduplicate inventory, changed owner/source, or an active new attempt stops\nverification. Provider-assigned job IDs may change on a failed-only retry;\nthe signed receipt's original run and attempt never change.</p>\n<p>Run the installed-package smoke on\nthat downloaded archive and packing receipt before reporting release success.</p>\n<p>End users can install a published version directly with Bun or npm using its\nversioned GitHub tarball URL. Keep dependency lifecycle scripts disabled until\nthe particular optional capability has been reviewed. Installation does not\ninitialize or modify a vault. See the <a href=\"/docs/overview#install\">README</a> for the\ncommand and runtime requirements.</p>\n<h2 id=\"publish-the-same-bytes-to-npm\">Publish the same bytes to npm</h2>\n<p>The Release workflow's <code>publish_npm</code> job runs after the immutable GitHub\nRelease is published. It is bound to the GitHub environment <code>npm-release</code>,\nwhose sole deployment policy is tag pattern <code>v*</code> with administrator bypass\ndisabled, no required deployment reviewers, and no secrets. The job holds only\n<code>actions: read</code>, <code>contents: read</code>, and <code>id-token: write</code>. It checks out no\nsource and runs no repository code. It reauthorizes the current attempt and\nboth owner actors against the active workflow and current main, downloads the\nattested artifact by its numeric artifact ID, rebinds every verified hash and\nthe provenance bundle, and proves that the immutable Latest Release for the\ntag carries the exact archive digest and size.</p>\n<p>Immediately before mutation it reads the exact registry version. An absent\nversion publishes; a present version with the identical <code>dist.integrity</code> is an\nalready-published rerun and succeeds without publishing; any other state fails\nclosed. Public <code>latest</code> must be older than the candidate, and <code>--tag</code> is never\npassed, so pinned npm's monotonic default-tag guard stays active. The one\nmutation is:</p>\n<pre><code class=\"language-sh\">npm publish &lt;canonical-archive&gt; \\\n  --access public \\\n  --ignore-scripts \\\n  --json \\\n  --provenance \\\n  --registry=https://registry.npmjs.org\n</code></pre>\n<p>It runs from a clean directory with empty user and global npm configuration\nfiles and no ambient <code>npm_config_tag</code>. npm 11.19 returns one receipt keyed by\npackage name; the job requires its <code>id</code> and <code>integrity</code> to match the canonical\narchive.</p>\n<p>The dependent <code>admit_npm</code> job checks out the exact reviewed verifier closure,\ndownloads the canonical archive and packing receipt through the GitHub API by\nasset ID, packs the registry version, compares complete content inventories\nwith <code>scripts/npm-package-identity.ts</code>, installs the registry version in an\nisolated consumer, and verifies registry signatures, the publish attestation,\nand SLSA provenance bound to <code>refs/tags/v&lt;version&gt;</code> of <code>release.yml</code> with\n<code>scripts/npm-release-attestation.ts</code>.</p>\n<p>If <code>publish_npm</code> fails after the GitHub Release exists, inspect the exact\nregistry state, then re-run the failed jobs of the same workflow run. The\nartifact-by-ID download and the idempotent registry check make that retry\nsafe. Never re-push the tag or create another release.</p>\n<h2 id=\"recover-the-published-0200-admission\">Recover the published 0.20.0 admission</h2>\n<p>Release run <a href=\"https://github.com/hraness/wordcell/actions/runs/34540823193\">34540823193</a>\nattempt 1 published the immutable canonical release and its exact npm mirror,\nthen admission stopped because the GitHub CLI had no <code>GH_TOKEN</code>. The protected\ntag and published bytes remain unchanged. Future tag admissions pass the\nexisting read-only GitHub token only to their verification step.</p>\n<p>After this repair is reviewed, merged, and checked on current <code>main</code>, the owner\nmay dispatch <strong>Admit published Wordcell 0.20.0</strong> (<code>admit-published.yml</code>) on\n<code>main</code>. This input-free recovery holds only <code>actions: read</code> and <code>contents: read</code>;\nit has no environment, OIDC permission, signing step, or provider mutation.\nIt verifies owner/event/run/workflow authority and fresh main before checkout,\nbefore admission, and before reporting the result.</p>\n<p>The recovery accepts only source\n<code>a3b44090b38aa22228e26b4be8e7727232b3ff17</code>, original run <code>34540823193</code>\nattempt <code>1</code>, archive SHA-256\n<code>5a3c61436d9d87ea90409e19ae8ad1511d2a5dc3f5c9a529037eb6fd49ce017a</code>,\nand the pinned SHA-512 integrity in <code>scripts/npm-admission-recovery.ts</code>.\nIt verifies all canonical assets and hosted-run certificates, checks immutable\nGitHub Latest, compares registry bytes exactly, installs with lifecycle scripts\ndisabled, and verifies npm signatures plus publish and SLSA attestations.\nThe npm provenance must identify the original tag source, run, and attempt;\nthe recovery workflow is solely new read-only verification evidence.</p>\n<p>Record the successful recovery run separately. The original failed run remains\nfailed; never move its tag, republish a version, forge its check result, or\nrelabel its provenance. A site publication datum can use the successful recovery\nrun only after it completes. No recovery result is claimed merely by merging\nthis workflow.</p>\n<h2 id=\"configure-trusted-publishing\">Configure trusted publishing</h2>\n<p>npm requires the package to exist before a trusted publisher can be bound.\nEstablish the new coordinate with a separately reviewed prerelease bootstrap\nartifact, then configure trusted publishing before tagging the first stable\nrelease. Publish exactly <code>0.20.0-bootstrap.1</code> with <code>--tag bootstrap</code>, then\nread back the complete registry version and tag inventory. npm documents that\nan explicit tag avoids <code>latest</code>; during the Soulscrape bootstrap on 2026-09-10,\nregistry readback nevertheless showed both tags and a deletion attempt returned\nHTTP 400. That observation does not establish that npm requires <code>latest</code>.</p>\n<p>The first Wordcell stable publication accepts only candidate <code>0.20.0</code>, with the\nsole published version <code>0.20.0-bootstrap.1</code>. Tags must be exactly <code>bootstrap</code>,\nor exactly <code>bootstrap</code> and <code>latest</code>, all pointing to that bootstrap. Its package\nname, version and archive integrity must match the reviewed bootstrap from\nsource <code>df48968ff2ef227acb47d72dada09be1ac5d4b60</code>:</p>\n<pre><code class=\"language-text\">sha512-31YbWYj6wCg3TmuFtNAN3bqiOyZUmfPWIlqHzKVau5BRDr41hOx/sw9FZ0D9o4v3i9k8Ly61vRMXqtIFtvYneA==\n</code></pre>\n<p>A successful full package-metadata response supplies one coherent version and\ntag snapshot. Hidden versions, additional tags, other prereleases, conflicting\nidentity or integrity, and failed or malformed reads stop publication. Later\nstable releases retain the strictly increasing stable <code>latest</code> guard. Retain\nthe bootstrap source and archive evidence; do not rebuild or republish that\nimmutable version to accommodate a later workflow change. Do not publish a\ndummy package or manually publish the stable archive: stable admission still\nrequires the exact protected tag workflow's OIDC provenance and canonical bytes.</p>\n<p>The coordinator prepares and verifies the concrete bootstrap artifact before\nrequesting the owner's interactive npm authentication. Preserve npm's current\npublication controls, including any exact-artifact approval it requires.\nSuccessful package creation does not waive those controls for later releases.</p>\n<p>Then configure the exact GitHub Actions identity once:</p>\n<pre><code class=\"language-sh\">npm trust github @hraness/wordcell --repo hraness/wordcell --file release.yml --environment npm-release --allow-publish --yes\nnpm trust list @hraness/wordcell --json\n</code></pre>\n<p>The relationship must name <code>hraness/wordcell</code>, the exact <code>release.yml</code>\nfilename, the <code>npm-release</code> environment, and publish permission. Keep package\npublishing access on <strong>Require two-factor authentication and disallow\ntokens</strong>; do not add an npm token to GitHub. Later releases need no interactive\nstep. No npm password, OTP, recovery code, session cookie, or token belongs in\nGit, workflows, task files, or chat.</p>\n<h2 id=\"retained-v0194-publication-failure\">Retained v0.19.4 publication failure</h2>\n<p><a href=\"https://github.com/hraness/wordcell/actions/runs/34353781377\">Release run 34353781377</a>\nverified source, packed bytes and attestation, then stopped after uploading\n<code>SHA256SUMS</code> to draft <code>385518557</code>. The descriptor used GitHub's temporary\n<code>untagged-ef6c1bd779e9dd4032bb</code> path; the original verifier required a published\ntag path even for a draft. Asset <code>552772852</code> is 256 bytes with SHA-256\n<code>7439c234c0a6d0166efef952e3d8ee76dfde2178934ffe8dcdebb84cd1dfe162</code>.</p>\n<p>Preserve that tag, draft, uploaded asset and original attested evidence. It is\nnot an admitted public release and must not be retried under changed source,\noverwritten, relabeled or deleted. The <code>0.19.5</code> candidate applies the reviewed\nstate-aware descriptor rule while retaining exact IDs, bytes, source and\nprovenance admission. Its install examples remain conditional until publication.</p>\n<h2 id=\"site-publication-datum\">Site publication datum</h2>\n<p>The independent Next application in <code>site/</code> builds through its own frozen\nlockfile and is required by CI. Run <code>cd site &amp;&amp; bun run check</code> before delivery;\nits README projection, source contracts, lint, types, production build, and\nHTTP runtime smoke test must pass. Vercel uses <code>site/</code> as the project root.</p>\n<p><code>site/published-release.json</code> starts with <code>version</code> and <code>verificationRun</code> both\nnull. The homepage then shows the first-release preparation state and offers\nno archive download. After the canonical GitHub release has passed public\nverification, set both fields to the exact stable Wordcell version and its\nsuccessful <code>https://github.com/hraness/wordcell/actions/runs/&lt;run-id&gt;</code> URL.\nNever use a pre-rename kb archive or an unverified source version as that datum.\nKeep both fields null if publication or verification is incomplete. Regenerate\nthe README projection and validate the site before deploying the update.</p>\n",
  "reference": "<h1 id=\"wordcell-installation-and-command-reference\">Wordcell installation and command reference</h1>\n<p><a href=\"/docs/overview#install\">Back to the quick start</a></p>\n<h2 id=\"installation-reference\">Installation reference</h2>\n<p><a href=\"https://bun.sh/docs/installation\">Bun</a> is the required runtime. GitHub Releases are the canonical distribution, and each release publishes the same archive to npm as <code>@hraness/wordcell</code>. The examples pin release <code>0.22.1</code>. Historical <code>@hraness/kb</code> installs remain available under their original package name. For signed artifact verification, see <a href=\"/docs/publishing#verify-a-published-release\">the release procedure</a>.</p>\n<h3 id=\"tell-your-coding-agent-to-install-it\">Tell your coding agent to install it</h3>\n<p>Copy this prompt into Codex, Claude Code, or another coding agent:</p>\n<pre><code class=\"language-text\">Install the `wordcell` Agent Skill from `hraness/wordcell#v0.22.1` with the standard skills\nCLI. Use the skill's runtime instructions to install the exact\nversioned GitHub release archive only when the command is missing. Verify it\nwith `wordcell doctor` and `wordcell --help`, but do not initialize or modify a vault until\nI ask.\n</code></pre>\n<p>Install the single public skill with either runner:</p>\n<pre><code class=\"language-sh\">npx skills add hraness/wordcell#v0.22.1\nbunx skills add hraness/wordcell#v0.22.1\n</code></pre>\n<p>Both commands discover the same <code>wordcell</code> skill and install it into the selected\nagent runner. Skill installation is inert: it does not initialize a vault,\nrefresh a catalog, or edit Markdown. When invoked, the skill uses an existing\n<code>wordcell</code> command or, when the command is missing, checks for Bun and installs the\nCLI from the immutable GitHub release archive.</p>\n<p>The public skills CLI reads <code>skills/wordcell/</code> from the repository. The immutable\n<code>0.22.1</code> packed release includes the same tree under\n<code>node_modules/@hraness/wordcell/skills/wordcell/</code>, and the package check verifies that the\ninstalled skill is byte-identical to the repository source.</p>\n<p>Install the two global commands with Bun:</p>\n<pre><code class=\"language-sh\">bun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.1/hraness-wordcell-0.22.1.tgz\nwordcell --help\nwordcell-evaluation-builder --help\n</code></pre>\n<p>The same GitHub archive can be installed with npm:</p>\n<pre><code class=\"language-sh\">npm install --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.1/hraness-wordcell-0.22.1.tgz\nwordcell --help\n</code></pre>\n<p>Both commands are Bun executables. Bun <code>1.3.14</code> or newer must remain in <code>PATH</code>\neven when npm performs the global installation. The conservative npm command\nabove disables dependency lifecycle scripts. Optional native search and\nrendered-browser setup remain unavailable until the relevant scripts are\nreviewed and enabled; run <code>wordcell doctor</code> to inspect the resulting capabilities.</p>\n<p>For programmatic use, add the versioned GitHub archive to a Bun project:</p>\n<pre><code class=\"language-sh\">bun add --exact --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.1/hraness-wordcell-0.22.1.tgz\n</code></pre>\n<p>The resulting dependency should remain exact:</p>\n<pre><code class=\"language-json\">{\n  &quot;dependencies&quot;: {\n    &quot;@hraness/wordcell&quot;: &quot;https://github.com/hraness/wordcell/releases/download/v0.22.1/hraness-wordcell-0.22.1.tgz&quot;\n  }\n}\n</code></pre>\n<p>Version 0.22.1 uses three public GitHub dependencies: <code>@hraness/oh</code> at\nimmutable release archive <code>v0.11.0</code> for graph projection and closure verification and <code>@tobilu/qmd</code> at commit\n<code>aa993dceb3ef8cfb71d470554ca437570f5a2b3c</code> for store-local model behavior, plus\n<code>@hraness/support-foundation</code> at commit\n<code>b32c1c81bb2444f50509ed54388758ecfab1f1c0</code> for standalone optional support.\nInstallation also needs Git and public GitHub access while it resolves those\ndependencies. Browser-cookie capture uses the exact upstream registry release\n<code>@steipete/sweet-cookie</code> <code>0.4.3</code>, which includes the host-scope, isolation, and\nexplicit Chromium Keychain fixes previously carried by the Hraness fork.</p>\n<h3 id=\"review-lifecycle-scripts-before-enabling-optional-adapters\">Review lifecycle scripts before enabling optional adapters</h3>\n<p><a href=\"https://bun.sh/docs/pm/lifecycle\">Bun blocks dependency lifecycle scripts</a>\nunless the consumer trusts them. Run\n<code>bun pm untrusted</code> in the consuming project and inspect the exact resolved\nversions and scripts before allowing any of them. Do not use <code>bun pm trust --all</code> for this package's dependency graph.</p>\n<p>The pinned QMD Git dependency has a <code>prepare</code> script that installs development\nhooks only when its own <code>.git</code> directory exists; the packaged runtime does not\nneed that script. Optional rendered capture uses <code>agent-browser</code>, whose\npostinstall downloads a platform-specific executable. QMD's native semantic\nand language-parser paths can report lifecycle scripts for <code>node-llama-cpp</code>,\n<code>tree-sitter-go</code>, <code>tree-sitter-javascript</code>, <code>tree-sitter-python</code>, and\n<code>tree-sitter-rust</code>. Trust only the packages required by the capability you have\nchosen, then reinstall and run <code>wordcell doctor</code> to verify that capability. npm runs\ndependency lifecycle scripts by default, so inspect the same packages before\nomitting <code>--ignore-scripts</code> from an npm installation.</p>\n<p>Wordcell can read explicitly selected signed-in browser state and perform\nbounded capture and network operations. Read the <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">security\npolicy</a> before using authenticated capture.</p>\n<p>Contributors can install from a checkout instead:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/hraness/wordcell.git\ncd wordcell\nbun install --frozen-lockfile\nbun link\nwordcell --help\n</code></pre>\n<p>HTTP and Archive.today capture work with the installed JavaScript dependencies. Rendered capture additionally needs a local Chromium-compatible browser. <a href=\"https://github.com/yt-dlp/yt-dlp\">yt-dlp</a> adds YouTube metadata, thumbnails, and transcripts; full audio or video localization is opt-in and some formats also need <a href=\"https://ffmpeg.org\">FFmpeg</a>. PDF ingestion uses the open-source Poppler tools <code>pdfinfo</code> and <code>pdftohtml</code>; <a href=\"https://github.com/tesseract-ocr/tesseract\">Tesseract</a> adds local OCR for scans and screenshots. URL metadata backfill requires Rust on macOS or Linux to build the immutable, fixed-network, memory-confined <code>metadata-search-engine-rs</code> helper included in the installed package. Run <code>wordcell url-metadata tool build</code> once, then use <code>wordcell url-metadata backfill</code> from any working directory.</p>\n<p>Structural commands and exact search read the current Markdown directly and\nneed no service, model, or graph database. Wordcell pins\n<a href=\"https://github.com/tobi/qmd\">QMD</a> 2.5.3 for local keyword and vector search.\n<code>--mode keyword</code> uses its full-text index without an embedding model. Hybrid\nand semantic search use a revision-pinned compact local EmbeddingGemma model;\nthe first index or vector query downloads about 300 MB. On macOS with Bun,\ninstall extension-capable Homebrew SQLite with <code>brew install sqlite</code> before\nusing vector retrieval.</p>\n<p><code>wordcell doctor</code> statically checks the pinned QMD, SQLite, sqlite-vec,\nnode-llama-cpp, and matching native packages without importing native code or\ndownloading the model. Exact and keyword search remain model-free. Wordcell also\nrefuses an older adjacent <code>.snapshot</code> directory that lacks its ownership\nmarker; inspect and remove only the explicitly named disposable directory,\nthen retry so Wordcell never guesses that unrelated files are cache data.</p>\n<h2 id=\"the-kb-vault-format\">The kb vault format</h2>\n<p>Wordcell is the product; <code>kb</code> is the vault format. The conventional vault\ndirectory is <code>kb/</code>, managed regions use the <code>&lt;!-- kb:catalog:start --&gt;</code> and\n<code>&lt;!-- kb:context … --&gt;</code> markers, the front-door frontmatter key is\n<code>kb_catalog</code>, portfolio identities use <code>kb://&lt;owner&gt;/&lt;vault&gt;/&lt;document_id&gt;</code>\nURIs, and the shell conventions in the skill and docs are <code>KB_ROOT</code> and\n<code>KB_REPO</code>. These names identify files that many repositories already commit,\nso they did not change with the product name. Every <code>wordcell</code> command reads\nand writes the same vaults that <code>kb</code> did.</p>\n<h2 id=\"start-a-vault\">Start a vault</h2>\n<p>Existing Markdown folders can be searched without initialization or an\n<code>index.md</code> file:</p>\n<pre><code class=\"language-sh\">wordcell search &quot;a phrase from your notes&quot; --root /path/to/notes --mode exact\n</code></pre>\n<p>This reads the existing files without writing a catalog. <code>refresh</code> still\nrequires an explicit <code>index.md</code> front door; use <code>kb_catalog: authored</code> in its\nfrontmatter if you want to keep it entirely hand-written. <code>init</code> creates a new\ndirectory and never merges into your existing notes.</p>\n<pre><code class=\"language-sh\">wordcell init my-kb\ncd my-kb\nwordcell clip https://example.com/article --output articles\nwordcell refresh --root .\nwordcell check --root .\n</code></pre>\n<p><code>wordcell init</code> creates an <code>index.md</code> front door plus <code>articles/</code>, <code>notes/</code>,\n<code>plans/</code>, <code>riffs/</code>, and optional repository-context <code>scopes/</code> boundaries. The\ngenerated Markdown remains ordinary Markdown: open it in Obsidian, edit it in a\ntext editor, search it with standard tools, and version it with Git.</p>\n<p>When a vault lives at <code>kb/</code> inside a repository, inspect the instructions and\nmapped context for a repository path from the repository root:</p>\n<pre><code class=\"language-sh\">wordcell agents identity packages/parser --json\nwordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell agents check --root kb --repo .\n</code></pre>\n<p>A note that declares <code>repository_scopes</code> asserts something checkable: the\nrepository still has that path. <code>wordcell check --root kb --repo .</code> compares every\nauthored declaration with the working tree and reports each current record whose\nscope is now absent, so a note left behind by a rename or deletion becomes\nvisible instead of being read as current fact. The pass is advisory: it never\nchanges the exit code, terminal plans stay silent because an absent path is\nvalid authored history, and nothing is inferred from prose. Absent-scope\nadvisories point at a note to reread, not at a proven error.</p>\n<pre><code class=\"language-sh\">wordcell check --root kb --repo . --json\n</code></pre>\n<p><code>wordcell agents identity</code> derives a canonical mapping without writing files.\n<code>wordcell context</code> lists inherited <code>AGENTS.md</code> files from the repository root toward\nthe target, verified context hubs from the nearest scope back toward the root,\nand bounded repository-scoped memory. Maintained knowledge, active plans,\ndated research, reports, and terminal plans stay in separate groups. Every\nrecord states the exact authored scope that matched and whether it exists.\nOpen only the useful summaries, then use <code>wordcell links</code>, <code>wordcell backlinks</code>, <code>wordcell list</code>,\nor <code>wordcell search</code> to expand the question deliberately.</p>\n<h2 id=\"command-surface\">Command surface</h2>\n<table>\n<thead>\n<tr><th>Command</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n<tr><td><code>wordcell init [directory]</code></td><td>Create a new vault without merging into or overwriting an existing path; the default directory is <code>kb</code>.</td></tr>\n<tr><td><code>wordcell clip &lt;url|current&gt;</code></td><td>Capture a source and write an article bundle. <code>current</code> reads an attached active tab without navigating it; <code>wordcell capture &lt;url&gt;</code> is the explicit URL form.</td></tr>\n<tr><td><code>wordcell capture show|verify|diff &lt;bundle&gt;</code></td><td>Inspect a stored capture as hostile content, verify its recorded document and optional asset hashes, or compare its exact Markdown bytes with a bounded Git ref.</td></tr>\n<tr><td><code>wordcell inspect &lt;url&gt;</code></td><td>Run acquisition and extraction without writing a bundle.</td></tr>\n<tr><td><code>wordcell pdf &lt;file-or-url&gt; [--slug &lt;slug&gt;]</code></td><td>Convert a local or public remote PDF into Markdown while retaining the original bytes, extracted images, OCR-derived text, URL provenance, and page provenance.</td></tr>\n<tr><td><code>wordcell refresh --root &lt;directory&gt;</code></td><td>Rebuild a managed catalog atomically and report graph findings. An authored-catalog vault remains unchanged.</td></tr>\n<tr><td><code>wordcell check --root &lt;directory&gt;</code></td><td>Verify catalog policy, graph integrity, and confined local image, PDF, and tldraw attachments without changing files. <code>--no-catalog</code> gates an edit lane without requiring the shared catalog refresh. <code>--repo &lt;repository&gt;</code> adds an advisory-only pass over authored <code>repository_scopes</code> against that working tree.</td></tr>\n<tr><td><code>wordcell catalog --root &lt;directory&gt;</code></td><td>Render an exhaustive disposable catalog without modifying an authored or managed front door.</td></tr>\n<tr><td><code>wordcell graph query --program &lt;name&gt; --root &lt;directory&gt;</code></td><td>Query a bounded, proof-bearing Oh projection; see <a href=\"/docs/graph-authority\">the graph guide</a>.</td></tr>\n<tr><td><code>wordcell graph rebuild|verify --root &lt;directory&gt;</code></td><td>Rebuild or verify the disposable local graph cache without editing Markdown.</td></tr>\n<tr><td><code>wordcell graph --root &lt;directory&gt;</code></td><td>Print the resolved contextual and typed graph, broken or ambiguous targets, orphans, and advisory mention candidates.</td></tr>\n<tr><td><code>wordcell backlinks &lt;note&gt; --root &lt;directory&gt;</code></td><td>Show incoming contextual links and typed relationships for a note resolved by path, title, or alias.</td></tr>\n<tr><td><code>wordcell links &lt;note&gt; --root &lt;directory&gt;</code></td><td>Traverse incoming, outgoing, or bidirectional contextual links and typed relationships with explicit depth and node limits.</td></tr>\n<tr><td><code>wordcell note create &lt;id&gt; --title &lt;title&gt; --root &lt;directory&gt;</code></td><td>Atomically create one confined Markdown note; use <code>--type concept</code> for a reusable concept.</td></tr>\n<tr><td><code>wordcell relation add|remove &lt;source&gt; &lt;predicate&gt; &lt;target&gt;</code></td><td>Idempotently edit one source note's typed outbound relationship using an exact local note ID or canonical stable <code>kb://</code> URI.</td></tr>\n<tr><td><code>wordcell relation list &lt;note&gt; --root &lt;directory&gt;</code></td><td>List a note's authored outbound and derived inbound typed relationships.</td></tr>\n<tr><td><code>wordcell percolate [note] --root &lt;directory&gt;</code></td><td>Report evidence-backed recurring-concept and missing-relationship candidates without writing notes.</td></tr>\n<tr><td><code>wordcell list --root &lt;directory&gt;</code></td><td>Filter typed nested frontmatter, tags, and repeated exact <code>--scope</code> declarations; sort by metadata, title, path, or graph counts. <code>wordcell notes</code> is an alias.</td></tr>\n<tr><td><code>wordcell index --root &lt;directory&gt;</code></td><td>Build or incrementally refresh the optional local QMD embedding index.</td></tr>\n<tr><td><code>wordcell search &lt;query&gt; --root &lt;directory&gt;</code></td><td>Combine live exact matches with local QMD keyword and vector retrieval. Use <code>--mode exact|keyword|semantic|hybrid</code>, metadata, tags, exact <code>--scope</code> filters, or bounded graph context. <code>--rules &lt;file&gt;</code> enables reviewed aliases; add <code>--priority</code> for explicit rule-based ordering. Omitted history performs no Git work; <code>--history</code> requests best-effort provenance and <code>--require-history</code> rejects unavailable or incomplete selected-note provenance.</td></tr>\n<tr><td><code>wordcell portfolio search &lt;query&gt; --registry &lt;file&gt; --workspace &lt;directory&gt;</code></td><td>Search only explicitly authorized vaults. Use <code>--shared</code> for public and organization entries or repeat <code>--vault owner/id</code> for a deliberate selection. The same <code>--rules &lt;file&gt;</code> and opt-in <code>--priority</code> apply within each selected vault before deterministic federation.</td></tr>\n<tr><td><code>wordcell portfolio audit --registry &lt;file&gt; --workspace &lt;directory&gt;</code></td><td>Audit selected vault identities, authority groups, graph references, attachments, exact duplicate content, catalogs, and Git availability without repairing or electing an authority.</td></tr>\n<tr><td><code>wordcell history &lt;note&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Return bounded direct provenance for one resolved note, including explicit oversized-commit limitations.</td></tr>\n<tr><td><code>wordcell history search &lt;query-or-path&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Search bounded commit subjects, note paths, and co-change paths without authoring links or repository scopes.</td></tr>\n<tr><td><code>wordcell context &lt;repository-path&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>List inherited guides root to nearest, reciprocal hubs nearest to root, and grouped repository-scoped current and historical memory. Use <code>--kind auto|file|directory</code> to control path interpretation.</td></tr>\n<tr><td><code>wordcell inbox --root &lt;vault&gt;</code></td><td>List recent captures without a maintained-note disposition. This is advisory and never creates links or fails merely because a source is a leaf.</td></tr>\n<tr><td><code>wordcell evaluate &lt;manifest.json&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Verify an exact frozen Git/vault snapshot and run built-in exact, QMD, metadata, graph, path-context, and Git retrievers with raw evidence, latency, resource counters, metrics, and paired intervals.</td></tr>\n<tr><td><code>wordcell publish --root &lt;directory&gt; --out &lt;directory&gt;</code></td><td>Project a vault or a selected subsection into a self-contained <code>hraness.wordcell.site.v1</code> static site with prerendered read-only pages, browser-local search, and content-addressed assets. See <a href=\"/docs/publish\">Publish a static site</a>.</td></tr>\n<tr><td><code>wordcell serve --root &lt;directory&gt; [--host &lt;host&gt;] [--port &lt;port&gt;]</code></td><td>Preview a published site over a loopback-default static file server that maps directories to <code>index.html</code>, returns the published <code>404.html</code>, and confines requests and symlinks to the root.</td></tr>\n<tr><td><code>wordcell-evaluation-builder --anchor-seal|--build --config &lt;file&gt; --artifact-root &lt;directory&gt;</code></td><td>Anchor or build a frozen evaluation corpus through the installed package boundary.</td></tr>\n<tr><td><code>wordcell url-metadata tool build|check</code></td><td>Build or validate the pinned Rust metadata-search helper through the installed package boundary.</td></tr>\n<tr><td><code>wordcell url-metadata backfill --root &lt;vault&gt;</code></td><td>Add resumable <code>url-metadata.json</code> sidecars for saved external URLs through the pinned metadata search helper and optional read-only Archive.today discovery.</td></tr>\n<tr><td><code>wordcell agents identity &lt;repository-scope&gt;</code></td><td>Derive the normalized scope, canonical hub ID and path, owning guide path, and exact reciprocal marker without writing files.</td></tr>\n<tr><td><code>wordcell agents check --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Validate context identities, exact scopes, reciprocal markers, real guide paths, collisions, confinement, and guide shape. Unmapped guides remain valid.</td></tr>\n<tr><td><code>wordcell agents audit --root &lt;vault&gt; --repo &lt;repository&gt;</code></td><td>Run the same correctness gate, then report deterministic per-guide, section, inherited-chain, long-bullet, and exact-duplicate advisories.</td></tr>\n<tr><td><code>wordcell doctor</code></td><td>Report capture capabilities and statically inspect local QMD, SQLite, sqlite-vec, node-llama-cpp, and native search prerequisites without loading a model.</td></tr>\n<tr><td><code>wordcell adapters</code></td><td>Print the installed platform capability matrix.</td></tr>\n</tbody>\n</table>\n<p>Vault commands default to the current directory and <code>index.md</code>; use <code>--root</code> and <code>--index</code> to select alternatives. Commands that report structured data accept <code>--json</code>. Run <code>wordcell --help</code> for the complete top-level surface and <code>wordcell clip --help</code> for capture, authentication, evidence, and resource-bound options.</p>\n<h2 id=\"capture-reference\">Capture reference</h2>\n<p>Use the current browser tab without navigating it:</p>\n<pre><code class=\"language-sh\">wordcell clip current --browser-live --output articles\nwordcell clip current --cdp 9222 --output articles\n</code></pre>\n<p>For <code>--browser-live</code>, first enable Chrome's local debugging connection at <code>chrome://inspect/#remote-debugging</code> (Chrome 144+). If Chrome was launched with an explicit loopback debugging port, pass that numeric port to <code>--cdp</code> instead.</p>\n<p>To open a URL with state from a path-backed Chromium profile, pass its path. The capture runs against a temporary copy, leaving the source profile unchanged. A named profile selects reusable agent-browser-managed state instead:</p>\n<pre><code class=\"language-sh\">wordcell clip https://example.com/private --browser-profile &lt;path&gt; --output articles\n</code></pre>\n<p>Each web capture writes readable Markdown, <code>capture.json</code>, localized assets, and optional evidence under <code>articles/&lt;slug&gt;/</code>. Unless media is disabled, YouTube captures add the title, description, duration, channel, thumbnail, and a locally extracted transcript when available; other video surfaces retain a poster or thumbnail instead of downloading the video by default. See <a href=\"/docs/capture\">Capture web content</a> for scopes, saved files, browser modes, media, evidence, completeness states, and limits.</p>\n<p>Schema v4 manifests bind the exact saved Markdown path, byte count, and SHA-256\ndigest. <code>wordcell capture verify articles/&lt;slug&gt;</code> checks that digest without executing\nthe content. Add <code>--verify-assets</code> to check recorded assets. Source HTML remains\nomitted unless <code>wordcell capture show</code> receives <code>--include-source-html</code>.</p>\n<p>PDF capture uses the same bundle boundary:</p>\n<pre><code class=\"language-sh\">wordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\nwordcell pdf &quot;https://example.com/document.pdf&quot; --output articles\n</code></pre>\n<p>The bundle includes byte-identical <code>source.pdf</code>, readable Markdown, <code>capture.json</code>, and content-addressed extracted images. A reviewed second pass also retains its hash-bound <code>annotations.json</code>. See <a href=\"/docs/pdf\">Capture PDF documents</a> for heading inference, OCR, screenshot metadata, completeness, and review.</p>\n<h2 id=\"graph-reference\">Graph reference</h2>\n<p>Vault-root wikilinks such as\n<code>[[notes/context-engineering|context engineering]]</code> and source-owned typed\nfrontmatter relationships are the graph's authored facts:</p>\n<pre><code class=\"language-yaml\">type: concept\ndocument_id: durable-agent-memory\nrelations:\n  supports:\n    - notes/durable-agent-memory\n</code></pre>\n<p>Predicates use lower-kebab-case. Local targets use exact vault-root IDs without\n<code>.md</code>; cross-vault targets use canonical stable <code>kb://</code> URIs. <code>wordcell graph</code>, <code>wordcell backlinks</code>, <code>wordcell relation list</code>, and <code>wordcell links</code> derive\ninverse edges and bounded paths without injecting reciprocal or inferred facts into notes.\n<code>wordcell percolate</code> proposes reusable concepts and missing connections with explicit\nsupport; an agent reviews the cited prose before authoring anything. In its V2\nresult, a missing relationship is an unordered endpoint pair with a required\npredicate, never an executable directed assertion or an automatic\n<code>related-to</code>. Common reviewed claims use <code>synthesizes</code>, <code>evidenced-by</code>,\n<code>informed-by</code>, <code>supersedes</code>, or <code>contradicts</code>; other canonical custom predicates\nremain valid when their meaning is supported.</p>\n<p>Within a portfolio, a note can target a stable cross-vault identity such as\n<code>kb://hraness/sleepyland/sound-wellness-expansion</code>. The target vault must be\nexplicitly selected for <code>wordcell portfolio audit</code> to resolve it. Missing or invalid\n<code>document_id</code> values remain legacy path identities and never gain a stable URI\nby inference.</p>\n<p>These focused views are rebuilt from current Markdown. Wordcell never commits a graph\ndatabase, generated fact file, or engine entity ID. Parallel agents therefore\nkeep editing separate notes. Each lane can run <code>wordcell check --no-catalog</code>, and the\nintegrator runs one final <code>wordcell refresh</code> for the only shared generated region in\n<code>index.md</code>. Use <code>wordcell graph --json</code> for a whole-vault structural question; when a\nquestion recurs, prefer adding a focused command with a bounded output contract\nover introducing a parallel query store.</p>\n<p>Frontmatter retains nested objects, arrays, finite numbers with safe integer precision, booleans, strings, and nulls. <code>wordcell list --where type=plan --tag ingestion --sort metadata.updated --order desc</code> answers exact questions from that authored data. Unquoted <code>true</code>, <code>false</code>, <code>null</code>, and numeric filter values are typed; keep the quotes inside the argument to match a string with the same spelling, for example <code>wordcell list --where 'external_id=&quot;9007199254740993&quot;'</code>. Hybrid search fuses exact and QMD result orders, then joins each match back to live metadata. Graph neighbors and Git provenance are returned as separate evidence. Similarity never becomes a link automatically.</p>\n<p>Repository context preserves a stricter authority boundary. <code>AGENTS.md</code> remains\nthe always-loaded, normative home for ownership, required commands,\nprohibitions, and edit gates. An optional <code>type: agent-context</code> note under\n<code>scopes/</code> holds rationale, history, examples, evidence, and links for one exact\nrepository-relative directory. Its reciprocal\n<code>&lt;!-- kb:context scopes/&lt;id&gt; --&gt;</code> marker appears before the guide headings.\nA hub cannot override its guide or become the only home of a load-bearing\nediting rule. Moving the scoped directory changes its identity.</p>\n<p>Scope hubs are ordinary Markdown in the graph and optional QMD index;\n<code>AGENTS.md</code> files remain excluded. This workflow reads repository and vault\nfiles at development time. Applications do not need to import Wordcell or couple\ntheir runtime to the vault.</p>\n<p>The package exports its full programmatic surface from <code>@hraness/wordcell</code>. Open one\nread-only vault session through <code>@hraness/wordcell/sdk</code> to share a live Markdown scan\nacross exact search, metadata queries, reads, navigation, hybrid search, and Git\nprovenance. Compose finite parallel retrieval graphs with\n<code>@hraness/wordcell/workflow</code>. <code>@hraness/wordcell/workflows</code> includes editable\n<code>decision-context</code>, <code>explain-change</code>, and <code>plan-radar</code> compositions. Focused\nlower-level entry points include\n<code>@hraness/wordcell/search</code>, <code>@hraness/wordcell/git</code>,\n<code>@hraness/wordcell/agent-context</code>,\n<code>@hraness/wordcell/agent-guide-audit</code>, <code>@hraness/wordcell/attachments</code>,\n<code>@hraness/wordcell/authoring</code>, <code>@hraness/wordcell/evaluation</code>,\n<code>@hraness/wordcell/evaluation-kb</code>, and <code>@hraness/wordcell/evaluation-builder</code>. The builder\nentry point owns frozen-corpus authoring, evidence compilation, implementation\ncommitments, seal validation, and the bounded v2 evaluation mechanics. A\nconsumer keeps its corpus, build configuration, repository-specific retriever\ndescriptors, and promotion expectations in its own repository. The installed\n<code>wordcell-evaluation-builder</code> binary exposes the same build lifecycle without a\nsource checkout.\nOther focused entries include\n<code>@hraness/wordcell/graph-authority</code>, <code>@hraness/wordcell/graph-percolation</code>, <code>@hraness/wordcell/graph</code>, <code>@hraness/wordcell/navigation</code>, <code>@hraness/wordcell/percolate</code>,\n<code>@hraness/wordcell/portfolio</code>, <code>@hraness/wordcell/query</code>, <code>@hraness/wordcell/repository-memory</code>,\n<code>@hraness/wordcell/search-rules</code>, <code>@hraness/wordcell/untrusted-content</code>,\n<code>@hraness/wordcell/source-inbox</code>, and <code>@hraness/wordcell/semantic</code>; web-capture orchestration and\ndiagnostics from\n<code>@hraness/wordcell/capture</code>; metadata search, Archive.today discovery, sidecar parsing,\nand backfill composition from <code>@hraness/wordcell/url-intelligence</code>; PDF ingestion from <code>@hraness/wordcell/pdf</code>; and reusable\ndisposable-profile helpers from <code>@hraness/wordcell/browser-profiles</code>. Embedders that\nneed the CLI's lower-level ingestion machinery can use the explicit\ncapture-primitive subpaths listed in <code>package.json</code>, including\n<code>@hraness/wordcell/clip/acquire</code>, <code>@hraness/wordcell/clip/args</code>, the DNS-pinned request and\nconnection-pool boundary at <code>@hraness/wordcell/clip/network</code>, and the browser proxy at\n<code>@hraness/wordcell/clip/network-proxy</code>. Stored-bundle inspection, capture refresh\ndiffs, and the explicit local job ledger are available from\n<code>@hraness/wordcell/clip/bundle-reader</code>, <code>@hraness/wordcell/clip/refresh</code>, and\n<code>@hraness/wordcell/clip/jobs</code>.</p>\n<h3 id=\"update-a-note-body-conditionally\">Update a note body conditionally</h3>\n<p><code>updateNoteBody</code> replaces the prose in one existing note. Read its revision\nbefore preparing the replacement:</p>\n<pre><code class=\"language-ts\">import { noteRevision, updateNoteBody } from &quot;@hraness/wordcell/authoring&quot;;\n\nconst root = &quot;kb&quot;;\nconst id = &quot;reports/weekly&quot;;\nconst expectedRevision = await noteRevision(root, id);\nconst body = &quot;# Weekly report\\n\\nThe current findings cite [[sources/study]].\\n&quot;;\nconst result = await updateNoteBody(root, id, body, { expectedRevision });\nconsole.log(result.changed, result.revision, result.documentId);\n</code></pre>\n<p><code>UpdateNoteBodyOptions</code> requires <code>expectedRevision</code>; it also accepts the same\nlocal lock options as other authoring operations. The update preserves the\nexact frontmatter bytes, including comments, custom fields, relations, and\n<code>document_id</code>. It adds a final newline when missing and uses one blank line\nbetween frontmatter and the new body, with the existing delimiter newline\nstyle. A note without frontmatter remains without it and gains no identity.\nThe operation refuses to introduce frontmatter through such a note's body.\nInput must be well-formed Unicode, and the complete rendered note must fit the\n16 MiB authoring bound.</p>\n<p>An identical result at the expected revision returns <code>changed: false</code> without\nreplacing the file. A stale revision throws <code>NoteRevisionConflictError</code>, even\nif the requested body happens to match the latest body. The operation uses the\nexisting same-note lease, replacement checks, and recovery paths; it does not\ncreate a missing note or update another file.</p>\n<p>When a replacement depends on the current prose, pair it with that prose's\nexact revision. An open knowledge-base session retains an older snapshot:\nread the revision before opening a fresh session, or derive the revision from\nthe exact complete UTF-8 content you read. Reject a truncated read. Close and\nreopen the session after a successful update. After an interrupted operation,\nread back the document and reconcile its content and revision before retrying;\ndo not assume that an error means the replacement did not become visible.\nIf a conflict or <code>NoteRecoveryRequiredError</code> reports <code>recoveryPath</code>, retain\nthose displaced bytes until recovery is resolved.</p>\n<h2 id=\"agent-skills\">Agent skills</h2>\n<p>The repository ships one reusable <code>wordcell</code> Agent Skill under <code>skills/wordcell/</code>. Its\nintent router loads focused references only when a task needs them: querying\nrepository context and agent memory, capturing URLs or PDFs, writing durable\nplans, promoting concepts and typed relationships, refreshing and checking a\nvault, or designing a setup through an interview and approved proposal. An\napproved setup may scaffold a bounded companion skill for a distinct recurring\nritual. The package smoke test keeps future tagged packages byte-identical to\nthat source tree.</p>\n<pre><code class=\"language-sh\">npx skills add hraness/wordcell#v0.22.1\n# or\nbunx skills add hraness/wordcell#v0.22.1\n</code></pre>\n<p>The skill invokes the installed <code>wordcell</code> command without depending on a repository\ncheckout. It routes setup and evolution before runtime preparation. For\nexecution workflows, runtime setup installs the pinned CLI only when the\ncommand is missing, and it never initializes or mutates a vault as an\ninstallation side effect. The repository's phase-orchestration skill remains\navailable to local repository agents but is marked internal, so public skill\ndiscovery omits it.</p>\n<p>See <a href=\"/docs/design\">Design</a>, <a href=\"/docs/portfolio\">Portfolio federation</a>, <a href=\"/docs/agent-workflow\">Agent workflow</a>, <a href=\"/docs/pdf\">PDF capture</a>, and <a href=\"https://github.com/hraness/wordcell/blob/main/CONTRIBUTING.md\">Contributing</a> for the durable contracts and development gate. hraness/wordcell is available under the <a href=\"https://github.com/hraness/wordcell/blob/main/LICENSE\">MIT License</a>.</p>\n",
  "platform-submission": "<h1 id=\"platform-submission--wordcellio-hosted-publication\">Platform submission — wordcell.io hosted publication</h1>\n<p>Evidence pack for submitting the Wordcell hosted surface to agent platforms\n(Muse connectors, Grok-style bots, Instinct-class clients) or any HTTP tool\nconsumer. Every claim below was exercised against production on 2026-09-21.</p>\n<h2 id=\"endpoints\">Endpoints</h2>\n<table>\n<thead>\n<tr><th>Fact</th><th>Value</th></tr>\n</thead>\n<tbody>\n<tr><td>Base URL</td><td><code>https://wordcell.io</code></td></tr>\n<tr><td>OpenAPI 3.1</td><td><code>GET /api/v1/openapi.json</code> (6 paths)</td></tr>\n<tr><td>MCP</td><td><code>POST /api/v1/mcp</code> — streamable HTTP; <code>create_token</code>, <code>publish_site</code>, <code>list_sites</code>, <code>delete_site</code></td></tr>\n<tr><td>Health</td><td><code>GET /api/v1/health</code> → <code>{&quot;ok&quot;:true,&quot;storage&quot;:true,&quot;artifact&quot;:&quot;hraness.wordcell.site.v1&quot;}</code></td></tr>\n<tr><td>Auth</td><td><code>POST /api/v1/tokens</code> → <code>wc_pub_…</code> bearer; digest-only storage</td></tr>\n<tr><td>Publish</td><td><code>PUT /api/v1/sites/{slug}</code> → <code>{url, digest, revision}</code></td></tr>\n<tr><td>Public reads</td><td><code>https://wordcell.io/p/&lt;key8&gt;/&lt;slug&gt;/</code> — CDN, no auth</td></tr>\n</tbody>\n</table>\n<h2 id=\"verified-surface-live-2026-09-21\">Verified surface (live, 2026-09-21)</h2>\n<ul>\n<li><code>POST /api/v1/tokens</code> minted a <code>wc_pub_</code> token self-serve; the server stores\nonly the SHA-256 digest and the first 8 hex chars own the slug namespace.</li>\n<li><code>PUT /api/v1/sites/{slug}</code> on a two-note Markdown vault returned revision 1\nwith 16 emitted files / 91,729 bytes and a <code>sha256:</code> source digest.</li>\n<li><code>GET /p/&lt;key8&gt;/&lt;slug&gt;/</code> served prerendered <code>hraness.wordcell.site.v1</code> HTML\nthrough the CDN rewrite; <code>catalog.json</code> returned\n<code>hraness.wordcell.site-catalog.v1</code>; a missing note returned the artifact's\nown <code>404.html</code>.</li>\n<li>Identical republish returned <code>idempotent: true</code> — same digest, same\nrevision, no rewrite.</li>\n<li>Changed vault produced revision 2 under a new digest; the prior digest's\nobjects were swept.</li>\n<li><code>GET /api/v1/sites</code> listed the caller's sites; <code>DELETE</code> unpublished —\nthe public URL returns 404 (worker immediately, edge after ≤60s cache TTL).</li>\n<li><code>POST /api/v1/mcp</code> answered <code>tools/list</code> with <code>create_token</code>, <code>publish_site</code>,\n<code>list_sites</code>, <code>delete_site</code>; <code>create_token</code> minted a working <code>wc_pub_</code> token\nthrough the adapter; <code>publish_site</code> consumed a <code>{&quot;upload&quot;: id}</code> asset,\n<code>list_sites</code> reported the revision, and <code>delete_site</code> unpublished it.</li>\n<li><code>POST /api/v1/uploads</code> minted a presigned PUT; raw bytes uploaded to the\nsigned URL; <code>publish_site</code> consumed <code>{&quot;upload&quot;: id}</code> and the asset served\nbyte-identical under <code>assets/&lt;digest&gt;.png</code> on both worker-direct and CDN\nreads.</li>\n<li>Unauthenticated <code>PUT</code> returns 401.</li>\n</ul>\n<h2 id=\"submission-form-facts\">Submission-form facts</h2>\n<ul>\n<li><strong>Auth model</strong>: capability token, <code>Authorization: Bearer wc_pub_…</code>. Minting\nis free and self-serve; a platform connector can mint per-installation.\nThere is no signup wall and no OAuth handshake.</li>\n<li><strong>Input contract</strong>: JSON <code>files</code> map — UTF-8 strings, <code>{&quot;base64&quot;: …}</code>, or\n<code>{&quot;upload&quot;: &quot;&lt;id&gt;&quot;}</code> from <code>POST /api/v1/uploads</code> (presigned PUT, ≤32 MiB,\nexpires in 1 day). ≤256 files / 4 MiB inline per request.</li>\n<li><strong>Output contract</strong>: <code>hraness.wordcell.site.v1</code> — the identical artifact\n<code>wordcell publish</code> emits locally. Server-side projection is the checked\nproperty: callers cannot produce contract-invalid sites.</li>\n<li><strong>Rate limits</strong>: 8 token mints and 120 publishes per client address per\nday; 60 publishes and 50 live sites per token. 429s carry <code>retryable</code> and\n<code>retryAfter</code>.</li>\n<li><strong>Privacy posture</strong>: the posted vault is materialized to a per-request\ntempdir and deleted when the request ends; only the emitted artifact\npersists. Quota counters expire in 2 days; uploads in 1 day. Token digests,\nsite records, and slug pointers persist until <code>DELETE</code>. Published sites\nare public by contract — the API refuses to be a private store.</li>\n<li><strong>Cost posture</strong>: free at launch; quotas bound provider spend. Reads are\nserved from R2 through a CDN rewrite — zero compute per read.</li>\n</ul>\n<h2 id=\"per-platform-readiness\">Per-platform readiness</h2>\n<ul>\n<li><strong>Muse / Instinct-class / Grok-style</strong>: REST + OpenAPI live and verified;\nMCP clients use <code>POST /api/v1/mcp</code> (<code>create_token</code>, <code>publish_site</code>,\n<code>list_sites</code>, <code>delete_site</code>) — a stateless streamable-HTTP adapter that\ndispatches to the identical route logic, so auth, bounds, and quotas are\nthe same surface.</li>\n<li><strong>Direct HTTP</strong>: ready now.</li>\n</ul>\n<h2 id=\"not-yet-evidenced\">Not yet evidenced</h2>\n<ul>\n<li>Vaults near the intake bounds (256 files / 4 MiB) are enforced by tests but\nnot exercised at the limit live.</li>\n</ul>\n",
  "hosted-publication": "<h1 id=\"hosted-publication-operations\">Hosted publication operations</h1>\n<p>Hosted writes use <code>hraness.wordcell.hosted-operation.v1</code>. A publication or\ndeletion names the revision it expects to replace. Concurrent writers cannot\nboth replace that revision. Keep the operation ID and exact request until its\noutcome is known.</p>\n<p>The static output still follows <code>hraness.wordcell.site.v1</code>. This operation\ncontract covers the hosted API, its receipts, and recovery after an interrupted\nrequest.</p>\n<h2 id=\"read-before-writing\">Read before writing</h2>\n<p>Call <code>GET /api/v1/sites/&lt;slug&gt;</code> with the namespace's Bearer token. A live site\nreturns <code>site.revision</code>. A <code>404</code> response includes <code>revision: 0</code> for a slug that\nhas never been created, or the retained revision of a deleted site. Authentication\nand storage failures do not establish that a slug is absent.</p>\n<p>PUT keeps the existing files and projection options and requires this member:</p>\n<pre><code class=\"language-json\">{\n  &quot;operation&quot;: {\n    &quot;contract&quot;: &quot;hraness.wordcell.hosted-operation.v1&quot;,\n    &quot;id&quot;: &quot;32d708ac-782d-4ca0-8c34-3e89ad0ed02a&quot;,\n    &quot;expectedRevision&quot;: 0\n  },\n  &quot;files&quot;: { &quot;index.md&quot;: &quot;# Handbook\\n&quot; }\n}\n</code></pre>\n<p>DELETE accepts a JSON body containing <code>operation</code> with the same shape. Use a\nnew lowercase UUID for each new operation. <code>expectedRevision</code> is a safe integer\nfrom zero through <code>Number.MAX_SAFE_INTEGER - 1</code>.</p>\n<p>Every committed operation advances the revision by one, including a new\noperation whose projected bytes match the current site. Repeating the same\noperation ID and request returns its original receipt without advancing the\nrevision. JSON object key order does not change request identity; changing a\nvalue, expected revision, method, or projection option does.</p>\n<h2 id=\"interpret-the-result\">Interpret the result</h2>\n<p>A successful PUT returns:</p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;contract&quot;: &quot;hraness.wordcell.hosted-operation.v1&quot;,\n  &quot;operation&quot;: {\n    &quot;contract&quot;: &quot;hraness.wordcell.hosted-operation.v1&quot;,\n    &quot;id&quot;: &quot;32d708ac-782d-4ca0-8c34-3e89ad0ed02a&quot;,\n    &quot;expectedRevision&quot;: 0,\n    &quot;kind&quot;: &quot;publish&quot;,\n    &quot;requestDigest&quot;: &quot;&lt;64 lowercase hexadecimal characters&gt;&quot;,\n    &quot;revision&quot;: 1,\n    &quot;status&quot;: &quot;committed&quot;\n  },\n  &quot;idempotent&quot;: false,\n  &quot;site&quot;: {\n    &quot;slug&quot;: &quot;handbook&quot;,\n    &quot;key8&quot;: &quot;abcd1234&quot;,\n    &quot;url&quot;: &quot;https://wordcell.io/p/abcd1234/handbook/&quot;,\n    &quot;digest&quot;: &quot;&lt;artifact digest&gt;&quot;,\n    &quot;sourceDigest&quot;: &quot;sha256:&lt;source digest&gt;&quot;,\n    &quot;revision&quot;: 1,\n    &quot;notes&quot;: 1,\n    &quot;files&quot;: 12,\n    &quot;bytes&quot;: 4096,\n    &quot;skippedAssets&quot;: 0,\n    &quot;createdAt&quot;: &quot;2026-09-23T00:00:00.000Z&quot;,\n    &quot;updatedAt&quot;: &quot;2026-09-23T00:00:00.000Z&quot;\n  }\n}\n</code></pre>\n<p><code>site.title</code> may also be present. DELETE returns <code>deleted: &quot;&lt;slug&gt;&quot;</code> and\n<code>revision</code> in place of <code>site</code>; its operation kind is <code>delete</code>.</p>\n<table>\n<thead>\n<tr><th>Result</th><th>Meaning</th></tr>\n</thead>\n<tbody>\n<tr><td><code>201</code></td><td>A new site was committed and acknowledged directly.</td></tr>\n<tr><td><code>200</code></td><td>A write was committed, an exact receipt was recovered, or a read completed.</td></tr>\n<tr><td><code>409 REVISION_CONFLICT</code></td><td>The expected revision is stale. Inspect the current site before preparing a new operation.</td></tr>\n<tr><td><code>409 OPERATION_ID_REUSED</code></td><td>This ID already names a different request. Preserve the original operation when reconciling it.</td></tr>\n<tr><td><code>428 OPERATION_REQUIRED</code></td><td>The versioned operation member is missing or malformed. No publication write was admitted.</td></tr>\n<tr><td><code>502 OPERATION_UNCERTAIN</code></td><td>Storage did not establish the outcome. Resolve the same operation ID.</td></tr>\n</tbody>\n</table>\n<p>Other validation, authentication, projection, and quota errors retain the\nnormal <code>{ok:false,error:{code,message,retryable}}</code> envelope. A timeout or lost\nconnection is also an uncertain outcome, even if no JSON response arrived.</p>\n<h2 id=\"reconcile-an-interrupted-request\">Reconcile an interrupted request</h2>\n<p>Call <code>GET /api/v1/sites/&lt;slug&gt;?operation=&lt;id&gt;</code> with the same Bearer token. This\nlookup never changes state.</p>\n<table>\n<thead>\n<tr><th><code>operation.status</code></th><th>Meaning and next action</th></tr>\n</thead>\n<tbody>\n<tr><td><code>committed</code></td><td>The response contains the original receipt and site or deletion result. Its revision may have been superseded. Read the current site separately before another write.</td></tr>\n<tr><td><code>pending</code></td><td>The intent exists and its expected revision is still current. Retry only the exact request and ID. A concurrent execution may still be running.</td></tr>\n<tr><td><code>conflict</code></td><td>The intent did not commit before another revision replaced its expected state. Inspect the current site and prepare a new operation if needed.</td></tr>\n<tr><td><code>unknown</code></td><td>No intent is visible for this ID. This is a point-in-time observation; an earlier request could still be arriving. Retrying the exact request and ID remains safe.</td></tr>\n</tbody>\n</table>\n<p>Pending and conflict responses include the intent's <code>contract</code>, <code>id</code>, <code>kind</code>,\n<code>requestDigest</code>, <code>expectedRevision</code>, and the top-level <code>currentRevision</code>.\nUnknown responses contain the contract and <code>{id,status:&quot;unknown&quot;}</code>. Storage\nor authorization failures are errors, never an unknown-operation result.</p>\n<p>The server freezes a prepared result after all artifact objects are durable.\nAn exact retry uses that result without projecting or uploading again. Assets\nuploaded through a presigned URL are write-once; a second PUT to the same\nupload returns <code>412</code>. Upload expiry can prevent an unprepared request from\nbeing resumed, but cannot change a previously prepared or committed result.</p>\n<h2 id=\"storage-and-compatibility\">Storage and compatibility</h2>\n<p>The API and public Worker read one authoritative site head. Its conditional\nreplacement changes the public artifact, revision, and operation receipt\ntogether. Before replacing a head, the next writer preserves its receipt in\nan immutable object. A committed operation is therefore recoverable from the\ncurrent head or its retained receipt, including after later publications.</p>\n<p>This protocol uses R2's documented conditional PUT and strong read-after-write\nconsistency. It does not require a transaction across objects. The signed proxy\nbinds the target, expiry, byte cap, condition, and payload digest. <a href=\"https://developers.cloudflare.com/r2/api/workers/workers-api-reference/\">R2 Workers\nAPI</a>,\n<a href=\"https://developers.cloudflare.com/r2/reference/consistency/\">R2 consistency</a>.</p>\n<p>Signed object reads carry R2's exact strong identity in <code>x-object-etag</code>.\nConditional writes use this header, never the ordinary HTTP <code>ETag</code>, which an\nintermediary may <a href=\"https://developers.cloudflare.com/cache/reference/etag-headers/\">weaken or remove when compressing a response</a>. Missing or\nmalformed object identities fail closed; the API does not strip <code>W/</code> or infer an\nidentity from response bytes. Deploy this Worker header before its API consumer.</p>\n<p>Existing v1 site records remain readable and migrate on their first conditional\nwrite. The Worker uses an old slug pointer only when no site record exists. A\nv2 tombstone prevents pointer fallback after deletion. Public responses may\nremain cached for up to 60 seconds.</p>\n<p>New tokens atomically reserve their short namespace. Operation journals and\nv2 site ownership bind the full token digest. Legacy tokens are admitted only\nwhen storage establishes one token for their namespace; ambiguous historical\ncollisions fail closed.</p>\n<p>Deletion retains the site tombstone and operation receipts. Publication and\ndeletion never sweep shared artifact prefixes. This preserves bytes still\nreferenced by another slug or a concurrent request. Artifact reclamation\nrequires a separate reference-aware retention policy. The current bound is\n50 reserved slugs per namespace, including deleted slugs and interrupted\noperations. A conditional namespace reservation enforces this bound across\nconcurrent writers to different slugs. A deleted slug can\nbe reused by naming its tombstone revision. Daily quotas remain best-effort\nabuse limits.</p>\n<p>Deploy the Worker before the API. The Worker rejects unconditional writes to\nsite heads and immutable journals, and rejects artifact-prefix deletion.\nOld API writers consequently fail closed during the transition. Do not roll\nback to a Worker that permits unconditional head writes after v2 heads exist.\nRetain heads, namespace ownership and capacity reservations, operation journals, and referenced\nartifact prefixes in bucket lifecycle rules.</p>\n<p>MCP exposes the same contract through <code>get_site</code>, <code>publish_site</code>, and\n<code>delete_site</code>. <code>get_site</code> accepts an optional operation ID for reconciliation.\n<code>GET /api/v1/openapi.json</code> describes the REST request shapes.</p>\n",
  "design": "<h1 id=\"design\">Design</h1>\n<p>hraness/wordcell treats a knowledge base as durable Markdown plus replaceable views.\nA vault must remain useful when the CLI is absent, and a capture must remain\ninspectable when the original page changes or disappears. Exact graph and\nmetadata views are deterministic; semantic search is optional derived state\nthat can be deleted and rebuilt.</p>\n<h2 id=\"storage-is-the-interface\">Storage is the interface</h2>\n<p>The vault is an ordinary directory of Obsidian-compatible Markdown, suitable for a text editor, Git, and standard filesystem tools. Frontmatter, headings, prose, and wikilinks are owned content. Refresh, check, graph navigation, metadata queries, and capture require no hosted account or model. A local QMD index is an optional cache for semantic recall, never the authoritative copy of a note.</p>\n<p><code>wordcell init</code> creates a small set of authority boundaries:</p>\n<ul>\n<li><code>articles/</code> contains captured sources and their local artifacts.</li>\n<li><code>notes/</code> contains maintained concepts, entities, comparisons, and syntheses.</li>\n<li><code>plans/</code> contains proposals, decisions, execution state, and verification.</li>\n<li><code>riffs/</code> contains cleaned first-person thought from dictated or stream-of-consciousness material.</li>\n<li><code>scopes/</code> contains optional pull-based context for selected repository directories.</li>\n<li><code>index.md</code> is the front door. It can be a short authored page or contain one marked, tool-managed catalog block.</li>\n</ul>\n<p>The boundaries separate what a source said from what the vault currently concludes. They are conventions expressed in Markdown and agent guides, not proprietary file formats.</p>\n<h2 id=\"setup-is-an-approved-instruction-workflow\">Setup is an approved instruction workflow</h2>\n<p>The packaged Agent Skill routes setup and evolution requests before it prepares\na runtime. It can inspect an explicitly proposed location, interview the user,\nand present exact read and write surfaces without installing Wordcell, creating a\nvault, building a QMD index, or accessing an ambient account. Only the approved\nproposal may scaffold files. A changed path, skill, repository, account, or\nintegration requires renewed approval.</p>\n<p>A vault may add zero to three companion skills for recurring rituals whose\ninputs, authority, durable output, and failure behavior need a distinct\ncontract. These are inert instruction files. They do not form a runtime plugin\nregistry, execute vault metadata, inherit account authority, or couple the\nvault to application code. An exact repeated scaffold is a no-op; divergent\ncontent, path escape, symbolic links, partial failure, and unapproved external\nsurfaces stop the workflow.</p>\n<p>The repository models these transitions with fake capabilities and verifies\npreapproval zero mutation, exact approved writes, no-op repeats, renewed\napproval, divergence, confinement, symbolic links, partial failure, and\nexternal-surface rejection. This is a tested contract example, not proof that\nevery agent or host integration complies.</p>\n<p>This interview-first setup and bounded extension model builds on Frank Chen's\npublic notes about <a href=\"https://gist.github.com/fxchen/773397095d7a6bffda621e4237da0da9\">designing a personal knowledge base with an\nagent</a>\nand <a href=\"https://gist.github.com/fxchen/09cb410b22c9c5256d80243ee925b57e\">extending it with skills</a>.</p>\n<p>Wordcell ships no <code>kb_role</code> field, lifecycle resolver or API, lifecycle CLI,\ncompatibility diagnostic, or metadata migration. A frozen value gate must show\nthat those surfaces improve deterministic agent decisions before they are\nintroduced. Current and historical plan routing remains derived from existing\nnote type, path, and status.</p>\n<h2 id=\"repository-instructions-and-context-have-different-authority\">Repository instructions and context have different authority</h2>\n<p>An <code>AGENTS.md</code> file is normative, path-scoped, and always loaded before an\nagent edits within its directory. It owns the information that must be present\nat edit time: directory ownership, required commands, prohibitions, invariants,\nand release or verification gates.</p>\n<p>A scope hub is optional and pull-based. It explains why a rule exists and\ncarries the history, examples, evidence, rejected alternatives, and links that\nwould make an always-loaded guide too large. A hub cannot override a guide, and\nit cannot be the only home of a rule whose omission could make an edit unsafe\nor invalid.</p>\n<p>Each hub maps to one exact repository-relative directory:</p>\n<pre><code class=\"language-md\">---\ntitle: Source context\nsummary: Design history and evidence for work under src.\ntype: agent-context\nscope: src\n---\n\n# Source context\n</code></pre>\n<p>The corresponding file is <code>scopes/src--25a6634263c1.md</code>. The identity consists\nof a lowercase ASCII slug made from the full scope, bounded to 48 characters,\nfollowed by the first 12 lowercase hexadecimal characters of the SHA-256 digest\nof the full NFC-normalized scope. The root scope is <code>.</code> and has the reserved\nidentity <code>scopes/repository--cdb4ee2aea69</code>. The full exact scope remains in\nfrontmatter; the readable filename is not a substitute for it. Moving a\ndirectory changes its scope and therefore its hub identity.</p>\n<p>Derive the exact tuple without writing files:</p>\n<pre><code class=\"language-sh\">wordcell agents identity src --json\n</code></pre>\n<p>The command returns the normalized scope, extensionless note ID, Markdown path,\nowning guide path, and reciprocal marker. Use its output rather than\nreimplementing slug or hash logic.</p>\n<p>The guide points back to the extensionless note ID with one exact marker before\nits headings:</p>\n<pre><code class=\"language-md\">&lt;!-- kb:context scopes/src--25a6634263c1 --&gt;\n# Contents\n\n- ...\n\n# Guidelines\n\n- ...\n</code></pre>\n<p>Mappings are reciprocal: a hub requires the marker in the <code>AGENTS.md</code> at its\nexact scope, and a marker requires that canonical hub. A guide without a marker\nis valid and remains fully normative.</p>\n<p><code>wordcell context &lt;repository-path&gt; --root &lt;vault&gt; --repo &lt;repository&gt;</code> returns the\napplicable guides from root to nearest, verified hubs from nearest to root, and\nthe authored memory records whose <code>repository_scopes</code> contain the target. The\ntext view includes summaries, not bodies. It keeps maintained knowledge, active\nplans, dated research, reports, and terminal plans in separate bounded groups,\nreports the exact declaration that matched, and prefers the deepest matching\nscope. Open only the useful record, then use <code>wordcell links</code>, <code>wordcell backlinks</code>, <code>kb list</code>, or <code>wordcell search</code> for a bounded expansion. <code>--kind auto</code> uses filesystem\nstate and a conservative path hint; <code>--kind file</code> or <code>--kind directory</code> makes\nthe target interpretation explicit.</p>\n<p><code>repository_scopes</code> is an optional array of exact, canonical,\nrepository-relative paths. Matching is case-sensitive and lexical. A directory\nscope matches itself and descendants; a file scope matches only that file.\nScopes can name future or retired paths, so existence is reported separately\nfrom validity. Active plans and maintained notes with missing scopes produce an\nadvisory; terminal plans may retain a retired path as historical evidence. The\ntool never follows Git renames or writes inferred scopes back into Markdown.</p>\n<p>The dated-research group is deliberately narrower than an arbitrary note with\na date. A record must live under <code>projects/&lt;domain&gt;/market/</code>, declare <code>type: market-research</code>, <code>status: snapshot</code>, a valid <code>as_of</code> date, and at least one\nrepository scope. Reports analogously declare <code>type: report</code>, a valid\n<code>generated</code> date, and a repository scope. Records outside those contracts stay\navailable to ordinary metadata, text, and graph queries without being labeled\ncurrent path memory.</p>\n<p><code>wordcell agents check</code> verifies canonical IDs, <code>type</code> and <code>scope</code> metadata,\nduplicate, case-fold, and Unicode-normalization collisions, repository\nconfinement, real scope directories and regular guide files, exact reciprocal\nmarkers, and the required guide shape. <code>wordcell agents audit</code> runs the same gate\nand adds deterministic measurements for every guide and section, inherited\nchains, long guideline bullets, and exact duplicate rules. Those measurements\nidentify review candidates. Length is not a correctness test, and moving a\nload-bearing rule out of an <code>AGENTS.md</code> file to satisfy a budget makes the\nsystem worse.</p>\n<p>Guide discovery skips common version-control, dependency, cache, coverage, and\nbuild-output directories. It never follows symbolic-link directories, and a\nmapped or discovered <code>AGENTS.md</code> symbolic link is invalid. These constraints\nkeep the inherited chain reproducible and confined to the selected repository.</p>\n<h2 id=\"the-graph-is-explicit\">The graph is explicit</h2>\n<p>The graph is built from wikilinks and typed relationships in authored Markdown.\nA scan parses note identity, title, aliases, tags, typed metadata, readable\ntext, outgoing links, and outbound assertions. It resolves each target and\nreports broken or ambiguous references rather than choosing a convenient\nmatch.</p>\n<p>Reusable concepts are ordinary notes:</p>\n<pre><code class=\"language-md\">---\ntype: concept\ntitle: Durable agent memory\naliases:\n  - persistent agent memory\n---\n\n# Durable agent memory\n</code></pre>\n<p>A note owns each typed assertion it makes:</p>\n<pre><code class=\"language-yaml\">relations:\n  supports:\n    - notes/durable-agent-memory\n  contrasts-with:\n    - notes/conversation-history\n</code></pre>\n<p>Predicates use lower-kebab-case and targets use exact vault-root note IDs\nwithout <code>.md</code>. The source note is the implicit subject. Different agents can\ntherefore edit relationships on different notes without contending on a central\nontology or edge file.</p>\n<p>The recommended vocabulary covers common Wordcell claims: <code>synthesizes</code>,\n<code>evidenced-by</code>, <code>informed-by</code>, <code>supersedes</code>, and <code>contradicts</code>. It is advisory,\nnot a closed ontology. A vault may author another canonical predicate when its\nprose and evidence define the claim. Note type, directory, chronology, shared\ntags, and semantic similarity do not choose a predicate.</p>\n<p>Four rules keep the result honest:</p>\n<ol>\n<li>Backlinks and inverse relationships are derived, never written into source\nnotes.</li>\n<li>The catalog or authored front door is navigation, so links to or from its\nnote (<code>index.md</code> by default) do not count as contextual edges.</li>\n<li>A title, alias, recurring tag, shared neighborhood, or semantic match is a\ncandidate. It becomes an edge only after an agent or person reviews the\nevidence and authors the assertion.</li>\n<li>Reciprocal, inverse, transitive, and similarity-derived relationships\nremain query results. They never silently become Markdown facts. External or\nunclassified material remains unresolved until evidence supports an authored\nassertion.</li>\n</ol>\n<p>This makes inbound and outbound counts, backlinks, relationships, and orphans\nreproducible. It also prevents reciprocal sections and generated catalogs from\nmaking a disconnected vault appear healthy.</p>\n<p>Fenced code, inline code, frontmatter, and HTML comments are excluded from\nmention analysis. Line breaks are preserved during masking so diagnostics\ncontinue to point at the authored source.</p>\n<h2 id=\"focused-graph-views-are-rebuilt-from-markdown\">Focused graph views are rebuilt from Markdown</h2>\n<p>Every structural command scans the current notes and resolves canonical note\nidentities, contextual wikilinks, and source-owned typed relationships. The\npackage does not maintain a second graph database or generated fact file.\n<code>wordcell graph</code> returns the whole resolved graph and its diagnostics;\n<code>wordcell backlinks</code> and <code>wordcell relation list</code> answer focused inbound or typed-edge\nquestions; and <code>wordcell links</code> performs cycle-safe traversal with explicit depth and\nresult limits.</p>\n<p><code>wordcell percolate</code> runs named, read-only analyses that surface repeated tags\nwithout concept notes, unconnected shared-concept neighborhoods, exact\nunlinked mentions, and relationship-hygiene findings. The output cites the\nauthored evidence that caused each candidate. A person or agent decides whether\nto run <code>wordcell note create</code> or <code>wordcell relation add</code>.</p>\n<p>Percolation Result V2 emits missing relationships as unordered endpoint pairs\nwith a required predicate. It does not present either endpoint as the source,\ndraw a directional edge, or suggest <code>related-to</code>. The reviewer reads both\nnotes, then authors a directed assertion only when the evidence determines its\nowner, target, and predicate. Explicit V1 parsers preserve historical\nunversioned results for one deprecation cycle; the default parser accepts V2\nonly, and no compatibility path guesses a semantic upgrade. V1 remains\navailable throughout 0.19.x and is not removed before 0.20.0.</p>\n<p>This named-command surface is deliberate. Common graph questions receive a\nsmall typed contract, deterministic ordering, and an operation-specific bound\ninstead of requiring every agent to construct an ad hoc query program. A\none-off whole-vault question can inspect <code>wordcell graph --json</code>; a recurring question\nearns a focused command and regression tests when real use demonstrates the\nneed.</p>\n<p>Commands that only need links and typed relationships skip quadratic\nprose-mention discovery. A scoped <code>wordcell percolate &lt;note&gt;</code> considers only mention\npairs touching the resolved note; vault-wide percolation and ordinary graph\nmaintenance use explicit pair and result budgets. Scans reject more than 10,000\nnotes before parsing, then bound each note at 16 MiB of valid UTF-8 and the\nvault at 256 MiB. These are package ceilings; callers may select lower\noperation-specific limits.</p>\n<p>Rebuilding views from Markdown keeps Git history on assertions people can read\nand avoids a repository-wide merge hotspot. A future cache may live outside the\nvault only if measurements justify it; it must be content-addressed by source\nand analysis version and rebuild on any mismatch.</p>\n<h2 id=\"oh-adoption-stops-at-a-review-candidate\">Oh adoption stops at a review candidate</h2>\n<p><code>createOhAdoptionPreparerV1</code> captures, in trusted host code, one exact Oh\nworking-authority binding and head plus the destination purpose and proposed\n<code>notes/</code> path, a purpose-matched rights decision, the required review route,\nand the conflict assessment. Its returned facade accepts only an Oh dependency\nclosure and explicit transformation or redaction disclosures. A model cannot\nreplace the source authority, destination, rights, review, or conflict policy\ninside a preparation call.</p>\n<p>Wordcell delegates contract, binding, head, record, and exact dependency-closure\nverification to the immutable <code>@hraness/oh</code> v0.2.0 store API. Wordcell keeps lower\nlocal byte, record, root, depth, and node ceilings and rejects accessors,\nsymbols, cycles, canonical-authority bindings, tampered or incomplete records,\nover-complete closures, wrong bindings or heads, and derived-only roots. The\nreview artifact records the source authority and binding digest, full head,\nclosure roots, and record digests without copying source realm or space IDs.</p>\n<p>The returned status is always <code>prepared</code>. The function does not open a vault,\nwrite a note, invoke Git, import an operation chain or database, retain a\nprojection, or write to canonical Oh. A reviewer must inspect the candidate and\nauthor destination Markdown through Wordcell's existing revision-checked write path;\nthe source's proposed assertion is never relabeled as reviewed knowledge.</p>\n<h2 id=\"catalog-ownership-is-explicit\">Catalog ownership is explicit</h2>\n<p>A managed vault gives one marked region in <code>index.md</code> to the tool. <code>wordcell refresh</code>\nrenders a sorted catalog and atomically replaces only that region. Text outside\nthe markers belongs to the author. Malformed or duplicate markers fail closed.</p>\n<p>An authored vault declares <code>kb_catalog: authored</code> in <code>index.md</code>. Refresh and\ncheck leave the complete file untouched, while <code>wordcell catalog</code> renders the same\nexhaustive inventory on demand. This removes a repository-wide generated-file\nhotspot without weakening the scan, graph, metadata, attachment, plan, research,\nor context checks.</p>\n<p><code>wordcell check</code> computes the expected managed catalog when one exists and applies\nthe remaining vault policy in either mode. <code>wordcell check --no-catalog</code> skips only\ncatalog freshness, which lets independent lanes validate their notes before\nintegration. A managed vault still performs one final refresh after lanes join;\nan authored vault has no shared generated Markdown write.</p>\n<p><code>wordcell graph</code> exposes the scan as a human-readable or structured report.\n<code>wordcell backlinks</code> and <code>wordcell relation list</code> use the same identities to retrieve\nincoming links and typed assertions. <code>wordcell links</code> traverses both kinds of authored\nedge to a bounded depth and node count, reporting when a high-degree\nneighborhood reaches the cap. There is no second graph state to synchronize.</p>\n<p>Single-note authoring commands confine paths to the vault, reject symbolic and\nhard-linked targets, serialize local same-note writers, compare an optimistic\nsource revision, and atomically replace the source file. Different-note writes\ndo not share a lock or graph file. Git remains the cross-worktree review and\nmerge mechanism.</p>\n<p>The single-note transaction has one internal Effect owner behind the Promise\nAPI. It retains the lock through every admitted native write and directory\nsync, including a sibling sync still pending after another fails. A visible\nreplacement and its directory durability are separate milestones: failure\nafter installation cannot authorize overwriting that replacement with older\nbytes. Existing revision, inode and no-clobber recovery checks decide which\nrecovery operation is safe. Temporary cleanup and outer lock release keep\ntheir established error precedence.</p>\n<h2 id=\"exact-metadata-is-authored\">Exact metadata is authored</h2>\n<p>Frontmatter is parsed as typed, nested data rather than flattened strings. Scalars retain their string, number, boolean, or null type; arrays and objects retain their structure. Tags from frontmatter are normalized for matching while the original metadata remains available in structured output.</p>\n<p><code>wordcell list</code> filters that authored state by nested dotted paths, field existence,\ntags, or repeated exact repository scopes, then sorts by title, path, graph\ncounts, or nested metadata. <code>wordcell search</code> and the SDK expose the same\ncase-sensitive scope constraint. Repeated filters are conjunctive; repeated\nscope values form one exact allowlist. Missing sort values are placed last and\nties are stable, so the same vault and query produce the same order.</p>\n<p>Metadata is useful for exact questions such as “which implementation plans are in progress?” It is not inferred from prose and the tool does not invent tags to improve retrieval. Authors and agents can evolve conventions in the vault's scoped <code>AGENTS.md</code> files without migrating to a package-owned schema.</p>\n<h2 id=\"hybrid-retrieval-keeps-its-evidence-visible\">Hybrid retrieval keeps its evidence visible</h2>\n<p><code>wordcell search</code> starts with the current Markdown. Its exact lane scans note identity,\ntitle, aliases, path, tags, typed metadata, and prose. Exact title and alias\nidentities remain visible in the result evidence and stay ahead of broader\nmatches.</p>\n<p>Hybrid mode is the default. It runs the exact lane alongside <a href=\"https://github.com/tobi/qmd\">QMD</a>.\nWordcell requests QMD's direct local full-text and vector rankings at the declared\ncandidate bound, then fuses them without query expansion or reranking models.\nThis avoids QMD 2.5.3's smaller fixed structured-hybrid pool. Both the inner\nQMD lists and the outer exact/QMD lists receive neutral equal weights in\nreciprocal-rank fusion. Exact title, alias, and path identities are\npinned separately, while agreement between lanes outranks single-lane evidence.\nEach result reports the lane ranks and contributions that produced its final\nposition. <code>--mode exact</code> stays model-free,\n<code>--mode keyword</code> uses QMD's full-text index, and <code>--mode semantic</code> selects its\nvector lane.</p>\n<p>Reranking is opt-in through <code>--rerank typesafe</code>. It uses <code>jev-1.13.0</code> and sends\nthe query plus each candidate's identifier, title, vault-relative path, and at\nmost 512 UTF-8 bytes of snippet text to TypeSafe. Enable it only for vaults\napproved for external processing and provider input-token charges. Use\n<code>--rerank-limit 25</code> to bound the window (2–25 candidates); four requests run\nconcurrently under one eight-second deadline with no retries. Exact identities\nremain first. Results retain their original scores and retrieval evidence.</p>\n<p>The CLI reads <code>TYPESAFE_API_KEY</code>, an explicit <code>TYPESAFE_API_KEY_FILE</code>, or the\nowner-only file <code>~/.config/wordcell/typesafe-api-key</code> (honoring\n<code>XDG_CONFIG_HOME</code>). Keep credentials outside the repository. A missing key or\nprovider failure retains baseline ordering and marks the rerank lane\nunavailable or degraded. Inspect its structured <code>rerank</code> receipt for attempted\nrequests, known usage, elapsed time, and whether usage is complete; an unknown\ncharge is never reported as zero. A successful exit alone does not establish\nthat reranking occurred. Omit <code>--rerank</code> for local-only retrieval.</p>\n<p>For a repository's ordinary KB searches, prefer its approved, pinned\n<code>kb:search</code> script when present. That script declares the vault and processing\nchoice. Read returned notes before acting; model probabilities are ranking\nsignals, not proof of truth. Explicit priority rules still run last.</p>\n<p>Wordcell pins QMD 2.5.3 and one full upstream revision of its compact\nEmbeddingGemma model for local vector retrieval. The revision prevents branch\ndrift and gives the model a revision-specific cache identity. Without an\nexplicit local source, the first hybrid or semantic query downloads that\nrevision; later runs reuse the local cache and incrementally update changed\nMarkdown.</p>\n<p>An explicit model file is accepted only when its SHA-256 matches the pinned\nartifact. Wordcell gives QMD that file as the per-store load source while retaining\nthe stable model URI and digest as derived-index identity. QMD 2.5.3's public\nvector method falls back to a process-global model for query embeddings, so Wordcell\nuses QMD's exposed per-store vector boundary for both query and document\ninference. That QMD release also asks its process-global model to tokenize fresh\ndocument chunks and legacy fingerprint samples. Wordcell pins an <a href=\"https://github.com/hraness/qmd/commit/aa993dceb3ef8cfb71d470554ca437570f5a2b3c\">immutable public\nHraness QMD compatibility commit</a>\nthat routes those two internal calls through QMD's existing store-local model\nwithout changing its public chunking API. The\nfork includes compiled distribution files so standalone Git installs do not\ndepend on consumer-relative patches or installation-time compilation. The local model-file path does not\nenter reports, SDK results, or generation identity, and moving identical model\nbytes does not require a new logical index.</p>\n<p>Each vault gets a path-derived SQLite cache under the user's cache directory unless <code>--database</code> selects another file outside the vault. Wordcell refuses a database symlink or multiply linked database file and claims its adjacent snapshot directory with a versioned ownership record before cleanup. It scans and bounds the live Markdown first, then atomically refreshes a disposable validated source projection beside the database. QMD indexes that projection, so it cannot read a note that bypassed Wordcell's per-note or aggregate vault limits or recursively ingest its own cache. Cached files are checked against the manifest before reuse. An older snapshot directory without the ownership record is never removed automatically; delete the explicitly named disposable <code>.snapshot</code> directory and retry.</p>\n<p>A database-scoped process lease serializes projection installation, store updates, and embedding writes across agents. The generation identity includes the immutable note bytes and the QMD version, embedding model, collection configuration, and projection contract that interpret the shared SQLite state. Sessions with the same identity may read concurrently; an identity change waits for older readers to close before mutating the database. QMD operations within one open session remain serialized. <code>index.md</code> and every <code>AGENTS.md</code> are excluded because they are navigation and always-loaded instructions rather than knowledge records. Scope hubs remain ordinary Markdown, so QMD indexes their rationale and evidence like any other note. The database and source projection may be removed at any time and recreated with <code>wordcell index</code>.</p>\n<p>Search results are joined back to the live session snapshot, so each hit carries\ncurrent typed metadata and tags. Files outside the requested vault and stale\nindexed identities are discarded. Metadata and tag constraints are authoritative\nat that join boundary. QMD 2.5.3 cannot rank against a path allowlist, so a\nfiltered search uses a bounded global candidate window. Selective searches use\nthe largest supported window by default. Any observed QMD rows discarded by\nlive reconciliation or filters leave an underfilled request explicitly degraded,\neven when QMD's chunk-level retrieval returned fewer rows than requested. QMD failure\ndoes not erase exact results; the response marks a failed lane unavailable or\nan incomplete embedding pass degraded, and reports that the result is partial. A\nretrieval score is a discovery aid, not a graph edge, a citation, or evidence\nthat the result is true.</p>\n<p>Immediate explicit links and typed relationships can be returned with search,\nalong with a bounded neighborhood around the strongest results. These graph\nneighbors remain a separate context collection. They do not enter primary text\nrank or become authored edges. When explicitly requested, bounded Git history\ncan likewise explain when a note changed and which paths changed with it. <code>kb history &lt;note&gt;</code> retrieves one note's provenance directly, and <code>wordcell history search &lt;query-or-path&gt;</code> searches commit subjects, note paths, and co-change\npaths without running text retrieval. <code>--history</code>, <code>--require-history</code>, or SDK\nhistory options enable that separate lane on search. Omitted history performs\nno Git indexing, and an explicit request with no\nprimary results has nothing to enrich. Query, note, and detail bounds are\nvalidated before the Git index opens. A commit that exceeds the per-commit\nchanged-path detail limit retains its\nhash, subject, time, and vault-local note associations while its co-change set\nis marked incomplete. Later commits continue indexing. Automatic history\nreturns the usable provenance with a degraded diagnostic only when the selected\nnotes are affected. Optional Git failure returns an explicit unavailable\ndiagnostic. Both cases mark the search partial. <code>history: &quot;required&quot;</code> or an\noptions object with <code>policy: &quot;required&quot;</code> rejects unavailable or incomplete\nselected-note provenance instead. Aggregate process time, output, commit, and\npath-observation limits remain hard failures. Git evidence is provenance and\nhistorical recall, not a recency boost.</p>\n<h2 id=\"code-mode-shares-one-bounded-snapshot\">Code mode shares one bounded snapshot</h2>\n<p>Agents that need several retrieval operations can use the SDK without spawning\none CLI process per question:</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase, packUntrustedSearchContext } from &quot;@hraness/wordcell/sdk&quot;;\n\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot;, repository: &quot;.&quot; });\ntry {\n  const result = await kb.search({\n    query: &quot;why captures preserve incomplete threads&quot;,\n    tags: [&quot;capture&quot;],\n    graph: { depth: 1 },\n    history: &quot;auto&quot;,\n  });\n  console.log(packUntrustedSearchContext(result).content);\n} finally {\n  await kb.close();\n}\n</code></pre>\n<p>Opening a session performs one confined vault scan. <code>grep</code>, <code>list</code>, <code>read</code>,\n<code>links</code>, <code>backlinks</code>, <code>search</code>, <code>history</code>, and <code>searchHistory</code> reuse that\nsnapshot. QMD and Git are opened lazily. The session is intentionally read-only\nand does not watch the filesystem. Close it and open a new session after any\nMarkdown write so later work cannot mistake an old snapshot for current state.</p>\n<p><code>packUntrustedSearchContext</code> accepts ordinary plain objects and arrays, such as\nvalues produced by JSON parsing or Wordcell itself. Do not pass same-realm <code>Proxy</code>\nobjects: proxy inspection can execute user code and is outside a data-only\nprojection boundary. Isolate or serialize foreign executable objects before\npacking them.</p>\n<p>Code-mode DAGs use <code>defineWorkflow</code> and <code>runWorkflow</code>. The staged\n<code>defineWorkflow&lt;Input&gt;(&quot;id&quot;).node(...).output(...)</code> builder infers each node's\nresult and the final output while exposing only declared dependencies. A\ndefinition has at most 64 nodes, must be acyclic, and names one output node.\nReady nodes run in declaration order with a default global concurrency of four\nand a maximum of eight. QMD work is always serialized; Git permits at most four\nnodes, bounded again by the global limit. The runner applies an aggregate\nstructured-output byte limit. Failure or abort stops dependent nodes from\nstarting and waits for already-running siblings to settle. The packaged\nworkflows are ordinary imports, accept explicit inputs, and return structured\nresults without writing the vault.</p>\n<p>One internal Effect program owns that scheduling lifetime. Native callbacks\nstill enter through the original Promise microtask and settle through the\nnative Promise race, preserving observable ordering and raw failure reasons.\nAn interrupted fiber does not stand in for a settled callback. The public\nrunner continues to return a Promise, so callers need no Effect runtime or\nservice configuration.</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase } from &quot;@hraness/wordcell/sdk&quot;;\nimport { runWorkflow } from &quot;@hraness/wordcell/workflow&quot;;\nimport { explainChangeWorkflow } from &quot;@hraness/wordcell/workflows&quot;;\n\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot;, repository: &quot;.&quot; });\ntry {\n  const explanation = await runWorkflow(explainChangeWorkflow, {\n    kb,\n    input: { query: &quot;why the capture path changed&quot; },\n  });\n  console.log(explanation.output);\n} finally {\n  await kb.close();\n}\n</code></pre>\n<p><code>decisionContextWorkflow</code> assembles ranked rationale and note provenance,\n<code>explainChangeWorkflow</code> searches authored rationale and Git evolution in\nparallel, and <code>planRadarWorkflow</code> joins exact plan state with retrieval and\nhistory.</p>\n<p>Only <code>decisionContextWorkflow</code> returns a bounded untrusted context envelope.\n<code>explainChangeWorkflow</code> and <code>planRadarWorkflow</code> intentionally return raw\nsource-derived Wordcell and Git structures for trusted application code to inspect.\nTreat every string field in those results as untrusted data: do not execute it\nor place it in a model instruction channel, and project or pack the selected\nfields through the untrusted-content boundary before an agent handoff.</p>\n<p>Changes to retrieval ranking use the exported deterministic metric helpers for\nrecall at k, reciprocal rank, and nDCG. The six-case synthetic rank-fusion\nfixture supplies already-ranked IDs for identity, conceptual, and mixed\nexamples. It checks metric and fusion arithmetic only.</p>\n<p>The real-corpus evaluator accepts a versioned manifest with query text,\nindependently authored relevance judgments, query classes, and structured lane\ninputs. <code>wordcell evaluate</code> fails before retrieval unless the checkout's exact <code>HEAD</code>,\nthe <code>HEAD:&lt;vault-root&gt;</code> tree, and the clean vault match the frozen manifest. It\nthen runs built-in exact, keyword, semantic, hybrid, graph, metadata,\npath-context, and Git adapters through one immutable session. Human query prose\nis never parsed into tool arguments; each adapter receives only its explicit\ninput object.</p>\n<p>The report retains raw rankings and evidence, unavailable and failed lanes,\nbackend and wall timings, bounded resource counters, aggregate and per-class\nquality, no-answer accuracy, and deterministic paired bootstrap intervals.\nMachine-local home and temporary roots are redacted from persisted hit evidence,\ndiagnostics, and failures while relative document identities and the surrounding\nevidence remain intact.\nSemantic or hybrid runs require <code>--model-file</code>. The evaluator verifies those\nbytes against the pinned model digest before retrieval, gives that file to QMD,\nand verifies it again before reporting. Reports retain the stable model URI,\nrevision, and digest without persisting the machine path. Cache state and\nhardware remain explicit environment evidence. The evaluator does not turn a\nlocal fixture into an industry claim: model download, cold and warm runs,\nscale, concurrency, and agent-task outcomes still need measured protocols of\ntheir own.</p>\n<h2 id=\"local-artifacts-remain-inspectable\">Local artifacts remain inspectable</h2>\n<p>Graph validation also checks local Markdown and Obsidian attachments. Relative\nimage, PDF, and tldraw targets must resolve to one regular confined file with\nmatching case. Symlinks, hard links, ambiguous case-fold matches, missing files,\nand paths outside the vault fail. External URLs and fragment-only links remain\noutside this local integrity lane.</p>\n<p><code>wordcell inbox</code> is a bounded advisory view over recent captured sources that have no\nmaintained-note disposition. Source-to-source and catalog links do not count as\nsynthesis. A capture may intentionally remain a leaf, so the inbox never writes\nlinks, creates notes, or fails the vault merely because an item is present.</p>\n<h2 id=\"capture-preserves-an-audit-trail\">Capture preserves an audit trail</h2>\n<p>Web capture is a bounded selection process rather than a promise to reproduce an unlimited website. Given a URL and requested scope, the capture pipeline can try:</p>\n<ol>\n<li>A platform-specific public structured adapter when one can make a stronger completeness claim.</li>\n<li>Bounded HTTP acquisition and article extraction.</li>\n<li>Optional browser rendering for client-side or authenticated pages.</li>\n<li>Explicit saved-HTML input when the user already has a saved representation.</li>\n</ol>\n<p>Candidates retain their attempt results. The selected representation becomes readable Markdown, while <code>capture.json</code> records the routes attempted, extractor, scope, status, counts, warnings, limits reached, asset hashes, and requested artifact outcomes. A failed lane does not erase useful output from another lane, and an uncertain fallback does not promote a conversation to <code>complete</code>.</p>\n<p>A bundle is installed atomically:</p>\n<pre><code class=\"language-text\">&lt;slug&gt;/\n  &lt;slug&gt;.md\n  capture.json\n  assets/\n  evidence/\n</code></pre>\n<p>The capture body is source material. Later synthesis belongs in a maintained note so recapture and interpretation do not silently overwrite each other.</p>\n<h2 id=\"completeness-is-a-data-property\">Completeness is a data property</h2>\n<p>Capture status distinguishes <code>complete</code>, <code>partial</code>, <code>auth-required</code>, <code>blocked</code>, and <code>unsupported</code>. The status describes the selected bounded representation, not the importance or quality of its prose.</p>\n<p>Counts use scope-specific semantics. Page counts cover primary entries; thread and comment counts cover replies or comments rather than roots, quotes, or pagination markers. Generic rendered prose does not prove a trustworthy item tree, so it may remain <code>partial</code> with a zero structured-item count even when the Markdown is useful.</p>\n<h2 id=\"safety-is-part-of-acquisition\">Safety is part of acquisition</h2>\n<p>URLs, redirects, DNS answers, response bodies, browser pages, cookies, subprocess output, and filesystem paths are foreign input. The controlled acquisition lanes therefore share several invariants:</p>\n<ul>\n<li>Only HTTP and HTTPS source URLs are accepted, with embedded credentials rejected.</li>\n<li>Private, reserved, and locally assigned network targets are denied by default.</li>\n<li>DNS answers are validated and accepted addresses are pinned across requests and redirects.</li>\n<li>Time, HTML bytes, asset bytes, total bytes, item counts, depth, browser actions, and process output are bounded.</li>\n<li>Cookies are read only from an explicitly selected source, filtered to matching targets, and kept out of persisted artifacts.</li>\n<li>Active source evidence is converted to inert HTML with credential-shaped values redacted.</li>\n<li>Bundle paths are owned, staged beside the target, and installed by atomic rename; forced replacement requires a compatible manifest and rollback.</li>\n</ul>\n<p>Live or CDP browser attachment keeps the browser's existing network stack and signed-in state. <code>wordcell clip current</code> reads the active tab without navigating or interacting with it and leaves the browser open. URL-based attached capture may navigate that tab and scroll within the configured bounds, taking bounded observations as content is rendered. Screenshots are also different from sanitized source evidence because private content can remain visible in pixels.</p>\n<p>These boundaries are not entitlement mechanisms. Capture does not bypass authentication, access controls, paywalls, CAPTCHAs, rate limits, DRM, or platform policy.</p>\n<h2 id=\"dependencies-follow-capabilities\">Dependencies follow capabilities</h2>\n<p><a href=\"https://bun.sh\">Bun</a> is the required runtime.\n<a href=\"https://eemeli.org/yaml/\">YAML</a> parses typed frontmatter,\nand <a href=\"https://github.com/tobi/qmd\">QMD</a> supplies the optional local keyword and\nembedding index. QMD is loaded only by index and search commands, so\ndeterministic graph and metadata commands do not initialize its native runtime\nor model.</p>\n<p><a href=\"https://github.com/kepano/defuddle\">Defuddle</a> performs article extraction. <a href=\"https://github.com/vercel-labs/agent-browser\">agent-browser</a> provides optional rendered acquisition. The pinned <a href=\"https://github.com/steipete/sweet-cookie/releases/tag/v0.4.3\">Sweet Cookie 0.4.3</a> supports explicit browser-cookie import while retaining host-only scope and rejecting partitioned or container-scoped state that the capture lanes cannot replay faithfully.</p>\n<p><a href=\"https://github.com/yt-dlp/yt-dlp\">yt-dlp</a> and <a href=\"https://ffmpeg.org\">FFmpeg</a> remain optional because only full audio or video localization needs them. <code>wordcell doctor</code> reports what is installed without probing cookie stores, and <code>wordcell adapters</code> reports the installed platform claims. A missing optional capability narrows the available route; it does not change the storage or graph model.</p>\n<h2 id=\"extension-boundaries\">Extension boundaries</h2>\n<p>New platform adapters should improve the strength of a capture claim, not merely add another scraper. Each adapter declares the scopes, acquisition modes, authentication requirements, item semantics, and media behavior it can support. It must remain bounded and must downgrade honestly when pagination, hidden branches, virtualized content, or access controls prevent completeness.</p>\n<p>New graph policy should remain a pure function of vault content and explicit configuration. Derived reports may guide an agent or person, but the tool should not silently mutate authored prose. This keeps automation reviewable and lets users replace any analysis layer without migrating their notes.</p>\n<p>Repository context follows the same separation. The CLI reads the repository\nand vault as development inputs, but no application needs to import Wordcell or\nread a scope hub at runtime.</p>\n",
  "agent-memory": "<h1 id=\"markdown-memory-for-coding-agents\">Markdown memory for coding agents</h1>\n<p><a href=\"/docs/overview\">Back to Wordcell</a> · <a href=\"/docs/agent-workflow\">Practical agent workflow</a></p>\n<h2 id=\"a-knowledge-base-for-your-coding-agents\">A knowledge base for your coding agents</h2>\n<blockquote>\n<p>Give coding agents durable, searchable memory beside the repository with plain Markdown, Git history, and replaceable local search.</p>\n</blockquote>\n<p>Coding agents lose useful context when a session ends. The next agent can search the code again, but it cannot recover a source that was never saved, a decision that stayed in chat, or the relationship between two notes that nobody recorded. Repeating that work costs time and produces inconsistent answers.</p>\n<p>Search alone cannot preserve agent memory. The system also needs a write path into inspectable files under version control: evidence can be captured, current understanding can be revised, plans can accumulate outcomes, and mandatory edit rules can move onto the instruction path. Search indexes, graph views, and embeddings used for meaning-based similarity should remain derived and replaceable.</p>\n<p><a href=\"https://wordcell.io\">hraness/wordcell</a> implements that split as repository-adjacent Markdown and Git. Exact lookup, metadata filters, local search, explicit links, and Git provenance help an agent find and inspect the files without making application code depend on the knowledge system.</p>\n<h3 id=\"the-pattern-converged-across-agent-tools\">The pattern converged across agent tools</h3>\n<p><a href=\"https://docs.devin.ai/release-notes/2024\">Devin's 2024 release history</a> records Knowledge that could be recalled across future sessions and Repo Knowledge produced by scanning repositories. Its <a href=\"https://docs.devin.ai/release-notes/2025\">2025 release history</a> records DeepWiki in April, codebase intelligence inside Devin in May, and a DeepWiki Model Context Protocol server later that month.</p>\n<p>In April 2026, Andrej Karpathy published an <a href=\"https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f\">LLM Wiki proposal</a> with immutable raw sources, an agent-maintained interlinked Markdown wiki, and an instruction schema. Its operations are ingest, query, and lint, with QMD as an optional search layer when a simple index stops being enough. These systems converged on durable agent-readable knowledge. The sequence does not establish direct lineage between them or hraness/wordcell.</p>\n<h3 id=\"separate-rules-from-explanations\">Separate rules from explanations</h3>\n<p>A repository needs two kinds of memory. Rules that must govern an edit belong in a scoped <code>AGENTS.md</code> file on the path to the code. Rationale, history, examples, evidence, plans, and neighboring decisions belong in a knowledge base that an agent pulls only when the task needs them. This keeps mandatory instructions short without throwing away the context behind them.</p>\n<p>A root guide carries repository-wide policy, and nested guides add constraints owned by a package or product. A nearby knowledge note can explain why a parser rejects a tempting shortcut, preserve the source behind the decision, and link the plan that introduced it. If the note and the applicable guide disagree, the guide controls the edit and the note needs repair.</p>\n<p>The result has two concrete parts: scoped instruction files govern edits, while an ordinary Markdown vault stores supporting context. Application code imports neither the vault nor its search indexes:</p>\n<p><strong>Repository rules beside durable knowledge</strong></p>\n<pre><code class=\"language-text\">repository/\n├── AGENTS.md                         # inherited root rules\n├── packages/parser/\n│   ├── AGENTS.md                     # scoped rules and checks\n│   └── src/\n└── kb/\n    ├── articles/&lt;slug&gt;/              # captured evidence and assets\n    ├── notes/                         # maintained explanations\n    ├── plans/                         # decisions and outcomes\n    └── index.md                       # short authored front door\n</code></pre>\n<h3 id=\"keep-the-implementation-small-and-the-files-authoritative\">Keep the implementation small and the files authoritative</h3>\n<p>hraness/wordcell packages the pattern as a small file contract. A useful vault can begin with Markdown, Git, <code>index.md</code>, and standard file search. Source capture, metadata queries, repository-path context, QMD, typed relationships, graph traversal, and TypeScript sessions are layers to add when the simpler setup stops answering the repository's questions. Application code need not import Wordcell, and no hosted service or graph database owns its records.</p>\n<p>Captured sources preserve evidence, notes hold current explanations, and plans retain decisions and outcomes. YAML frontmatter adds queryable metadata without requiring one domain schema for every vault. A code-related record may declare a few exact repository-relative <code>repository_scopes</code> so an agent can recover it from the path it is about. The declaration stays in the record instead of a central project database, which lets parallel agents update unrelated memory without sharing a generated file.</p>\n<p>The Markdown files are authoritative. The catalog, QMD database, backlink view, path-context view, graph traversal, and bounded Git index are derived and replaceable. A vault can keep a managed catalog or an authored front door and render the complete inventory on demand. Deleting one of those views removes a way to retrieve knowledge, not the knowledge itself.</p>\n<h3 id=\"route-current-memory-from-the-code-path\">Route current memory from the code path</h3>\n<p>A broad semantic search over years of completed plans can rank a detailed historical record above the short explanation that owns the code today. <code>wordcell context packages/parser/src/index.ts --root kb --repo .</code> starts from a stronger signal: the path being changed. It returns the inherited guides that govern the edit, curated scope hubs, and bounded records whose declared scope is that path or one of its ancestors.</p>\n<p>The records stay grouped by role. Maintained notes, proposed through blocked plans, dated market research, and generated reports form current memory. Completed, superseded, and cancelled plans remain available in a separate historical group. Every result states the declaration that matched and whether the target currently exists. A plan can therefore describe a future path, while a retired path remains honest historical evidence instead of being silently rewritten after a rename.</p>\n<p><strong>Path context, exact scope filtering, and Git memory</strong></p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell list --root kb --scope packages/parser --where type=plan --json\nwordcell history search packages/parser/src/index.ts --root kb --repo . --json\n</code></pre>\n<h3 id=\"preserve-evidence-and-plans-as-working-records\">Preserve evidence and plans as working records</h3>\n<p>Durable reasoning needs inspectable evidence. <code>wordcell clip</code> can read a public URL,\nsaved HTML, rendered page, a page already open in an authenticated browser, or\nan existing exact Archive.today snapshot after the direct routes fail. Archive\nfallback is read-only and always partial. The\n<a href=\"/docs/capture\">capture documentation</a> defines the supported routes. A capture\nwrites readable Markdown beside localized assets and <code>capture.json</code>, whose\nmanifest records where the material came from, how it was extracted, what was\nsaved, and any warnings. “Complete” describes the selected page surface, not\nevery hidden branch or future version of the site.</p>\n<p><strong>Capture a web source or local PDF</strong></p>\n<pre><code class=\"language-shell\">wordcell clip &quot;https://example.com/article&quot; --output articles\nwordcell pdf &quot;/absolute/path/to/document.pdf&quot; --output articles\n</code></pre>\n<p>The resulting bundle is evidence, not final interpretation. A maintained note can cite several captures, record disagreement, and change when later evidence warrants it. The sources stay available for audit. This prevents an agent from silently replacing what a page said with what it now believes the page meant.</p>\n<p>The <code>wordcell</code> Agent Skill routes vault planning requests to a focused durable-plan workflow. It creates a normal Markdown file under <code>kb/plans/</code> with an outcome, status, area, repository scopes, assumptions, dependencies, decisions, and verification method. The file grows during execution as agents record deviations, review findings, and reproducible evidence. Closeout adds a compact result and durable-memory disposition: each reusable conclusion links to the maintained note, guide, code contract, or runbook that now owns it, or says that no promotion was needed. Completed plans remain in Git as the history of the work. When a finding becomes a rule whose omission would make a future edit wrong, move that rule into the applicable <code>AGENTS.md</code> and retain the plan as its rationale.</p>\n<h3 id=\"search-and-connect-with-bounded-signals\">Search and connect with bounded signals</h3>\n<p>An identifier, title, alias, path, tag, or quoted phrase should not depend on an embedding. Exact mode reads the live Markdown. The default hybrid mode combines those results with keyword and vector result orders from <a href=\"https://github.com/tobi/qmd\">QMD, a local search engine for Markdown</a>, while keeping exact identity matches first. Graph context and Git provenance remain separate evidence, so neither silently changes the primary text rank.</p>\n<p><strong>Path context, exact and hybrid search, and direct history</strong></p>\n<pre><code class=\"language-shell\">wordcell context packages/parser/src/index.ts --root kb --repo .\nwordcell search &quot;parser-v2&quot; --root kb --mode exact\nwordcell search &quot;why does the parser reject this input?&quot; --root kb \\\n  --tag architecture --where status=active --json\nwordcell history &quot;notes/parser-design&quot; --root kb --repo . --json\n</code></pre>\n<p><code>--mode keyword</code> uses QMD's local full-text index without loading an embedding model. Hybrid and semantic modes use a pinned local embedding model. Wordcell reconciles every QMD hit with current Markdown before returning it, and applies metadata and tag filters to those live notes. Search modes remain explicit through <code>--mode exact</code>, <code>--mode keyword</code>, <code>--mode semantic</code>, and <code>--mode hybrid</code>.</p>\n<p>Retrieval is bounded. The high-level <code>wordcell search</code> and <code>KnowledgeBaseSession.search</code> surfaces return at most 100 primary results and request at most 500 candidates from each QMD retrieval lane. Selective filters can discard stale or ineligible rows from that window. When those discards prevent Wordcell from filling the requested eligible result set, Wordcell marks the QMD lane degraded and the overall result partial instead of presenting the bounded approximation as complete. Scores are local ranking signals, not probabilities, and cannot be compared across modes.</p>\n<p>Each note owns its outbound typed relationships in frontmatter. Wordcell derives backlinks, inverse edges, and bounded traversal at read time, so parallel agents do not contend on one generated fact file. <code>wordcell percolate &lt;note&gt;</code> reports recurring concepts and missing-link candidates with inspectable support but writes nothing. An agent reads the cited notes before creating a reusable concept or relationship. Semantic similarity never creates an edge automatically.</p>\n<p>Percolation Result V2 presents a missing relationship as an unordered pair of\nnotes with a required predicate. It does not choose the source, direction, or a\n<code>related-to</code> fallback. Recommended authored predicates include <code>synthesizes</code>,\n<code>evidenced-by</code>, <code>informed-by</code>, <code>supersedes</code>, and <code>contradicts</code>; they are an\nadvisory vocabulary, so a vault can use another canonical predicate when its\nprose and evidence define the claim. Wordcell never infers reciprocal, inverse,\ntransitive, or similarity-derived relationships.</p>\n<p>Git provenance is opt-in. A search without <code>--history</code> performs no Git indexing. <code>--history</code> requests best-effort provenance, while <code>--require-history</code> rejects unavailable history or incomplete provenance for the selected notes. If one commit exceeds the 2,000-path detail limit, Wordcell retains its identity and vault-local note associations, marks its co-change detail incomplete, and continues through later commits. Best-effort search reports that requested lane as partial.</p>\n<p>Local attachment checks cover Markdown and Obsidian references to images, PDFs, and editable tldraw sources. They reject missing or escaping files while leaving external URLs alone. A source-inbox view separately lists recent captures that have no inbound disposition from maintained knowledge. It is an advisory, not an automatic backlink requirement: a saved source may intentionally remain a leaf.</p>\n<h3 id=\"measure-retrieval-on-a-frozen-corpus\">Measure retrieval on a frozen corpus</h3>\n<p>The August 2, 2026 pilot froze one repository snapshot and 18 questions whose graded relevance judgments were written before the rankings were inspected. The evaluator scanned 156 Markdown records and projected 155 searchable notes into QMD after excluding the authored vault index and agent guides. Nine questions formed the development set, and nine were held out for the test. The test covered exact identity, conceptual recall, active plans, current decisions, code-path context, source evidence, historical rationale, stale-versus-current conflicts, and one no-answer case.</p>\n<p>At a cutoff of 10 results, exact search recorded <code>Recall@10</code> of 0.833333, <code>MRR@10</code> of 0.892857, and <code>nDCG@10</code> of 0.790377. Hybrid search recorded 0.833333, 0.937500, and 0.833884, respectively. Recall measures how much of the judged relevant set appeared; mean reciprocal rank rewards an earlier first relevant result; normalized discounted cumulative gain also accounts for graded relevance and position.</p>\n<p>Eight test questions had an answer. A 10,000-resample paired bootstrap, which repeatedly samples those same questions to estimate the stability of the difference, measured hybrid minus exact. The <code>Recall@10</code> difference was 0 with a 95% confidence interval of [0, 0]; the <code>MRR@10</code> difference was +0.044643 with [0, 0.133929]; and the <code>nDCG@10</code> difference was +0.043508 with [-0.012752, 0.111832]. Both retrievers returned a result for the one no-answer question instead of abstaining, so their no-answer accuracy was 0.</p>\n<p>The same mixed-cache, single-run test recorded p95 latencies of 44.345 milliseconds for exact, 62.834 for hybrid, 821.370 for keyword, and 41,000.524 for semantic retrieval. The semantic figure includes the first in-process model load. The run used <a href=\"https://github.com/hraness/qmd/commit/aa993dceb3ef8cfb71d470554ca437570f5a2b3c\">QMD 2.5.3 at Hraness compatibility commit aa993dc</a> and a locally verified EmbeddingGemma 300M Q8 model on Bun 1.3.14 and Node 24.3.0 under arm64 Darwin 25.5.0, with an Apple M4 Max, 16 logical CPUs, and 128 GiB of memory. Each p95 summarizes only nine queries with mixed cold and warm state, so these are local diagnostics, not speed claims. The corpus is too small to establish that hybrid is generally superior to exact search or to compare Wordcell with industry retrieval systems.</p>\n<p>Search finds candidates. Similarity does not establish that a passage is current, correct, or supported by its sources. The Markdown, cited captures, explicit relationships, and requested Git history supply the material a reader must inspect.</p>\n<h3 id=\"customize-through-an-approved-proposal\">Customize through an approved proposal</h3>\n<p>The Agent Skill routes setup and evolution requests before it prepares a\nruntime. It inspects the proposed location without mutation, interviews the\nuser about the memory questions the Wordcell should answer, and presents exact read\nand write targets. Only the approved targets may be scaffolded. A changed path,\nrepository, account, integration, or companion skill requires renewed\napproval.</p>\n<p>The standard router may be enough. A recurring ritual can instead receive a\ncompanion skill with explicit inputs, authority, durable outputs, idempotence,\nfailure behavior, and verification. These skills are inert instructions. They\ndo not create a plugin runtime, execute vault metadata, inherit ambient account\naccess, or couple application code to the Wordcell. An exact repeat is a no-op;\ndivergence, path escape, symbolic links, partial writes, and unapproved\nexternal surfaces stop the workflow.</p>\n<p>The repository's fake-capability suite exercises those transitions. It is a\ntested contract example, not proof that every agent or host integration\ncomplies.</p>\n<p>This workflow builds on Frank Chen's public notes about <a href=\"https://gist.github.com/fxchen/773397095d7a6bffda621e4237da0da9\">designing a personal\nknowledge base with an\nagent</a>\nand <a href=\"https://gist.github.com/fxchen/09cb410b22c9c5256d80243ee925b57e\">extending it with\nskills</a>.</p>\n<p>Wordcell ships no <code>kb_role</code> metadata, lifecycle resolver or API, lifecycle CLI,\ncompatibility diagnostic, or metadata migration. A frozen Phase 0 value gate\nmust show that those surfaces improve deterministic agent decisions before they\nare introduced. Current and historical plan routing remains derived from\nexisting type, path, and status conventions.</p>\n<h3 id=\"adopt-the-smallest-useful-split\">Adopt the smallest useful split</h3>\n<p>Start with a short inherited <code>AGENTS.md</code> path for rules whose omission would make an edit wrong. A small knowledge base may need only Markdown, Git, an index page, and ordinary file search. Add source capture when evidence keeps disappearing. Add repository scopes when agents need to recover current memory from code paths. Add metadata or hybrid search when file search stops answering the repository's questions. Add links and graph views only when the relationships themselves help people make decisions.</p>\n<p>Treat the knowledge base as repository-adjacent durable memory. Authored Markdown and Git are the record; catalogs, indexes, embeddings, and graph views are replaceable ways to find and inspect it. Checks can validate structure, captures can preserve a selected surface, and similarity can suggest candidates. None of those mechanisms proves that a source is trustworthy or an explanation is still true. People and agents must revise the knowledge as the repository changes.</p>\n",
  "comparisons": "<h1 id=\"choose-a-markdown-knowledge-workflow\">Choose a Markdown knowledge workflow</h1>\n<p>Wordcell is for people who want their coding agent to reuse decisions, follow\nauthored links, recover Git context, and publish selected notes from a local\nMarkdown knowledge base. It combines those operations in a CLI and TypeScript\nSDK. You can keep your text editor, Git workflow, and existing Markdown files.</p>\n<p>Local storage is a shared strength of this category. QMD, Basic Memory's local\nmode, and Obsidian also work with files on your machine. Choose by the workflow\nyou need, rather than by a claim that only one of these projects is local.</p>\n<p>This comparison was checked against the linked primary documentation on\nSeptember 19, 2026. It describes the documented products and standard workflows;\nplugins or custom scripts can add other behavior. The selection advice below is\nour assessment of those documented capabilities.</p>\n<h2 id=\"at-a-glance\">At a glance</h2>\n<table>\n<thead>\n<tr><th>Approach</th><th>A good fit when you want…</th><th>What Wordcell adds or changes</th></tr>\n</thead>\n<tbody>\n<tr><td>Plain Markdown with your editor, Git, and <code>rg</code></td><td>Portable files and a small toolset you already know.</td><td>Consistent metadata queries, derived backlinks and typed relations, repository-scope context, bounded agent handoffs, and a publication workflow.</td></tr>\n<tr><td>QMD</td><td>Local search over document collections, with keyword, vector, and hybrid retrieval.</td><td>Uses QMD as an optional retrieval layer, then joins results to current authored metadata, explicit graph context, and bounded Git provenance.</td></tr>\n<tr><td>Basic Memory</td><td>A Markdown knowledge graph that AI assistants can read and update through MCP.</td><td>A headless CLI/SDK workflow centered on repository scopes, code-mode composition, explicit Git evidence, and static publication.</td></tr>\n<tr><td>Obsidian</td><td>An interactive editor for a local vault, with links, graph navigation, and optional publishing.</td><td>Agent-oriented operations over compatible Markdown; it can accompany your editor. Wordcell does not supply a desktop note editor.</td></tr>\n<tr><td>Quartz</td><td>A customizable website or digital garden built from Markdown.</td><td>Publishes selected slices directly from the same agent knowledge workflow, with paths, metadata, tags, or graph neighborhoods as selectors.</td></tr>\n</tbody>\n</table>\n<h2 id=\"start-with-markdown-when-it-already-solves-the-problem\">Start with Markdown when it already solves the problem</h2>\n<p>Markdown is a plain-text document format. Its readability and ordinary files\nare part of Wordcell's foundation, not a limitation to replace. The format\nitself does not define a search service, repository-context router, or publishing\npolicy. <a href=\"https://spec.commonmark.org/0.31.2/\">CommonMark's specification</a>\ndescribes the document syntax.</p>\n<p>A small set of notes and <code>rg</code> may be enough. Wordcell becomes useful when you\nrepeatedly reconstruct backlinks, filter frontmatter, connect a decision to a\nrepository path, or prepare the same context for another agent session. It adds\nthose operations while keeping Markdown authoritative. There is no evidence\nhere that Wordcell beats a carefully scripted Markdown workflow on speed or\nanswer quality.</p>\n<h2 id=\"use-qmd-for-retrieval-or-use-it-inside-wordcell\">Use QMD for retrieval, or use it inside Wordcell</h2>\n<p><a href=\"https://github.com/tobi/qmd\">QMD</a> provides on-device BM25 keyword search,\nvector search, query expansion, and reranking. Its CLI, SDK, and MCP interfaces\nreturn structured results; it supports collection context, metadata filtering,\nsnippets, and bounded document retrieval. Those are substantial agent features.</p>\n<p>Wordcell's optional semantic search uses a pinned QMD implementation. Its\nadditional workflow combines current Markdown metadata with authored\nrelationships, repository scopes, graph neighbors, and Git history. Search\nevidence keeps exact matches and QMD matches inspectable; graph neighbors and\nhistory remain context instead of silently changing relevance scores.\nSee <a href=\"/docs/design\">the design</a> and <a href=\"/docs/agent-workflow\">agent workflow</a>.</p>\n<p>If all you need is local retrieval, standalone QMD may be the shorter path.\nChoose Wordcell when maintaining and reusing the knowledge around retrieval\nmatters too. We have not published a head-to-head retrieval-quality or latency\nbenchmark. The <a href=\"/docs/evidence\">payload demonstration</a> compares full-note and\nsnippet handoffs within Wordcell.</p>\n<h2 id=\"consider-basic-memory-for-an-mcp-centered-knowledge-graph\">Consider Basic Memory for an MCP-centered knowledge graph</h2>\n<p><a href=\"https://docs.basicmemory.com/concepts/knowledge-format\">Basic Memory's knowledge format</a>\nuses Markdown, observations, and relations to derive a connected knowledge\ngraph. Its <a href=\"https://docs.basicmemory.com/local/mcp-tools-local\">local MCP tools</a>\ninclude note reading, writing, editing, search, and graph context. Its\n<a href=\"https://docs.basicmemory.com/reference/mcp-tools-reference\">current tool reference</a>\nalso documents text, vector, and hybrid search plus structured filters and\nbounded graph traversal. Local mode and a cloud service are available.</p>\n<p>Both projects preserve editable Markdown and expose connected knowledge to\nagents. Wordcell's emphasis is repository work: exact authored path scopes,\n<code>AGENTS.md</code> context routing, Git provenance, deterministic graph maintenance,\nand composable read-only SDK sessions. Basic Memory is worth considering when\nyou prefer its MCP tools and observation/relation format. Review both formats\nbefore sharing the same authored vault between them; Markdown compatibility\ndoes not imply identical metadata conventions.</p>\n<h2 id=\"keep-obsidian-as-an-editor\">Keep Obsidian as an editor</h2>\n<p><a href=\"https://obsidian.md/\">Obsidian</a> is a local note-taking application with linked\nnotes and a plugin ecosystem. Wordcell is headless. You can edit an\nObsidian-compatible Markdown vault in Obsidian while using Wordcell for agent\nqueries and repository context; Wordcell does not replace Obsidian's editor.</p>\n<p><a href=\"https://obsidian.md/help/publish/publish\">Obsidian Publish</a> lets you select\ncontent for a hosted site. Its\n<a href=\"https://obsidian.md/help/publish/headless\">headless publishing commands</a> also\nsupport automation and require a Publish subscription. Wordcell instead emits\nordinary static files that you can host with a provider you choose. The hosting\nprovider may still charge you.</p>\n<h2 id=\"consider-quartz-when-the-website-is-the-main-product\">Consider Quartz when the website is the main product</h2>\n<p><a href=\"https://quartz.jzhao.xyz/\">Quartz</a> is a Markdown static-site generator with\nsearch, backlinks, graph views, and customizable layouts. It supports\n<a href=\"https://quartz.jzhao.xyz/features/private-pages\">private-page filtering</a>,\nincluding an explicit <code>publish: true</code> workflow. Selective publishing is not\nunique to Wordcell.</p>\n<p>Wordcell is useful when the published site is one output of an agent-maintained\nknowledge base. Its <a href=\"/docs/publish\">publish command</a> selects note paths, tags,\nmetadata, repository scopes, or bounded link neighborhoods, previews the\nselection, and emits a self-contained reader. Quartz may be a better fit if\ncustom website layouts and its publishing ecosystem are your main concern.\nWordcell's built-in reader deliberately has a smaller customization surface.</p>\n<h2 id=\"what-local-means-in-wordcell\">What “local” means in Wordcell</h2>\n<p>Authored Markdown and Git remain yours. Exact queries, graph operations, and\nstatic site generation run locally without a hosted model or account. Optional\nQMD search runs local models after setup. Package installation and model\ndownloads require network access. Opt-in <code>--rerank typesafe</code> sends bounded\ncandidate context to the external Jev provider; capturing a URL contacts that\nsource, and publishing files to a host makes the selected content available\nthere. A cloud agent can receive whatever context you send it. Local storage\ndoes not change the agent host's data handling. See\n<a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">security and data boundaries</a>.</p>\n",
  "evidence": "<h1 id=\"measure-a-smaller-context-handoff\">Measure a smaller context handoff</h1>\n<p>Wordcell can pass selected snippets to an agent before the agent opens full notes.\nIn the four-query example below, those handoffs contained <strong>79.98% fewer UTF-8\nbytes than the same matching notes in full</strong>: 12,126 bytes instead of 60,584.\nThis is a small, reproducible payload-size demonstration on Wordcell's public\nseven-note knowledge base. It does not measure tokens, answer quality, speed,\nor an advantage over another search tool.</p>\n<h2 id=\"what-was-measured\">What was measured</h2>\n<p>Each query uses the real <code>openKnowledgeBase().search()</code> API in <code>exact</code> mode,\nwith up to five results and graph and Git context disabled. The results pass\nthrough <code>packUntrustedSearchContext()</code> with a 12,000-byte ceiling. The measured\noutput includes its source paths, snippets, metadata, and explicit\nuntrusted-content envelope. No result was dropped by the packer's byte ceiling\nin this run. The search result limit still applies, and snippets omit most of\neach note's body.</p>\n<p>The baseline is the sum of the complete, original UTF-8 Markdown files selected\nby that same query, including their frontmatter. It adds no artificial padding\nor tool wrappers. A note selected by two queries counts twice, once for each\nseparate handoff.</p>\n<table>\n<thead>\n<tr><th>Fixed query</th><th align=\"right\">Selected notes</th><th align=\"right\">Full selected notes, bytes</th><th align=\"right\">Packed handoff, bytes</th><th align=\"right\">Reduction</th></tr>\n</thead>\n<tbody>\n<tr><td><code>selective publishing</code></td><td align=\"right\">2</td><td align=\"right\">19,303</td><td align=\"right\">2,322</td><td align=\"right\">87.97%</td></tr>\n<tr><td><code>repository memory</code></td><td align=\"right\">5</td><td align=\"right\">29,703</td><td align=\"right\">5,097</td><td align=\"right\">82.84%</td></tr>\n<tr><td><code>typed relationships</code></td><td align=\"right\">2</td><td align=\"right\">2,949</td><td align=\"right\">2,354</td><td align=\"right\">20.18%</td></tr>\n<tr><td><code>capture</code></td><td align=\"right\">2</td><td align=\"right\">8,629</td><td align=\"right\">2,353</td><td align=\"right\">72.73%</td></tr>\n<tr><td><strong>All four handoffs</strong></td><td align=\"right\"><strong>11 selections</strong></td><td align=\"right\"><strong>60,584</strong></td><td align=\"right\"><strong>12,126</strong></td><td align=\"right\"><strong>79.98%</strong></td></tr>\n</tbody>\n</table>\n<p>The aggregate is <code>100 × (1 − 12,126 / 60,584)</code>, rounded to two decimal places.\nIt weights by bytes; it is not the arithmetic mean of the percentages.\nThe small-note case saves 20.18%, which illustrates why one headline number\ncannot predict the savings on another vault.</p>\n<p>One read-only session scanned the seven-note vault once for all four queries.\nThe measurement opened zero semantic search sessions and used no model.\nWordcell still reads the corpus locally to build that snapshot. These numbers\ndescribe what crosses the context boundary, not reduced disk I/O. Passing every\nnote for every query would total 130,456 bytes; the raw report also records that\nseparate, less selective baseline.</p>\n<h2 id=\"reproduce-the-result\">Reproduce the result</h2>\n<p>The <a href=\"https://github.com/hraness/wordcell/blob/main/docs/product-evidence.json\">raw report</a> contains every selected path, each source\nfile's byte count and SHA-256, the full packed output, output hashes, the exact\noptions, and aggregate calculations. The\n<a href=\"https://github.com/hraness/wordcell/blob/main/scripts/product-evidence.ts\">measurement script</a> runs against the source SDK;\nits <a href=\"https://github.com/hraness/wordcell/blob/main/scripts/product-evidence.test.ts\">tests</a> cover UTF-8 counting, repeatable\nresults, corpus changes, empty matches, and negative savings.</p>\n<p>Recorded on September 19, 2026 using the source SDK at the snapshot below and\nBun 1.3.14. Its package manifest declared version 0.21.3; this is a source\nmeasurement, not verification of the immutable 0.21.3 release artifact. The\npublic corpus is <code>kb/</code> at commit\n<a href=\"https://github.com/hraness/wordcell/tree/454bfeca3f09680a0a9dc3a8f85d417684818ebc/kb\"><code>454bfeca3f09680a0a9dc3a8f85d417684818ebc</code></a>:\nseven scanned Markdown notes, including the index, totaling 32,614 bytes.\n<code>AGENTS.md</code> guides are excluded by the vault scanner. The corpus identity is\nSHA-256 over the compact JSON array of <code>{path, bytes, sha256}</code> records sorted by\npath, with no trailing newline:</p>\n<pre><code class=\"language-text\">19854ac834111f66cd4b1bbc0cfc8610f5b92312403d11d72dfee8fef8a95c9b\n</code></pre>\n<p>From a source checkout with its dependencies installed, measure its current KB:</p>\n<pre><code class=\"language-sh\">bun scripts/product-evidence.ts\n</code></pre>\n<p>To restore the frozen corpus into a temporary directory without changing your\nworking files, run these commands from the repository root:</p>\n<pre><code class=\"language-sh\">evidence_dir=&quot;$(mktemp -d)&quot;\ngit archive 454bfeca3f09680a0a9dc3a8f85d417684818ebc kb | tar -x -C &quot;$evidence_dir&quot;\nbun scripts/product-evidence.ts &quot;$evidence_dir/kb&quot; &gt; &quot;$evidence_dir/result.json&quot;\n</code></pre>\n<p>The script prints JSON and does not write the vault or a search index. Compare\n<code>corpus</code>, <code>configuration</code>, <code>cases</code>, and <code>aggregate</code> with the recorded report.\nThe <code>tool</code> field reports the installed source version, so a later release may\nhave a different version field even when its measured output is identical.\nHardware is not specified because this report makes no timing measurement.</p>\n<h2 id=\"use-the-measurement-correctly\">Use the measurement correctly</h2>\n<p>This demonstrates the cost of the first context handoff. It leaves the original\nnotes available for follow-up reading. If an agent then reads every selected\nnote, its total context can exceed the full-note baseline. Model token counts,\nprompt caching, tool wrappers, follow-up calls, and billing depend on the host;\nno fixed byte-to-token conversion is assumed.</p>\n<p>These four queries were chosen to exercise product topics. They are not an\nindependent or representative relevance test set. The small corpus consists of\nWordcell's own engineering notes and plans. A shorter payload does not establish\nthat it contains enough information to answer a question correctly.</p>\n<p>QMD and other tools also return snippets and bounded results. This report\ncompares two Wordcell handoff strategies, not Wordcell against QMD, Basic\nMemory, or a well-tuned <code>rg</code> workflow. See the <a href=\"/docs/comparisons\">comparison guide</a>\nfor differences in workflow and features.</p>\n<h2 id=\"retrieval-quality-needs-separate-evidence\">Retrieval quality needs separate evidence</h2>\n<p>The six-case synthetic rank-fusion fixture in <code>src/benchmark.ts</code> checks\ndeterministic behavior. It is not a retrieval-quality or performance benchmark\nand must not be cited as one. Wordcell provides an\n<a href=\"/docs/reference\">evaluation-builder API</a> for frozen corpora and relevance\njudgments. A competitive quality claim needs a published corpus, independently\njudged queries, pinned tool versions and settings, the raw rankings, and named\nhardware for any timing claims. No competitive quality or latency result is\nclaimed here.</p>\n",
  "graph-authority": "<h1 id=\"query-the-derived-graph\">Query the derived graph</h1>\n<p>Wordcell 0.21 adds named graph programs backed by the immutable Oh 0.4.3\nlibrary. Markdown is authoritative. Queries never add links or inferred\nrelationships to notes, and the existing <code>wordcell graph --json</code> report keeps\nits format.</p>\n<h2 id=\"query-without-creating-a-cache\">Query without creating a cache</h2>\n<p>Use an exact extensionless note ID. The default query creates an in-memory\nprojection of a complete bounded Markdown snapshot and closes it afterward.</p>\n<pre><code class=\"language-sh\">wordcell graph query --program backlinks --note notes/design --root kb --json\nwordcell graph query --program reachability --note notes/design --depth 3 --root kb --json\nwordcell graph query --program relation-closure --note notes/design --predicate depends-on --depth 3 --root kb --json\nwordcell graph query --program scope-route --scope src --root kb --json\n</code></pre>\n<table>\n<thead>\n<tr><th>Program</th><th>Returned columns</th><th>Meaning</th></tr>\n</thead>\n<tbody>\n<tr><td><code>backlinks</code></td><td><code>source</code>, <code>target</code>, <code>line</code>, <code>kind</code>, <code>predicate</code></td><td>Incoming resolved contextual links and local typed relationships.</td></tr>\n<tr><td><code>reachability</code></td><td><code>source</code>, <code>target</code>, <code>depth</code></td><td>Positive contextual and typed paths of lengths 1 through the requested depth.</td></tr>\n<tr><td><code>relation-closure</code></td><td><code>source</code>, <code>target</code>, <code>depth</code></td><td>Positive local relationship paths using one exact predicate.</td></tr>\n<tr><td><code>scope-route</code></td><td><code>note</code>, <code>scope</code></td><td>Exact authored <code>repository_scopes</code> declarations.</td></tr>\n<tr><td><code>shared-tags</code></td><td><code>note</code>, <code>other</code>, <code>tag</code></td><td>Positive tag evidence shared with another note.</td></tr>\n<tr><td><code>shared-concepts</code></td><td><code>note</code>, <code>other</code>, <code>concept</code></td><td>Positive evidence that two ordinary notes connect to the same concept, in either direction.</td></tr>\n</tbody>\n</table>\n<p>Depth defaults to 3 and is bounded at 8. A cyclic path may return its starting\nnote; the same target at different lengths is a different row. External\n<code>kb://</code> relationship targets remain external facts and never enter local path\nclosure. Broken and ambiguous links remain diagnostics in the existing graph\nreport. The configured catalog is excluded from graph relationships.</p>\n<p>There is no arbitrary Datalog text evaluator. These reviewed named programs\nuse positive rules. Wordcell computes absence, orphans and counts from a\ncomplete snapshot; an Oh proof does not prove that an authored claim is true.</p>\n<h2 id=\"rebuild-and-verify-local-state\">Rebuild and verify local state</h2>\n<pre><code class=\"language-sh\">wordcell graph rebuild --root kb --json\nwordcell graph verify --root kb --json\nwordcell graph query --program backlinks --note notes/design --root kb --persisted --json\n</code></pre>\n<p>Only rebuild writes <code>.wordcell/oh.sqlite</code> and its self-ignoring cache directory.\nIt serializes local rebuilds, applies changed records and deletions in a private\nstaging database, verifies replay, rechecks the Markdown revision, and atomically\ninstalls the result. A failed build leaves the previous database in place.\nThe operation never edits Markdown or initializes a vault.</p>\n<p>Verification and <code>--persisted</code> queries read a bounded copy into memory. They\nreject a missing, stale, foreign or corrupt cache; they do not repair it or\ncreate files. SQLite sidecars and symbolic or hard links are refused. Close any\nforeign writer before retrying. To rebuild a damaged disposable cache from\nMarkdown, explicitly request a fresh database:</p>\n<pre><code class=\"language-sh\">wordcell graph rebuild --fresh --root kb --json\n</code></pre>\n<p>The previous file stays in place until its replacement passes verification.\nA fresh rebuild also removes accumulated derived history. Oh supports atomic\nrecord updates, but its query engine evaluates the complete projection after\nan input change. Equal source snapshots produce equal rows and source proofs;\nhistory-bound projection identities can differ after a fresh rebuild.\nIn-memory projections use a fixed synthetic genesis time so identical snapshots\ncan reproduce and verify their proof results across sessions. That logical time\nis never evidence of when a note was authored or an operation occurred.</p>\n<h2 id=\"inspect-proof-and-limit-information\">Inspect proof and limit information</h2>\n<p>Each result identifies the vault, exact source revision, named request,\nprojection digest, evaluated limits, rows, and supporting proof trees. Fact\nproofs name the source note, its content digest and its exact Oh record digest.\nDerived proofs name the applied rule and its premises. A declared <code>document_id</code>\nretains record identity across renames; notes without one use an explicit path\nidentity. Wordcell never invents or writes IDs. Invalid or duplicate stable IDs\nand invalid scope metadata stop extraction.</p>\n<p>The graph boundary accepts at most 4,000 Markdown notes, 100,000 facts, and\n64 MiB of source text; individual query atoms are bounded at 16 KiB. Queries\nalso have explicit row, work, derived-tuple, round, result-byte and proof\nbudgets. <code>--limit</code> sets the row limit up to 1,000. SDK callers can reduce each\nbudget. Work exhaustion fails instead of claiming a complete answer. Row or\nproof truncation is retained in JSON and yields CLI exit code 4. Inspect\n<code>truncated</code>, <code>proofsTruncated</code>, and proof truncation nodes before using a result\nas complete evidence.</p>\n<pre><code class=\"language-ts\">import { openKnowledgeBase } from &quot;@hraness/wordcell&quot;;\nconst kb = await openKnowledgeBase({ root: &quot;kb&quot; });\ntry {\n  const result = await kb.graphQuery({\n    program: &quot;reachability&quot;, note: &quot;notes/design&quot;, depth: 3,\n    limits: { rows: 100, workUnits: 500_000 },\n  });\n  console.log(result.revision, result.rows);\n  console.log(await kb.graphVerifyResult(result));\n} finally {\n  await kb.close();\n}\n</code></pre>\n<p>A session intentionally retains one read-only snapshot. Reopen the session\nwhen Markdown changes. <code>graphVerifyResult</code> re-evaluates a bounded result\nagainst that session and rejects modified, foreign or stale evidence; it does\nnot authorize writing any fact into Markdown. The lower-level graph API is\navailable at <code>@hraness/wordcell/graph-authority</code>.</p>\n<h2 id=\"add-positive-proofs-to-percolation\">Add positive proofs to percolation</h2>\n<pre><code class=\"language-sh\">wordcell percolate notes/design --proofs --root kb --json\n</code></pre>\n<p>This opt-in result has kind <code>wordcell.graph-percolation</code>. <code>suggestions</code> retains\nthe existing percolation V2 candidates. <code>positiveSupport</code> contains separate\nshared-tag and shared-concept query results, each with its own limits and\nproofs. Missing concepts, absent edges, support counts, and predicate selection\nremain Wordcell or author decisions. A positive shared tag does not establish\nthat two notes need a relationship. Read the cited Markdown before editing.</p>\n<p>SDK callers use <code>kb.percolateWithProofs({ note: &quot;notes/design&quot; })</code> or\n<code>percolateWithGraph(snapshot, options)</code> from\n<code>@hraness/wordcell/graph-percolation</code>. No proof operation writes notes.</p>\n",
  "overview": "<h1 id=\"wordcell\">Wordcell</h1>\n<p><a href=\"https://github.com/hraness/wordcell/tree/main/skills/wordcell\">Install the Agent Skill</a></p>\n<p>A local Markdown knowledge base with superpowers. Save decisions, sources, and\nplans as files you own, then recover them through exact and semantic search,\ntyped relationships, backlinks, and Git history. Coding agents get the context\nbehind your code, and the same record holds everything else you need to\nremember.</p>\n<p>Your files stay yours: read them in Obsidian or any editor, review changes in\nGit, and rebuild every index from the Markdown. Exact search and graph queries\nrun on your machine with no account or model. Web capture, hosted agents, and\noptional remote reranking have <a href=\"#privacy-and-boundaries\">separate privacy boundaries</a>.</p>\n<p><a href=\"https://wordcell.io/docs\">Documentation</a> · <a href=\"/docs/comparisons\">Comparisons</a> · <a href=\"/docs/evidence\">Measured evidence</a> · <a href=\"https://github.com/hraness/wordcell/blob/main/CHANGELOG.md\">Changelog</a></p>\n<h2 id=\"why-wordcell\">Why Wordcell</h2>\n<ul>\n<li><strong>Keep what you learn in files you own.</strong> Write decisions, sources, and plans\nin Markdown. Obsidian, Git, and Wordcell read the same record, and every\nindex rebuilds from the files.</li>\n<li><strong>Find it by words or by meaning.</strong> Exact search needs no model or account.\nOptional local semantic search joins each match to current metadata, links,\nand history instead of returning isolated text.</li>\n<li><strong>Author the connections.</strong> Wikilinks and typed relationships turn notes into\na queryable ontology. Backlinks, bounded graph proofs, and percolation\nsuggestions recover them without injecting edges you did not write.</li>\n<li><strong>Ground coding agents in context.</strong> Scope notes to repository paths, recover\nthe commits behind a note, and give the next session only what it needs.\nOnly context you save becomes part of the record.</li>\n</ul>\n<p>Plain Markdown may be enough for a small set of notes. QMD is a good fit for\nlocal document retrieval and also supplies Wordcell's optional semantic search.\nWordcell adds a connected workflow for repository context, authored relationships,\nGit evidence, and selective publishing. <a href=\"/docs/comparisons\">Compare the tradeoffs</a>.</p>\n<h2 id=\"install\">Install</h2>\n<p><a href=\"https://bun.sh/docs/installation\">Bun 1.3.14 or newer</a> and Git are required.\nThe CLI and TypeScript SDK run with Bun. Install the versioned GitHub archive:</p>\n<pre><code class=\"language-sh\">bun add --global --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.0/hraness-wordcell-0.22.0.tgz\nwordcell --help\n</code></pre>\n<p>Prefer npm? The same release is <a href=\"https://www.npmjs.com/package/@hraness/wordcell\">mirrored there</a>:</p>\n<pre><code class=\"language-sh\">npm install --global --ignore-scripts @hraness/wordcell@0.22.0\nwordcell --help\n</code></pre>\n<p>Keep Bun in <code>PATH</code> for either installation. If your shell cannot find <code>wordcell</code>,\nadd your package manager's global executable directory to <code>PATH</code> and reopen the\nterminal. Optional semantic search, browser capture, and PDF tools have\n<a href=\"/docs/reference#review-lifecycle-scripts-before-enabling-optional-adapters\">additional prerequisites</a>.</p>\n<h2 id=\"keep-one-decision-available-to-the-next-session\">Keep one decision available to the next session</h2>\n<p>Run this from a directory where you want a new <code>kb/</code> folder. It creates one\nMarkdown note and finds it without downloading a model or contacting a service:</p>\n<pre><code class=\"language-sh\">wordcell init kb\nwordcell note create notes/parser-contract \\\n  --title &quot;Parser contract&quot; --type concept --tag architecture \\\n  --body &quot;Parser retries stop after three attempts.&quot; --root kb\nwordcell search &quot;parser retries&quot; --root kb --mode exact\n</code></pre>\n<p>The result includes <code>notes/parser-contract</code> and the saved retry constraint.\nOpen <code>kb/notes/parser-contract.md</code> to see the ordinary Markdown file. Wordcell\nadds a stable <code>document_id</code> in its frontmatter; you can edit the prose normally.</p>\n<p>Already have Markdown or an Obsidian vault? In <strong>v0.22.0 or newer</strong>, search\nthe existing folder without initialization or an <code>index.md</code> file:</p>\n<pre><code class=\"language-sh\">wordcell search &quot;a phrase from your notes&quot; --root /path/to/your/vault --mode exact\n</code></pre>\n<p>Replace the path and phrase with your own. You don't need to initialize,\nconvert, or move the existing files to search them.</p>\n<h3 id=\"use-with-a-coding-agent\">Use with a coding agent</h3>\n<p>After trying the CLI, install the public Agent Skill into a compatible agent,\nsuch as Claude Code, Codex, Cursor, or GitHub Copilot:</p>\n<pre><code class=\"language-sh\">bunx skills add hraness/wordcell#v0.22.0 --skill wordcell\n</code></pre>\n<p>Then ask:</p>\n<pre><code class=\"language-text\">Use Wordcell to search for &quot;parser retries&quot; in ./kb using exact mode.\nRead the matching note and explain the saved constraint.\n</code></pre>\n<p>The skill installs instructions, not a service. Installation does not create a\nvault, modify your notes, or grant an agent permission to access other accounts.\nThe agent still follows its own provider and data-handling settings.\n<a href=\"https://github.com/hraness/wordcell/blob/main/skills/wordcell/SKILL.md\">Inspect the skill</a>.</p>\n<h2 id=\"recover-the-stopped-session\">Recover the stopped session</h2>\n<p>Link a plan to the decision you saved:</p>\n<pre><code class=\"language-sh\">wordcell note create plans/parser-v2 \\\n  --title &quot;Parser v2&quot; --type plan \\\n  --body &quot;The plan implements [[notes/parser-contract|the parser contract]].&quot; \\\n  --root kb\nwordcell backlinks notes/parser-contract --root kb\n</code></pre>\n<p>The backlink result includes <code>plans/parser-v2</code>, so a later session can find the\nwork that depends on the constraint.</p>\n<p>For code-path lookup, add this field inside the existing frontmatter of\n<code>kb/notes/parser-contract.md</code>:</p>\n<pre><code class=\"language-yaml\">repository_scopes:\n  - packages/parser\n</code></pre>\n<p>From your repository root, use an actual path under that scope and inspect the\nreturned notes and inherited <code>AGENTS.md</code> guides:</p>\n<pre><code class=\"language-sh\">wordcell context packages/parser/src/index.ts --root kb --repo .\n</code></pre>\n<p>Commit the vault with your repository to preserve its history, then use\n<code>wordcell history notes/parser-contract --root kb --repo .</code> to inspect the\ncommits behind the note. History is optional and requires recorded Git commits.</p>\n<p>These views recover saved decisions, related work, rules, and provenance.\nThey do not reconstruct private chat or prove that the note is still correct.\nOpen the returned Markdown and guides before acting on them.</p>\n<h2 id=\"rerank-a-search-window\">Rerank a search window</h2>\n<pre><code class=\"language-sh\">wordcell search &quot;why releases use immutable archives&quot; --root kb --mode exact \\\n  --rerank typesafe --rerank-limit 25 --limit 5 --json\n</code></pre>\n<p>This optional hosted lane uses TypeSafe's pinned <code>jev-1.13.0</code> model. It sends\nbounded query and note snippets to the provider, needs a private local\ncredential, and incurs provider charges. Exact identities remain first; a\nprovider failure retains the baseline order with a diagnostic. See the\n<a href=\"/docs/reranking\">setup, SDK examples, measured results, and limits</a>.</p>\n<h2 id=\"what-you-can-do\">What you can do</h2>\n<table>\n<thead>\n<tr><th>Task</th><th>Command</th><th>Evidence and effects</th></tr>\n</thead>\n<tbody>\n<tr><td>Find a saved decision</td><td><code>wordcell search &quot;parser retries&quot; --root kb --mode exact</code></td><td>Reads current Markdown; no model or network request.</td></tr>\n<tr><td>Recover context for code</td><td><code>wordcell context packages/parser/src/index.ts --root kb --repo .</code></td><td>Returns scoped notes, plans, and inherited <code>AGENTS.md</code> rules.</td></tr>\n<tr><td>Inspect explicit connections</td><td><code>wordcell backlinks notes/parser-contract --root kb</code></td><td>Returns notes that link to the decision.</td></tr>\n<tr><td>Search by meaning</td><td><code>wordcell search &quot;retry policy&quot; --root kb --mode hybrid</code></td><td>Adds optional local QMD keyword and vector retrieval; model setup is required.</td></tr>\n<tr><td>Query with graph proofs</td><td><code>wordcell graph query --program scope-route --scope packages/parser --root kb</code></td><td>Returns bounded results tied to the source revision. Queries do not write a cache.</td></tr>\n<tr><td>Capture a source</td><td><code>wordcell clip https://example.com/article --output kb/articles</code></td><td>Reads the selected URL and writes a Markdown bundle with a capture receipt.</td></tr>\n<tr><td>Capture a PDF</td><td><code>wordcell pdf /absolute/path/to/document.pdf --output kb/articles</code></td><td>Preserves the original PDF and extracted evidence; Poppler is required.</td></tr>\n<tr><td>Check the vault</td><td><code>wordcell check --root kb</code></td><td>Reports structural and attachment problems without editing files.</td></tr>\n<tr><td>Publish selected notes</td><td><code>wordcell publish --root kb --out site/ --include notes/parser-contract --dry-run --json</code></td><td>Previews a static site selection locally; remove <code>--dry-run</code> to build it.</td></tr>\n<tr><td>Preview a site</td><td><code>wordcell serve --root site --port 8080</code></td><td>Serves a published site on a loopback static file server with the emitted <code>404.html</code> fallback.</td></tr>\n</tbody>\n</table>\n<p>Use <code>--json</code> for structured output and <code>wordcell --help</code> for the complete command\nsurface. <a href=\"/docs/reference#command-surface\">Full command reference</a>.</p>\n<h2 id=\"publish-a-selected-part-of-your-vault\">Publish a selected part of your vault</h2>\n<p>Preview the decision and plan from the example before writing an output folder:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include notes/parser-contract --include plans/parser-v2 --dry-run --json\n</code></pre>\n<p>Review the selection, then build and preview it:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site \\\n  --include notes/parser-contract --include plans/parser-v2\nwordcell serve --root site --port 8080\n</code></pre>\n<p>Open <code>http://127.0.0.1:8080</code>. You get readable pages, linked notes, and search\nthat runs in the browser. Upload the <code>site/</code> folder to your chosen static host\nwhen you want to share it; <code>publish</code> itself never uploads anything.</p>\n<p>For a repeatable slice, use path patterns or combine folders, tags, metadata,\ncode scopes, and linked neighborhoods:</p>\n<pre><code class=\"language-sh\">wordcell publish --root kb --out site-notes \\\n  --include-glob 'notes/**/*.md' --exclude-glob '**/draft-*' --dry-run --json\n</code></pre>\n<p>Preview reports show up to 20 selected IDs by default, with a total count and\nselection digest. They keep note bodies out of the agent's context. Use\n<code>--list-limit</code> to adjust that preview without changing what gets published. <code>publish: false</code> excludes a note, but selected\nprose and attachments still need review before sharing: selection is not secret\nredaction. <a href=\"/docs/publish\">Selection recipes and hosting guide</a>.</p>\n<h2 id=\"evidence-and-comparisons\">Evidence and comparisons</h2>\n<p>In a four-query example over a seven-note public vault, packed search snippets\nused <strong>80% fewer UTF-8 bytes</strong> than passing the same matching notes in full:\n12,126 versus 60,584 bytes. This measures context payload size, not tokenizer\ncounts, answer quality, latency, or a win over another search tool.</p>\n<p>In a separate public SciFact study, optional hosted Jev reranking placed a\njudged relevant result first for <strong>161 of 300 queries</strong>, versus <strong>101</strong> with\nWordcell exact search alone. It sends bounded context to a paid provider;\nthis is evidence on scientific abstracts, not a comparison with QMD or a\nguarantee for repository notes. <a href=\"/docs/reranking#evidence-and-limits\">Results and limits</a>.</p>\n<p>Wordcell's benefit is selecting relevant context and keeping its sources\ninspectable. Local ownership is also available in other tools, and Wordcell\ndoes not claim to beat QMD's retrieval quality or every Markdown workflow.</p>\n<p><a href=\"/docs/evidence\">Measured evidence</a>\nshows a reproducible public-vault example, with the inputs, output sizes, and\nlimits beside each result. <a href=\"/docs/comparisons\">The comparison guide</a>\ncovers Markdown alone, QMD, Basic Memory, Obsidian, and static publishing tools\nusing their own documentation. Choose the smallest workflow that meets your\nneeds.</p>\n<h2 id=\"how-the-files-fit-together\">How the files fit together</h2>\n<pre><code class=\"language-text\">repository/\n├── AGENTS.md                # rules that govern edits\n├── packages/parser/\n│   └── AGENTS.md            # rules scoped to this code\n└── kb/\n    ├── index.md             # authored or managed front door\n    ├── articles/            # captured sources and assets\n    ├── notes/               # maintained explanations\n    └── plans/               # decisions and outcomes\n</code></pre>\n<p>Markdown, YAML frontmatter, explicit wikilinks, and Git hold the record. Open\nthe same files in Obsidian, a text editor, or ordinary file-search tools.\nApplication code does not need to import Wordcell or its vault.</p>\n<p>QMD supplies optional local search. <a href=\"https://oh.computer\">Oh</a>, the Hraness\nrecord and memory kernel (<a href=\"https://github.com/hraness/oh\">source</a>), is embedded\nas a derived graph authority behind an engine-neutral port: only an explicit\n<code>wordcell graph rebuild --root kb</code> writes <code>.wordcell/oh.sqlite</code>, the file stays\nignored and rebuildable, and nothing flows from the projection back into notes.\nBacklinks and typed relationships come from authored links. Percolation\nsuggests connections for review and does not add inferred edges to notes.</p>\n<p>Graph proofs explain a supported derivation from a specific source revision.\nThey do not prove that a note is true or that a missing relationship cannot\nexist. <a href=\"/docs/graph-authority\">Graph queries and proof limits</a>.</p>\n<h2 id=\"build-with-the-typescript-sdk\">Build with the TypeScript SDK</h2>\n<p>Add the same immutable release to a Bun project:</p>\n<pre><code class=\"language-sh\">bun add --exact --ignore-scripts https://github.com/hraness/wordcell/releases/download/v0.22.0/hraness-wordcell-0.22.0.tgz\n</code></pre>\n<p>The SDK provides read-only vault sessions, metadata queries, search, graph\nproofs, Git context, and composable workflows. A session owns one snapshot;\nreopen it after Markdown changes. <a href=\"/docs/reference#graph-reference\">SDK and workflow examples</a>\nshow the public imports and lifecycle.</p>\n<h2 id=\"privacy-and-boundaries\">Privacy and boundaries</h2>\n<ul>\n<li>Structural queries and exact search read local files. Optional semantic\nsearch downloads its model on first use and runs locally. Optional Jev\nreranking sends the query and bounded candidate context to a remote provider;\nit is off by default.</li>\n<li>URL capture contacts the requested source. Signed-in capture uses only\nexplicitly selected browser state. Review the <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">security policy</a>\nbefore using it with private sources.</li>\n<li>An agent that reads the vault follows its own provider and data-handling\nsettings. Keep private records out of public repositories and outputs.</li>\n<li>Git history is opt-in. Saved notes preserve recorded context; Wordcell does\nnot reconstruct unsaved conversations or silently record every agent action.</li>\n</ul>\n<h2 id=\"documentation\">Documentation</h2>\n<p>The documentation follows the Diataxis split: a tutorial to learn the loop,\nhow-to guides for tasks, reference for exact interfaces, and explanation for\nthe design. Browse it on the <a href=\"https://wordcell.io/docs\">documentation index</a>.</p>\n<table>\n<thead>\n<tr><th>Read next</th><th>Purpose</th></tr>\n</thead>\n<tbody>\n<tr><td><a href=\"/docs/getting-started\">Get started</a></td><td>Learn the full loop on a first vault: save, find, connect, and publish one note.</td></tr>\n<tr><td><a href=\"/docs/agent-workflow\">Agent workflow</a></td><td>Set up, query, maintain, and revise repository memory.</td></tr>\n<tr><td><a href=\"/docs/reference\">Installation and command reference</a></td><td>Exact interfaces, SDK imports, optional adapters, and troubleshooting prerequisites.</td></tr>\n<tr><td><a href=\"/docs/capture\">Web capture</a> and <a href=\"/docs/pdf\">PDF capture</a></td><td>Save sources with provenance, assets, and explicit completeness limits.</td></tr>\n<tr><td><a href=\"/docs/publish\">Publish selected notes</a></td><td>Preview a slice, build a static site, and choose how to host it.</td></tr>\n<tr><td><a href=\"/docs/graph-authority\">Graph guide</a></td><td>Named queries, proofs, revisions, resource limits, and cache recovery.</td></tr>\n<tr><td><a href=\"/docs/portfolio\">Portfolio federation</a></td><td>Search only selected, authorized vaults.</td></tr>\n<tr><td><a href=\"/docs/design\">Design</a> and <a href=\"/docs/agent-memory\">memory rationale</a></td><td>File contracts, design choices, and evaluation context.</td></tr>\n<tr><td><a href=\"/docs/publishing#verify-a-published-release\">Release verification</a></td><td>Verify archive identity, signatures, and provenance.</td></tr>\n</tbody>\n</table>\n<h3 id=\"installation-reference\">Installation reference</h3>\n<p><a href=\"/docs/reference#installation-reference\">Alternative installs and optional capabilities</a>.</p>\n<h3 id=\"the-kb-vault-format\">The kb vault format</h3>\n<p>Existing <code>kb/</code>, <code>index.md</code>, <code>kb://</code> identifiers, and Markdown formats remain\ncompatible. <a href=\"/docs/reference#the-kb-vault-format\">Vault format reference</a>.</p>\n<h3 id=\"command-surface\">Command surface</h3>\n<p><a href=\"/docs/reference#command-surface\">Complete CLI reference</a>.</p>\n<h3 id=\"capture-reference\">Capture reference</h3>\n<p><a href=\"/docs/reference#capture-reference\">Capture commands and dependencies</a>.</p>\n<h3 id=\"graph-reference\">Graph reference</h3>\n<p><a href=\"/docs/reference#graph-reference\">Graph and SDK interfaces</a>.</p>\n<h3 id=\"agent-skills\">Agent skills</h3>\n<p><a href=\"/docs/reference#agent-skills\">Installation and runtime contract</a>.\nThe same skill is included at <code>node_modules/@hraness/wordcell/skills/wordcell/</code>.</p>\n<h2 id=\"release-notes\">Release notes</h2>\n<p>Version 0.22 adds selective static publishing and local preview. Choose notes,\nfolders, path patterns, metadata, or linked neighborhoods, then inspect a\nbounded selection report before building. Existing vaults need no migration.\nThe deprecated <code>kb</code> command was removed in 0.21.0; use <code>wordcell</code>.\n<a href=\"https://github.com/hraness/wordcell/blob/main/CHANGELOG.md\">All release notes and upgrade instructions</a>.</p>\n<h2 id=\"contributing\">Contributing</h2>\n<p>Run <code>bun run check</code> for source, architecture, package installation, types, and\nnative-helper validation. The site has its own <code>bun run check</code> under <code>site/</code>.\nSee <a href=\"https://github.com/hraness/wordcell/blob/main/CONTRIBUTING.md\">Contributing</a>, <a href=\"https://github.com/hraness/wordcell/blob/main/SECURITY.md\">Security</a>, and the <a href=\"https://github.com/hraness/wordcell/blob/main/LICENSE\">MIT License</a>.</p>\n<p><a href=\"https://github.com/hraness/wordcell/releases\">Install Wordcell from GitHub Releases</a> ·\n<a href=\"https://github.com/hraness/wordcell\">Wordcell source on GitHub</a> ·\n<a href=\"https://wordcell.io\">Wordcell overview</a></p>\n<h2 id=\"optional-development-support\">Optional development support</h2>\n<p><code>wordcell support</code> shows optional development support for Wordcell. Features stay\nfree and no account is required. Accounts presents current terms; the person\nconfirms any payment in their browser. Wordcell has no product newsletter and\ndoes not look up an email address for support.</p>\n<p>After useful standalone work, agents can receive a discovery record on stderr.\nRead <code>wordcell support protocol --json</code> for the shared closeout procedure.\nHelp, diagnostics, evaluation, SDK calls and nested commands stay quiet.\n<code>HRANESS_SUPPORT=off</code> or <code>HRANESS_SUPPORT_AUDIENCE=off</code> suppresses incidental\ninvitations. Explicit human terminal rendering requires\n<code>HRANESS_SUPPORT_AUDIENCE=human</code>; the default audience is an agent, including in\na pseudo-terminal.</p>\n<p><code>wordcell support dismiss</code> disables invitations across participating tools on\nthis machine. <code>snooze</code> pauses them for thirty days, <code>enable</code> restores them, and\n<code>status --json</code> shows their separate local preferences. These commands do not\nchange a vault, sign up or pay. Acknowledged invitations share a seven-day\ncooldown; discovery itself does not consume it.</p>\n",
};
